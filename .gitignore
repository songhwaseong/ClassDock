.claude/settings.local.json
ffmpeg.exe

# 빌드 산출물 (커밋된 소스로부터 재생성됨 — node build-offline.js + desktop/build.bat)
classdock-offline.html
ClassDock.exe
desktop/app.html

# 배포용 테스트 패키지 (pack.bat 로 재생성)
dist/

# exe 가 실행 중에 만드는 폴더 — 소스가 아니다(자동 설치한 JDK, 받아 둔 자바 라이브러리 jar)
jdk/
java-libs/

# 개발/테스트 의존성·산출물 (npm install · playwright)
node_modules/
package-lock.json
test-results/
playwright-report/

# 로컬 백업 스크립트 (커밋 안 된 변경분 zip 만들기)
backup-uncommitted.ps1
backup-uncommitted.bat

# 테스트가 db_worker.py 를 import 할 때 생기는 바이트코드 캐시
desktop/__pycache__/
