# .msheet 악보 문서 — 설계 문서

> 작성 2026-08-13 · 상태: **P0 구현 완료**(2026-08-13) · P1~P4 예정 · 목표: 동요 수준 단선율 악보를 도구상자로 작성하고, 눌러 듣고, 전체·부분 재생하고, 오디오 파일로 저장
> 구현: `src/js/music-model.js`·`src/js/music-audio.js` (P0) → `src/js/music-editor.js` (P1) + 접점(`file-loaders.js`·`documents.js`·`code-viewer.js`·`command-palette.js`·`lazy.js`·`scripts.manifest.json`·`classdock.html`·`styles.css`) · vendor: `vexflow.min.js` · 테스트: `tests/music-model.test.js`·`tests/music-audio.test.js`
> 선례: [.mnote 블록 문서 설계](mnote-design.md) — 새 문서 종류를 붙이는 경로를 그대로 따른다.

## 1. 목표와 범위

**한 화면에서 악보를 만들고, 만들면서 바로 듣는다.** 오선 위 자리를 클릭해 음표를 놓고, 도구상자에서 음표 길이·쉼표·임시표를 고르고, 음표 하나를 누르면 그 음이 나고, ▶로 전체 또는 고른 마디만 들려주고, 결과를 **WAV 파일로 저장**한다. 자체 확장자 **`.msheet`(JSON)** 로 저장·재편집한다.

### 결정된 네 가지 (2026-08-13)

| 항목 | 결정 | 이유 |
|------|------|------|
| 녹음 | **작성한 악보 → 오디오 파일 저장**(마이크 아님) | `OfflineAudioContext` 렌더 + WAV 인코딩으로 코덱 의존 없이 무손실. 실시간 대기 없음 |
| 수준 | **동요 단선율**(한 성부, 높은음자리표) | 교실 용도에 충분하고, 조판 난이도가 급감 |
| 렌더 | **VexFlow 도입** | 조판(기둥 방향·꼬리 잇기·간격 배분)을 검증된 코드에 위임 |
| 음색 | **실제 피아노·기타·실로폰·하프·플루트·클라리넷 + 오실레이터 신디사이저** | 실제 녹음은 자연스러운 소리, 합성음은 즉시 동작하는 가벼운 대안. 샘플은 해당 음색을 처음 재생할 때만 지연 로드 |

### 범위
- **포함**: 단선율 한 성부 · 높은음자리표 · 2/4·3/4·4/4·6/8 박자 · 조표(#·b 최대 2개) · 온음표~16분음표와 각 쉼표 · 점음표 · 임시표(#·b·♮) · 음표 클릭 소리 · 전체/부분 재생 · 템포 · WAV 저장 · `.msheet` 저장·열기 · 인쇄용 SVG
- **1차 제외**: 화음(한 자리 여러 음), 두 성부·대소보표, 붙임줄·이음줄, 가사, 셈여림, 반복기호·도돌이, 잇단음표, MusicXML, MIDI 입력

## 2. 데이터 모델 (.msheet 스펙)

`.msheet` 파일 = **UTF-8 JSON 한 개**. `.mnote`와 같이 판별 서명 + 버전을 앞에 둔다.

```jsonc
{
  "format": "classdock-sheet",      // 판별 서명
  "version": 2,
  "title": "학교종",
  "createdAt": 0, "updatedAt": 0,
  "tempo": 100,                     // BPM (40~208)
  "time": { "beats": 4, "beatValue": 4 },
  "key": "C",                       // C G D F Bb … (#·b 최대 2개)
  "clef": "treble",
  "timbre": "piano",                // piano(기본)·guitar·xylophone·harp·flute·clarinet·triangle·sine·square
  "measures": [
    { "id": "m-1", "notes": [
      { "id": "n-1", "rest": false, "step": "G", "octave": 4, "alter": 0,
        "value": "quarter", "dots": 0 },
      { "id": "n-2", "rest": true,  "value": "eighth", "dots": 0 }
    ]},
    { "id": "m-2", "lineBreakBefore": true, "notes": [] } // 선택: 이 마디 앞에서 새 오선 단 시작
  ]
}
```

### 음높이를 VexFlow 표기로 저장하지 않는 이유 — 결정

VexFlow는 음을 `"g/4"` 같은 문자열로 받는다. 그걸 그대로 파일에 저장하면 **파일 포맷이 렌더 라이브러리에 묶인다.** 대신 MusicXML과 같은 결의 `{ step, octave, alter }`로 저장하고, 경계에서 변환한다.

```
{step:"G", octave:4, alter:0}
        ├─→ musicVexNote()  → "g/4"   (화면)
        └─→ musicMidiNumber() → 67 → 391.995Hz (소리)
```

이 변환 두 개가 모델과 바깥을 잇는 **유일한 접점**이다. 나중에 렌더러를 바꾸거나 MIDI 내보내기를 붙일 때 모델은 그대로 둔다.

### 음표 길이 표

| `value` | VexFlow | 4분음표=480틱 기준 | 점 1개 |
|---------|---------|-----|--------|
| `whole` | `w` | 1920 | 2880 |
| `half` | `h` | 960 | 1440 |
| `quarter` | `q` | 480 | 720 |
| `eighth` | `8` | 240 | 360 |
| `16th` | `16` | 120 | 180 |

쉼표는 같은 `value`에 `rest:true`(VexFlow는 `"qr"`처럼 `r` 접미사). 틱은 정수로만 다뤄 부동소수 오차를 없앤다.

### 마디 채움 규칙
- 한 마디의 틱 합 = `beats × (1920 / beatValue)`. 4/4면 1920틱.
- **넘치면 입력을 막고** 안내한다(자동 마디 넘김은 P3). **모자라면** 남은 만큼 렌더 시 빈 자리로 두되, 재생 시에는 그 시간만큼 쉰다.
- 마지막 마디를 뺀 모든 마디가 정확히 차 있는지 `musicValidate()`가 검사하고, 저장은 막지 않되 상태 표시줄에 경고한다.

## 3. 화면 구성

```
┌ 상단바 ─────────────────────────────────────────────────┐
│ 제목  |  ♩=100  4/4  다장조  높은음자리표 |  ↶ ↷  💾  ⬇WAV │
├ 도구상자 ───────────────────────────────────────────────┤
│ [𝅝 𝅗𝅥 ♩ ♪ 𝅘𝅥𝅯] [ · 점] [𝄻 𝄼 𝄽 쉼표] [♯ ♭ ♮] [지우개] [마디+] [오선+] │
├ 악보(VexFlow SVG) ──────────────────────────────────────┤
│  &  ###  4/4  |  ♩  ♩  ♪ ♪  |  ♩  𝄽  ...                │
│                    ▲ 재생 중 이 음표 강조                  │
├ 재생 ──────────────────────────────────────────────────┤
│ ▶ 전체   ▶ 고른 마디(3~6)   ■ 정지   ♩=[100]  음색[△▽]    │
└────────────────────────────────────────────────────────┘
```

### 편집 조작
- **도구상자에서 길이를 고른 뒤 오선을 클릭** → 클릭한 세로 위치의 음높이로 음표 삽입. 오선 밖은 덧줄 2개까지(C4~A5 정도, 동요 음역).
- **음표 클릭** = 선택 + **그 음 소리**(요구사항의 "하나씩 눌러도 소리"). 선택 상태에서 ↑↓로 음 이동, ←→로 이웃 음표 이동, Delete로 삭제.

> **↑↓ 이동 단위 — 결정(2026-08-13, P2 구현 중 변경)**: 처음에는 "반음 이동"으로 적었으나 **흰건반 한 음(도→레→미)** 으로 바꿨다.
> 반음 이동은 임시표(alter)를 계속 바꿔 동요 편집에서 예측하기 어렵고, 화면상 음표가 제자리에 머무는 경우(F→F#)가 생긴다.
> 반음은 도구상자의 ♯·♭·♮ 버튼이 담당하고, **Shift+↑↓는 한 옥타브**로 둔다.
- **마디 클릭(음표 없는 여백)** = 마디 선택. Shift+클릭으로 범위 선택 → 그 범위가 "부분 재생" 대상.
- 도구상자 없이도 되게: 숫자키 1~5 = 온·2분·4분·8분·16분, `R` = 쉼표 토글, `.` = 점.

## 4. 아키텍처 접점 (파일별)

`.mnote`가 붙은 경로를 그대로 따른다. `check:manual`이 아니라 **`npm run check`(tools/check-source.js)** 가 매니페스트 정합성을 강제하므로 등록 누락은 바로 잡힌다.

> **이름 규칙 — 결정(2026-08-13)**: 코드에서는 `sheet*` 를 쓰지 않고 **`music*`** 로 통일한다.
> 이 코드베이스에서 `sheet` 는 이미 스프레드시트를 뜻한다(`spreadsheet-viewer.js` 의 `sheetBaseName` 등).
> 번들러 없이 전역을 공유하는 구조라 이름이 겹치면 조용히 덮어쓴다(`tools/check-source.js` 의 전역 충돌 가드).
> 파일 확장자만 `.msheet` 이고 파일·함수 이름은 `music-*`·`music*` 이다.

| # | 파일 | 추가/변경 |
|---|------|-----------|
| 1 | **`src/js/music-model.js`** ✅ P0 | 순수 모델: `musicEmpty`·`musicParse`·`musicSerialize`·`musicValidate`·`musicVexNote`·`musicMidiNumber`·`musicNoteTicks`·`musicTimeline`. DOM·오디오 의존 0 → node 테스트가 쉽다 |
| 2 | **`src/js/music-audio.js`** ✅ P0 | 공개 API `MNMusicAudio`. 오실레이터 신디사이저·lookahead 스케줄러·한 음 미리듣기·구간 재생·`OfflineAudioContext` 렌더·WAV 인코딩 |
| 3 | **`src/js/music-editor.js`** (P1) | 편집기: `loadMusicSheet(file, opts)`·`mountMusicEditor`·도구상자·VexFlow 렌더·선택/입력·되돌리기·저장·`newMusicScratch` |
| 4 | `file-loaders.js` `handleFiles` ([60행](../src/js/file-loaders.js:60) 부근) | `else if (ext === "msheet" && typeof loadMusicSheet === "function") made = await loadMusicSheet(file, opts);` 한 줄 |
| 5 | `documents.js` `makeDoc` | 새 `kind:"music"` 허용(el 클래스는 mnote와 같이 "office" 재사용). 통합검색은 제목만 노출(음표는 검색 대상 아님) |
| 6 | `code-viewer.js` `saveTextDoc` | 저장 경로 재활용 — `musicSerialize(sheet)` 문자열을 넘긴다. 원본 덮어쓰기·서버 저장·다운로드 3경로 공짜 |
| 7 | `code-viewer.js` `createScratchInFolder` ([3983행](../src/js/code-viewer.js:3983)) | 폴더 우클릭 → 새 악보. `.mnote`처럼 이름을 본문에 넣는 `makeContent(name)` 콜백 사용 |
| 8 | `command-palette.js` | "새 악보 (.msheet)" 명령 추가 → `newMusicScratch` |
| 9 | `lazy.js` `BUNDLES` | `vexflow: { label:"악보 그리기", files:["vexflow.min.js"] }` |
| 10 | `scripts.manifest.json` | `localScripts`(learning-tools 레이어 안 `mnote.js` 뒤) + `applicationLayers` 같은 자리 + `scriptDependencies`(`music-audio.js` → `music-model.js`) + `moduleBoundaries`(`music-audio.js` / `MNMusicAudio`) + `vendorScripts`(`vexflow.min.js`, `lazy:"vexflow"`, `sha384`). *P0에서 앞의 넷 등록 완료(consumers 는 P1에서 `music-editor.js` 추가)* |
| 11 | `classdock.html` | script 태그(매니페스트와 **순서까지 동일**해야 `npm run check` 통과). *P0 2개 등록 완료* |
| 12 | `styles.css` | `.music-*` (도구상자·선택 강조·재생 중 음표) |
| 13 | `vendor/licenses/` | `vexflow-<버전>.txt` (MIT) |
| 14 | `history.js` | `MNEditHistory.create`로 되돌리기 — mnote와 동일 패턴 |
| 15 | `i18n.js` | 새 명령·버튼 문자열 |
| 16 | `README.md`·`사용법.md` | 기능 소개(`사용법.html`은 `npm run build:manual` 생성물이라 직접 수정 금지) |

### 로드 패턴 (기존과 동일)
```js
async function loadMusicSheet(file, opts){
  const sheet = musicParse(await file.text());           // 실패 시 loadText 폴백
  await MNLazy.load("vexflow");                          // 이 문서를 열 때 처음 로드
  const doc = makeDoc("music", file.name, opts);
  doc.sheet = sheet;
  doc.render = async () => { doc.el.innerHTML = ""; mountMusicEditor(sheet, doc.el, doc); };
  refreshChrome();
  activateIfIdle(doc, opts);
  return doc;
}
```

## 5. 오디오 엔진 설계 (`music-audio.js`)

실제 악기 샘플과 합성음 예약을 `music-audio.js` 하나에 가둔다. 각 악기 녹음은 앱 음역(G3~C6)을 가까운 실제 샘플로 덮고 사이 음은 재생 속도로 보정한다. 플루트·클라리넷은 긴 음표에서 안정 구간을 반복하고 끝에서 릴리스한다.

### 5.1 신호 흐름

```
피아노·기타·실로폰·하프·관악기 → AudioBufferSourceNode(가까운 실제 녹음·playbackRate 보정) ─┐
합성음 → OscillatorNode(sine/triangle/square) ────────────────────┴→ GainNode → master Gain → destination
```

- **주파수**: `440 * 2 ** ((midi - 69) / 12)`
- **ADSR**: attack 0.01s / decay 0.06s / sustain 0.7 / release 0.08s. 게인을 **0에서 시작**해 램프로 올리고 내린다 — 안 그러면 딸깍 잡음이 난다.
- **동시 발음**: 단선율이라 최대 2개(앞 음의 release와 다음 음의 attack이 겹치는 순간)면 충분.
- **음색**: 실제 녹음인 `piano`를 기본으로 하고 `guitar`·`xylophone`·`harp`·`flute`·`clarinet`과 `sine`·`triangle`·`square` 합성음을 함께 노출.
- **지연 로드**: 실제 악기 MP3는 해당 음색의 첫 재생 때만 따로 읽고 디코딩하며, 단일 HTML에서는 악기별로 분리한 실행되지 않는 JSON 데이터 URL 블록에 포함한다.
- `AudioContext`는 **첫 소리 요청 때 생성**하고, 브라우저 자동재생 정책 때문에 **사용자 제스처 안에서 `resume()`** 한다. 문서를 여는 것만으로는 만들지 않는다.

### 5.2 스케줄링 — 정확한 템포의 핵심

`setTimeout`으로 음을 울리면 템포가 흔들린다. **`AudioContext.currentTime` 기준으로 미리 예약**한다.

```
25ms마다 도는 타이머 → 앞으로 100ms 안에 시작할 음들을 지금 예약(osc.start(t) / stop(t2))
```

메인 스레드가 파이썬 실행 등으로 잠깐 밀려도 이미 예약된 소리는 정확히 난다.

**재생 중 음표 강조는 오디오 콜백이 아니라 `requestAnimationFrame`에서** 한다. 예약할 때 만들어 둔 `[{noteId, startTime, endTime}]` 타임라인을 `ctx.currentTime`과 비교해 현재 음표에 클래스를 붙인다. 오디오 스레드에서 DOM을 만지지 않는다.

### 5.3 공개 API

```js
MNMusicAudio.previewNote(note, timbre)      // 음표 하나 미리듣기(클릭 시)
await MNMusicAudio.play(sheet, { from, to, onNote, onEnd }) // 피아노 첫 로드 뒤 재생
MNMusicAudio.stop()
MNMusicAudio.renderWav(sheet, { from, to }) // → Promise<Blob>
```

### 5.4 WAV 저장 — 들리는 것과 저장본을 같게

**핵심 설계: 예약 함수를 컨텍스트에 의존하지 않게 하나로 만든다.**

```js
function scheduleInto(target, destination, events, offset, timbre){ … }   // 실시간·오프라인 공용
```

- **재생**: `scheduleInto(liveCtx, liveCtx.destination, …)`
- **저장**: `new OfflineAudioContext(1, Math.ceil((총초 + 0.5) * 44100), 44100)`에 같은 함수를 태우고 `startRendering()` → `AudioBuffer` → **16bit PCM WAV 인코딩** → `Blob`

같은 코드가 두 경로를 만들므로 "들은 것과 다른 파일이 저장되는" 사고가 구조적으로 막힌다. 오프라인 렌더는 실시간을 기다리지 않아 3분 곡도 1초 안에 끝난다.

WAV 헤더(44바이트, RIFF/fmt /data)는 자체 인코더 40줄 남짓 — 외부 의존 없음.

**저장 경로 주의**: exe에서는 로컬 토큰 때문에 `<a href>`로 서버 파일을 받을 수 없다. 여기서는 메모리 Blob이라 `URL.createObjectURL` + `<a download>`로 충분하고, 원본 폴더에 떨어뜨릴 거라면 `saveTextDoc` 계열의 바이너리 저장 경로를 쓴다.

### 5.5 대기 화면(화면보호기) 충돌 — 확인된 문제와 해법

`screensaverBusy()`는 재생 중 미디어를 `document.querySelectorAll("video,audio")`로 검사한다([screensaver.js:154](../src/js/screensaver.js:154)). **Web Audio 소리는 `<audio>` 요소가 아니라 이 검사에 걸리지 않는다** → 긴 곡을 재생하는 동안 대기 화면이 덮을 수 있다.

**해법: `screensaver.js`를 고치지 않는다.** 같은 함수가 바로 위에서 `.is-running`을 "파이썬/노트북 실행 중"으로 이미 검사하므로([screensaver.js:151](../src/js/screensaver.js:151)), **재생 중에만 악보 편집기 컨테이너에 `is-running` 클래스를 붙였다 떼면** 기존 규칙이 그대로 우리를 보호한다.

## 6. VexFlow 사용 범위 — 경계를 좁게

VexFlow는 **그리기만** 담당한다. 소리·편집·저장은 우리 코드다.

- 넘기는 것: 음자리표·조표·박자표·마디별 `StaveNote` 배열. 꼬리 잇기는 `Beam.generateBeams()`, 간격은 `Formatter`에 맡긴다.
- **자동 줄바꿈은 VexFlow가 해 주지 않는다** → 한 줄에 4마디 고정으로 시작하고(동요는 대부분 4마디 프레이즈), P3에서 컨테이너 폭 기반 계산으로 넓힌다.
- **클릭 좌표 → 음높이 변환은 우리 몫**: `stave.getYForLine(line)`로 각 줄의 y를 얻어 표를 만들어 두고, 클릭 y에 가장 가까운 줄/칸을 고른다.
- 렌더는 **마디 단위 캐시 + 디바운스**. 편집마다 전체 재조판하면 저사양 교실 PC에서 느려진다.
- 버전은 **고정해서 `vendor/`에 내장**하고 `sha384`를 매니페스트에 등록한다. 최근 VexFlow는 TypeScript로 다시 쓰이며 API가 바뀌었으므로, 착수 시 배포본의 전역 이름·API·용량을 먼저 확인한다.

## 7. 단계별 구현 계획

1. **P0 — 모델·오디오 뼈대** ✅ *(2026-08-13 구현)*: `music-model.js`(모델·직렬화·틱·MIDI 변환·조표별 임시표·타임라인) + `music-audio.js`(오실레이터·ADSR·lookahead 예약·미리듣기·구간 재생·`OfflineAudioContext` 렌더·WAV 인코더). 매니페스트·HTML 등록까지 마쳐 `npm run check`(전역 충돌 가드 포함)와 `npm test`가 새 파일을 함께 검사한다. 테스트 13개: 학교종 앞 4마디 표본으로 타임라인·부분 재생·덜 찬 마디, 가짜 AudioContext 로 예약 시각·주파수·엔벨로프, WAV 헤더·샘플 변환. **남은 확인**: 실제 소리와 `OfflineAudioContext` 렌더는 브라우저가 있어야 하므로 P1에서 화면과 함께 듣고 확인한다(node 에는 Web Audio 가 없다).
2. **P1 — 문서 종류 + 보기** ✅ *(2026-08-13 구현)*: `src/js/music-editor.js` 신규 — `.msheet` 열기·저장(`saveTextDoc` 재사용), VexFlow 5.0.0 조판(한 줄 4마디, 줄마다 음자리표·조표 반복), 음표 클릭 미리듣기, 전체·구간(마디 번호) 재생과 재생 중 음표 강조, WAV 저장, 제목·빠르기·음색 변경, 박자 불일치 경고, 창 크기 변경 시 디바운스 재조판. 접점: `file-loaders.js`(.msheet 분기) · `command-palette.js` · `app.js`+HTML(사이드바 `새 악보`) · `documents.js`(폴더 우클릭 `+Ms`) · `lazy.js`(`vexflow` 묶음) · `scripts.manifest.json` · `styles.css`(`.music-*`). vendor: `vexflow-bravura.min.js`(710KB, MIT, 음악 글꼴 Bravura 내장 → 네트워크 불필요) + `vendor/licenses/vexflow-5.0.0.txt`. 대기 화면 차단은 설계대로 `.is-running` 클래스로 처리(`screensaver.js` 무수정). VexFlow 5 API는 배포본의 `.d.ts`로 확인(`Formatter.FormatAndDraw(ctx, stave, notes, { autoBeam, alignRests })`, `Voice`는 camelCase `numBeats`/`beatValue`, `Dot.buildAndAttach`). 테스트: `tests/music-editor.test.js` 7개(접점 계약). **남은 확인**: 화면·소리 실제 동작은 브라우저에서 열어 봐야 한다(node 로는 조판·오디오를 실행할 수 없다). 사용자 문서(`README.md`·`사용법.md`)는 편집까지 들어간 P2 뒤에 한 번에 쓴다.
3. **P2 — 편집** ✅ *(2026-08-13 구현)*: 도구상자(길이 5종·점/겹점·쉼표·♯♭♮·지우개·마디 추가/삭제), 오선 클릭 삽입(클릭 y → 줄 값 → 음높이는 `musicPitchFromStaveLine`), 선택·↑↓ 한 음 이동(Shift+↑↓ 한 옥타브)·←→ 이웃 음표·Delete 삭제, 음표 클릭 미리듣기, 마디 채움 검사로 넘치는 입력 차단, 음역(G3~C6) 밖 차단, 되돌리기(`MNEditHistory`, 스냅샷=악보 JSON 문자열, 제목 타자는 `commitSoon` 으로 묶음), 자판 단축키(1~5·R·`.`·Ctrl+Z/Y, 입력칸 안에서는 무시). 모델에 오선 자리 계산(`musicDiatonicValue`·`musicPitchFromStaveLine`·`musicShiftPitch`) 추가. 테스트: 모델 2개 + 편집기 계약 4개 추가. **남은 확인**: P1 과 마찬가지로 실제 조작·소리는 브라우저에서 봐야 한다.
4. **P3 — 다듬기** ✅ *(2026-08-13 구현)*: 박자표(2/4·3/4·4/4·6/8)·조표(다·사·라·바·내림나장조) 변경 UI, **폭 기반 자동 줄바꿈**(`musicPackLines` — 마디마다 음표 수로 필요한 폭을 어림잡아 줄에 채우고 남는 폭은 비례 배분), 인쇄, i18n(명령·사이드바 문구), 사용법 문서(`사용법.md` 5장 + 제한사항 표 + `README.md`). 단축키는 P2 에서 완료.
   - **조표 변경의 소리 규칙**: `musicRetuneForKey` — 임시표 없이 적혀 있던 음만 새 조표를 따라간다(다장조 F♮ → 사장조 F#). 일부러 붙인 임시표는 그대로 둔다. `alter` 가 실제 울리는 반음이라(§2) 표시와 소리가 함께 맞는다.
   - **박자 변경**: 마디 용량이 바뀌어 넘치는 마디가 생길 수 있다. 막지 않고 경고만 띄운다(되돌리기로 취소).
   - **인쇄를 새 창·iframe 으로 하지 않은 이유 — 결정**: VexFlow 5 는 음표를 **Bravura 글꼴의 글자**로 그린다. SVG 만 다른 문서로 옮기면 그 문서에는 글꼴이 없어 악보가 깨진다. 화이트보드 `printBoard` 와 같은 방식으로 **같은 문서 안에 인쇄용 층(`.music-print`)을 만들고 나머지를 `@media print` 로 숨긴다**. 머리말 🖨 버튼도 `doc.printScore` 로 같은 경로를 탄다.
   - **PDF 내보내기를 따로 만들지 않은 이유**: 같은 글꼴 문제로 SVG→PDF 변환(pdf-lib)은 글리프까지 심어야 한다. 인쇄 대화상자의 `PDF로 저장`이 같은 결과를 정확한 글꼴로 만들어 준다. 별도 `.svg` 저장도 같은 이유로 두지 않았다.
5. **P4(선택, 일부 구현)** — 실제 피아노·기타·실로폰·하프·플루트·클라리넷 샘플 음색과 MusicXML은 구현 완료. MIDI(.mid) 내보내기, 붙임줄·가사·반복기호, 화이트보드·시험지에 악보 삽입은 남음.
6. **조옮김(전조)** ✅ *(2026-08-13 구현)* — `musicTransposeSheet`(모델) + 머리말 `조옮김` 버튼·우클릭 메뉴(`transposeContextItems`). §11 참조.

## 8. 테스트 계획

`node --test tests/*.test.js`로 도는 순수 로직 위주(브라우저 없이).

| 파일 | 확인 |
|------|------|
| `tests/music-model.test.js` | 직렬화 안정성(같은 모델 → 같은 JSON), 버전·서명 거부, 마디 틱 합 검사, 점음표 틱, `{step,octave,alter}` → MIDI → 주파수, VexFlow 키 문자열 변환 |
| `tests/music-audio.test.js` | BPM·박자별 타임라인 시작시각 계산, 구간 재생 시 시작 오프셋, WAV 헤더 44바이트·샘플 수·채널 수 (`OfflineAudioContext`가 없는 node에서는 인코더와 타임라인만) |

E2E(playwright)는 P2 이후: 새 악보 만들기 → 음표 3개 넣기 → 저장 → 다시 열기.

## 9. 리스크·결정 필요

| 리스크 | 크기 | 대응 |
|--------|------|------|
| VexFlow 최신 API 확인 필요(전역 이름·팩토리 방식) | 중 | P1 착수 전 배포본 실물 확인. 고정 버전 내장 |
| 클릭 좌표 → 음높이 매핑의 정확도 | 중 | `getYForLine` 기반 표 + 스냅. P2에서 실제 조작 검증 |
| 편집마다 전체 재조판 시 성능 | 중 | 마디 단위 캐시 + 디바운스. 저사양 PC에서 20마디 기준 확인 |
| 오프라인 HTML 용량(현재 20MB) | 소 | VexFlow는 `MNLazy` 지연 로드 → 시작 시간 영향 없음 |
| 재생 중 대기 화면 | 해결됨 | `.is-running` 클래스 재사용(§5.5) |
| 소리가 "삑삑" 들려 실망 | 해결됨 | 여섯 가지 실제 악기 녹음을 선택지로 제공하고 합성음은 가벼운 대안으로 유지 |
| exe 재빌드 | — | 코드 반영 시 오프라인 HTML → `desktop\build.bat` → exe (프로젝트 규칙) |

### 아직 열려 있는 결정
- **확장자 이름**: `.msheet` 제안(`.mscore`는 MuseScore와 혼동). 확정 필요.
- **음역 상한**: 덧줄 2개(대략 C4~A5)로 잡았는데, 동요 반주까지 생각하면 넓혀야 할 수 있음.
- **부분 재생 단위**: 마디 범위로 잡았음. 음표 단위 구간이 필요하면 P2에서 확장.

## 10. 학생 연습 편의 기능 (2026-08-13)

- 재생 속도 50%·75%·100%, 한 마디 카운트인, 메트로놈, 고른 마디 반복을 보기 상태로 제공한다. `.msheet` 내용과 WAV 내보내기에는 연습 설정과 클릭음을 섞지 않는다.
- 선택한 마디의 사용·남은·초과 박자를 계속 표시하고, 정확히 채워졌을 때 완료 상태를 보여 준다. 표현 가능한 남은 길이는 “점 2분음표나 같은 길이 쉼표 하나”처럼 해결 방법까지 안내한다.
- 오선 클릭 대신 4·5옥타브 `도·레·미·파·솔·라·시` 버튼으로 입력할 수 있다. 현재 위치부터 들어갈 수 있는 마디를 찾고 모두 차 있으면 새 마디를 만든다.
- `학교종`·`작은별` 4마디 예제를 모델 함수로 만들며 기존 내용 교체 전에는 확인한다. 교체도 편집 이력 한 단계라 되돌릴 수 있다.
- 악보 전용 음량·음소거는 보기 상태로 두고, 음색 선택 시 C4를 미리 들어 샘플을 바로 비교하게 한다.

## 11. 조옮김(전조) — 2026-08-13

교실에서 가장 자주 나오는 요구가 "아이들 목소리에 맞게 두 음 올려 주세요"인데 그때까지는 방법이 없었다.
조표 선택(`musicRetuneForKey`)은 **음표를 제자리에 두고 조표만** 갈아끼우는 반대 기능이라, 이걸로는 노래 높이를 못 바꾼다.

### 왜 "반음 몇 개"만으로는 부족한가 — 결정

반음 수는 **소리**만 정하고 **적는 법**은 정하지 않는다. 다장조를 반음 올리면 올림다장조(♯7개)와 내림라장조(♭5개)가
같은 소리다. 그래서 순서를 뒤집어, **새 조표를 먼저 정하고 그 조표에 맞춰 음을 적는다.**

```
1) 새 조표      5도권에서 fifths + 7×반음 → 12로 나눈 나머지가 같은 후보 중 |fifths| 가 작은 쪽
                (같으면 올릴 때는 ♯쪽) → 다장조 +1반음 = 내림라장조
2) 이동 칸수    새 으뜸음이 음이름으로 몇 칸 움직였나(D − C = 1칸)
3) 음 하나하나  이름을 그 칸수만큼 옮기고, 남는 반음 차이를 alter 로 적는다
                C4(60) → 이름 D4(62) → alter = 61 − 62 = −1 → D♭4
```

이름을 한 옥타브(7칸) 어긋나게 잡으면 `alter` 가 12씩 튀므로 **`-2~2` 안에 드는 자리는 하나뿐**이다.
그래서 후보 세 개만 훑으면 답이 유일하게 정해진다(`musicTransposePitch`).

### 지키는 것

| 항목 | 규칙 |
|------|------|
| 범위 | 위아래 한 옥타브(`MUSIC_TRANSPOSE_LIMIT = 12`). 그 이상은 옥타브를 겹쳐 쓰면 된다 |
| 대상 | 네 성부(오른손·왼손 × 성부 1·2) 전부 + 화음음 + **마디별 `keyChange`** 까지 같은 간격으로 |
| 음역 밖 | 막지 않고 **먼저 물어본다**(박자 바꾸기와 같은 규칙). 되돌리기 한 단계로 취소 |
| 적을 수 없는 음 | 옥타브 0~9를 벗어나면 **하나라도 있으면 전부 취소**(`blocked`). 일부만 옮긴 악보는 고치는 것보다 다시 그리는 게 빠르다 |
| 미리보기 | `musicTransposeSheet(sheet, n, { apply:false })` — 같은 코드로 세어만 본다(경고용 계산을 따로 만들지 않는다) |

### 화면

머리말 `조표` 옆의 **조옮김** 버튼과 악보 우클릭 메뉴가 **같은 목록(`transposeContextItems`)** 을 연다.
간격마다 도착할 조표를 미리 적어(`온음(장2도) 올리기 — 라장조`) 누르기 전에 결과를 알 수 있고,
`prompt()` 로 숫자를 받지 않는다(마디 설정의 프롬프트 연속 입력이 남긴 교훈).

`musicPitchFromMidi` 는 이때 만든 표기 함수인데, MIDI 건반 입력에도 같은 표(편집기에 중복돼 있던 것)를 쓰도록 합쳤다.

## 12. 자동 복원 — 저장했는데 다음 실행에 빈 악보가 되던 문제 (2026-08-13 수정)

**증상**: 음표를 넣고 `Ctrl+S` 로 저장한 뒤 앱을 껐다 켜면 그 악보가 **만들 때의 빈 상태**로 열렸다.

**원인**: 자동 복원은 문서를 **열 때 담아 둔 File 바이트**로 되살린다(`workspace-store.js`).
`saveTextDoc` 은 **디스크에만** 쓰고 이 사본은 건드리지 않으며, `readRestoredLocalFile` 이 디스크 최신본을
다시 읽어 오는 확장자는 `.py/.txt/.db` 뿐이라 `.msheet` 는 항상 저장된 사본을 쓴다.
새 악보의 그 사본은 `newMusicScratch` 가 만든 **빈 악보**였다 → 아무리 저장해도 되살아나는 건 빈 악보.

**해결**(다른 문서 종류가 이미 쓰던 공용 헬퍼 두 개를 그대로 붙였다. `workspace-store.js` 는 고치지 않는다):

| 시점 | 부르는 것 | 하는 일 |
|------|-----------|---------|
| 저장 성공 직후 | `markDocumentSavedSnapshot(doc, bytes, "application/json")` | `doc.sourceFile` 과 작업공간 사본을 저장한 내용으로 교체 |
| 편집이 1.5초 멈췄을 때 | `saveDocumentRecoverySnapshot(doc, bytes, "application/json")` | 저장 전에 꺼져도 되살아나게 복구본만 남긴다(원본 파일은 안 건드림) |

`doc.flushBackupRecovery` 도 붙여 백업·종료 경로가 마지막 상태를 한 번 더 밀어 넣게 했다(`backup.js`).

> **주의**: `saveTextDoc` 을 쓰는 문서는 저장만으로 복원 사본이 갱신되지 않는다.
> 새 문서 종류를 붙일 때는 **저장 직후 `markDocumentSavedSnapshot`** 을 잊지 말 것.

같은 구멍이 `.mnote`(`mnote.js` `saveMnote`)와 코드 편집기(`js-editor.js` 저장 버튼·자동 저장)에도
좁게 남아 있었다 — 복구본 디바운스(1.5초) 안에 저장하면 타이머가 `hasUnsavedEdits=false` 를 보고
건너뛰어 그 마지막 편집분만 사라졌다. 2026-08-13 같은 헬퍼로 함께 맞췄다.

## 13. 도구막대 접기 (2026-08-15)

악보 위에 줄이 넷(머리말·편집 도구·쉬운 입력·재생) 쌓여 있어 노트북 화면에서는 정작 오선이 좁았다.
**아래 세 줄만 접고 머리말(제목·저장·되돌리기)은 남긴다.**

- **접어도 기능을 잃지 않는 이유**: 우클릭 메뉴(`scoreContextItems`)가 이미 도구막대를 거의 전부 덮고
  있었다(입력 도구·악보 구조·조옮김·재생·연습·보기 배율·저장·내보내기). 이번에 유일하게 비어 있던
  **MIDI 건반 입력**을 `nextInputContextItems` 에 넣어 구멍을 없앴다.
- **`hidden` 으로만 감춘다 — 결정**: 요소를 지우면 메뉴가 읽는 값(속도·쉬운 입력 옥타브·재생 구간·음량·
  `wavBtn.disabled`)이 함께 사라진다. 감추기만 하면 그 값들이 그대로 살아 있어 메뉴 항목을 다시 만들
  필요가 없다. 전역 `[hidden]{display:none!important}`(`styles.css`)가 있어 CSS 추가도 없다.
- **재조판**: `ResizeObserver(scoreHost)` 가 이미 붙어 있어 높이가 바뀌면 알아서 다시 그린다(수동 호출 없음).
- **기억은 `localStorage.musicToolbarVisible`**: 배율(`scoreZoom`)과 같은 보기 상태라 `.msheet` 와
  되돌리기 스냅샷에는 넣지 않는다. 화이트보드 `wbToolbarVisible` 과 같은 규칙.
- **다시 펴는 길 셋**: 머리말 `▤ 도구 보이기` 단추 · 악보 우클릭 메뉴 · 자판 `H`. 화이트보드는 우클릭
  메뉴 하나뿐이라 못 찾는 사람이 있었다 — 악보는 머리말을 남겨 단추가 늘 보이게 했다.
- **⛶ 전체화면에서는 머리말도 접는다 (2026-08-15 추가)**: 창 모드에서 머리말을 남기는 이유는 "다시 펴는
  단추가 보여야 해서"인데, 전체화면은 나가는 길(`Esc`·⛶ 컨트롤)이 따로 있고 화면을 꽉 채우는 것이 목적이라
  `bar.hidden = fullscreenNow && !toolbarVisible` 한 줄로 머리말까지 접기 대상에 넣었다.
  - 들어갈 때 **자동으로 접고**(`toolbarBeforeFullscreen` 에 담아 둔다) 나가면 되돌린다. 이 임시 접기는
    `localStorage` 에 쓰지 않는다 — 전체화면 한 번이 평소 설정을 바꿔 버리면 안 된다.
  - 전체화면 안에서 `H`·우클릭으로 직접 고르면 그 값이 이기고(`setToolbarVisible` 이 `toolbarBeforeFullscreen`
    을 지운다) 평소 설정으로도 저장된다 — 나갈 때 되돌아가 사용자의 선택이 뒤집히는 일을 막는다.
  - **판정은 `isViewerFullscreen()`(documents.js)** 하나로 통일한다. 실제 전체화면은 `fullscreenchange` 로
    알 수 있지만 **창 안 폴백(`body.viewer-fullscreen`)은 이벤트가 없어**(`setViewerFullscreenFallback`)
    body 클래스 변화를 `MutationObserver` 로 함께 지켜본다. 둘 다 `doc.cleanupFns` 에서 뗀다.
- `Ctrl+S` 는 `.run-save` 를 `querySelector` 로 찾아 누르므로(`app.js`) 접어도 영향이 없다.

## 14. 따라치기(음 맞추기) — 2026-08-24

파이썬 편집기의 **코드 따라치기**(`python-editor.js`)를 악보로 옮긴 연습 모드다.
악보를 흐린 교본으로 깔고, 그 위에 학생이 자판을 건반 삼아 음을 차례로 눌러 본다.

### 왜 "음만" 맞추는가 — 결정

박자까지 채점하는 실시간 모드(반주에 맞춰 치기)는 성격이 다른 기능이다. 오디오 출력 지연과
입력 지연을 함께 보정해야 하고, 마우스로 누르는 도레미 버튼으로는 아예 성립하지 않으며,
한 번 밀리면 그 뒤가 통째로 무너진다. **먼저 시간 개념이 없는 "음 맞추기"만** 만든다 —
이쪽이 코드 따라치기와 1:1로 대응하고, 초등 교실에서 바로 쓸 수 있다.

### 교본 = `musicPracticeSteps(sheet, {from, to, staff})` (music-model.js)

재생 타임라인을 **"동시에 울리는 음 묶음"** 하나씩으로 잘라 차례를 만든다. 코드 따라치기의
교본 문자열에 해당하는 자리이고, 순수 모델이라 `node --test` 로 규칙을 검증한다.

| 코드 따라치기 | 악보 따라치기 |
|------|------|
| `target` 교본 문자열 | `steps[]` — 눌러야 할 차례 배열 |
| `pos` 캐럿 | `practice.pos` 차례 번호 |
| `marks[i]` 맞음·틀림 | `practice.state[i]` (0 아직 / 1 맞음 / 2 틀렸다 통과) |
| `tp-ghost` 흐린 교본 | `.music-score.is-practice` 가 음표를 흐리게, 지나온 것만 `mp-ok`/`mp-bad` |
| 줄 앞 들여쓰기 자동 통과 | **쉼표 자리(`auto:true`) 자동 통과** |
| 한/영 폭 어긋남 방어(`wideChar`) | **필요 없음** — 본문을 건드리지 않으니 정렬이 어긋날 일이 없다 |
| `getValue()` 로 교본 지키기 | **필요 없음** — `sheet` 를 한 글자도 안 고친다 |

- **옥타브는 통과시킨다**: 비교는 `pcs`(옥타브를 뺀 음이름 0~11)로만 한다. 들려줄 때는
  `midis`(실제 높이)를 써서 학생이 제 옥타브의 소리를 듣는다.
- **도돌이는 펼치지 않는다(`repeats:false`)**: 같은 음표가 두 번 나오면 화면의 맞음·틀림 표시가
  서로를 덮어써 지금 어디를 누르는지 알 수 없다.
- **붙임줄은 한 번만 누른다**: 타임라인이 이미 소리를 하나로 합치므로(`tiedIds` 로 음표 id 를
  함께 들고 간다) 한 번 눌러 이어진 음표를 모두 칠한다.

### 채점 규칙 — 코드 따라치기와 다른 한 곳

틀린 키는 **진도를 나가지 않는다**. 글자는 틀린 것을 그 자리에 보여 줄 수 있어 관대 모드가
성립하지만, 음은 그럴 수 없어 진도까지 나가면 학생이 노래의 어디인지를 잃는다. 대신 지금 차례를
빨갛게 두고 실수로 세며, 무엇을 눌렀는지 소리와 안내로 알려 준다. 맞게 누르면 진도가 나가되
빨간 표시는 남고(정확도에 반영), `Backspace` 로 한 음 되돌아가면 그 표시가 지워져 정확도가
도로 올라간다 — 이 되돌리기 규칙만은 코드 따라치기와 같다.

### 자판은 `event.code` 로 읽는다 — 결정

`event.key` 로 읽으면 한글 입력 상태에서 letter 키가 자모로 들어와 코드 따라치기가 한/영으로
겪은 문제를 그대로 되풀이한다. `event.code`(`KeyA`·`Digit1`)는 **자판의 자리**라 입력기 상태와
무관하다. 배열은 피아노 그대로 — 흰건반 `A S D F G H J`, 검은건반 `W E T Y U`, 숫자 `1~7` 도 같다.

### 입력 경로를 하나로 모은다

자판·**쉬운 입력의 도레미 버튼**·**MIDI 건반** 셋이 모두 `practicePress(pc)` 한 문으로 들어온다.
특히 MIDI 는 그냥 두면 연습 중 건반이 악보를 고쳐 버리므로(`handleMidiMessage` → `insertNote`)
반드시 채점으로 돌려야 한다.

**MIDI 안내 — 권한을 묻지 않고 안내한다(결정)**: 학생이 피아노를 연결해 두고도 `🎹 MIDI 입력`(원래는
*편집* 도구다)을 켜지 않으면 건반을 눌러도 조용해서 이유를 알 수 없다. 그렇다고 연습을 시작할 때마다
`requestMIDIAccess()` 로 확인하면 건반이 없는 교실에서도 브라우저 권한 창이 뜬다. 그래서
`navigator.permissions.query({name:"midi"})` 로 **이미 허락된 경우에만** 조용히 열어 보고, 붙어 있는
입력 장치가 있으면 `toast` 의 행동 단추(`opts.action`)로 **"🎹 켜기"** 를 함께 띄운다. 아직 허락하지
않았다면 아무것도 묻지 않고, '쉬운 입력' 도움말 줄의 "피아노는 🎹 MIDI 입력을 켜세요" 가 대신 남는다
(그 줄은 원래 "무엇으로 누르나"를 모아 두는 자리다).

### 연습 중 잠그는 것들

연습 상태는 화면 표시일 뿐 `sheet` 를 건드리지 않는다. 그 대신 **악보를 바꿀 수 있는 길을 전부
막는다** — 오선 클릭 입력, 음표 드래그, 우클릭 편집 메뉴, 편집 단축키와 되돌리기(자판은 통째로
건반이 된다), 재생 버튼(누른 음과 반주가 겹친다). 머리말·편집 도구상자는 CSS
(`.music-doc.is-practice`)로 잠그고, 쉬운 입력과 재생 바는 살려 둔다(거기서 따라치기를 끄고 켠다).
연습 중 삼킨 키는 `stopPropagation()` 까지 해서 다른 곳의 한 글자 단축키가 함께 반응하지 않게 한다.

### 다시 그려도 살아남는다

배율·창 크기가 바뀌면 VexFlow 가 음표 요소를 새로 만든다. `drawScore()` 끝에서 `paintSelection()`
다음에 `paintPractice()` 를 불러 진도 표시를 다시 칠한다(선택 표시와 같은 규칙).

### 다음 단계 (아직 안 함)

- **박자 채점(실시간 모드)**: 반주·메트로놈에 맞춰 치고 `event.start` 대비 판정창으로 채점.
  출력·입력 지연 캘리브레이션이 함께 필요하다.
- **화음 진행 표시**: 지금은 화음에서 몇 음을 눌렀는지 화면에 드러나지 않는다(동요에는 드물어 미룸).
- **마이크 입력**(리코더·노래): 설계 §1 에서 마이크를 배제한 결정을 그대로 따른다.


## 15. 음감 테스트(듣고 음 맞히기) — 2026-08-24

§14 따라치기가 "악보를 보고 누르기"라면 이쪽은 **"악보를 보지 않고 듣고 맞히기"** 다.
재생 바의 `🎧 음감 테스트`로 시작하고, 진행·채점 화면은 새 파일 `src/js/music-eartest.js`
(공개 API `MNMusicEarTest`)에, 문제를 뽑고 채점하는 규칙은 `music-model.js`에 둔다.

### 왜 따라치기 옆에 새 파일로 붙였나 — 결정

`music-editor.js`는 이미 3,600줄이고 여기에 진행 상태·타이머·성적표가 더 들어가면 읽기 어려워진다.
그런데 **입력 경로는 새로 만들 것이 하나도 없었다** — 자판(`MUSIC_PRACTICE_KEYS`)·도레미 버튼·MIDI
건반이 따라치기 때문에 이미 한 문(`practicePress`)으로 모이는 구조라, 그 자리에 형제 분기 한 줄씩만
더하면 그대로 음감 테스트의 답이 된다. 그래서 **입력 배선은 편집기가 계속 갖고**, 진행·채점·화면만
모듈로 뺐다. 모듈은 편집기를 모르고(`create({ timbre, toast, onStart, onFinish, onEnd })` 만 받는다)
편집기는 모듈의 내부를 모른다.

| | 따라치기(§14) | 음감 테스트 |
|---|---|---|
| 악보 | 흐린 교본으로 **깐다**(`.is-practice`) | 아예 **감춘다**(`.is-eartest` → `display:none`) |
| 문제 | 악보의 음표 차례 | 단계별 음역에서 무작위(`musicEarQuestions`) |
| 틀렸을 때 | 진도 정지(악보 위 위치를 잃으니까) | **정답을 들려주고 진도가 나간다** |
| 옥타브 | 늘 통과 | 4단계에서만 채점 |
| 다시 듣기 | 개념 없음 | **한 문제에 한 번**(`Space`) |
| 핵심 지표 | 정확도·분당 음 수 | 정확도·**반응 시간**·혼동한 음 짝 |

### 악보를 감추는 것이 이 기능의 핵심 규칙

지금은 문제를 악보와 무관하게 뽑지만, 그래도 악보를 남기면 학생이 귀 대신 눈으로 음이름을 짚어 본다.
나중에 "이 악보에서 문제 뽑기"를 붙이면 화면의 악보가 **그대로 정답표**가 된다. 그래서 처음부터
악보를 지운다. `visibility` 가 아니라 `display:none` 인 이유는 테스트 판이 그 폭을 온전히 받아야 하고,
되돌아올 때 `ResizeObserver(scoreHost)` 가 이미 붙어 있어 저절로 다시 조판되기 때문이다(수동 호출은
`scheduleRedraw()` 한 번으로 보강).

### 절대음감을 재려면 지켜야 하는 두 가지

1. **잔상 지우기** — 앞 문제의 음이 귀에 남아 있으면 학생은 "그것보다 두 칸 위"로 답을 **견주어** 맞힌다.
   그러면 절대음감이 아니라 상대음감을 재는 셈이다. 문제 사이에 조성이 생기지 않는 반음·트라이톤
   덩어리(`musicEarDistractor` — G2~C3, 문제 음역 밖)를 짧게 울려 귀를 지운다.
2. **시행착오 막기** — 다시 듣기를 열어 두면 이 음 저 음 눌러 보며 맞히는 게임이 된다. 한 문제에 한 번.

`🎵 기준음`을 켜면 시작할 때 가온다(C4)를 들려준다 — 이때는 일부러 **상대음감** 연습이 된다.
"기준음이 있을 때와 없을 때가 어떻게 다른가"를 보여 주는 것 자체가 수업 소재라 토글로 남겼다.

### 음역을 도4~시5 두 옥타브로 좁힌 이유 — 결정

앱 음역은 G3~C6 이지만 문제는 **C4~B5** 에서만 낸다. 옥타브가 정확히 둘이라 4단계의 "옥타브까지
맞히기"가 4·5 둘 중 하나로 떨어지기 때문이다. 반옥타브가 끼면 문항마다 고를 수 있는 옥타브 수가
달라져 정답률을 서로 견줄 수 없다.

| 단계 | 나오는 음 | 옥타브 채점 |
|---|---|---|
| 1 | 흰건반 7음(도4~시4) | 안 함 |
| 2 | 흰건반 14음(도4~시5) | 안 함 |
| 3 | 반음 24개(도4~시5) | 안 함 |
| 4 | 반음 24개(도4~시5) | **함** |

4단계는 계이름을 먼저 받고 옥타브를 한 번 더 받는다(자판·버튼으로는 둘을 한 번에 누를 수 없다).
그 사이에만 숫자키의 뜻이 음이름에서 옥타브로 바뀐다(`MUSIC_EAR_OCTAVE_KEYS`). **MIDI 건반은
예외로 한 번에 답한다** — 건반은 옥타브를 함께 누르므로 `press(pc, midi)` 로 번호까지 넘긴다.

### 지키는 것

| 항목 | 규칙 |
|---|---|
| 악보 | 테스트 내내 `sheet` 를 한 글자도 고치지 않는다(따라치기와 같다). 편집·재생 줄은 잠그고 '쉬운 입력' 도레미 버튼만 답으로 살려 둔다 |
| 배타 | 재생·따라치기와 서로 배타. 소리가 겹치면 문제 음을 들을 수 없다 |
| 대기 화면 | 테스트 중 `.is-running` 을 붙인다(재생과 같은 규칙, `screensaver.js` 무수정) |
| 보기 상태 | 단계·문항 수·기준음은 `localStorage`(`musicEarLevel`·`musicEarCount`·`musicEarReference`). `.msheet` 와 되돌리기 스냅샷에는 넣지 않는다 — 배율·도구막대와 같은 규칙 |
| 문서 닫기 | `earTest.destroy()` 가 예약해 둔 다음 문제 타이머까지 걷는다 |
| 결과 문구 | **"절대음감이 있는지를 가리는 검사는 아니에요"** 를 성적표에 늘 적는다. 통제된 조건의 검사가 아니고, 아이에게 "너는 절대음감이 없다"로 읽히면 안 된다 |

### 테스트

`tests/music-model.test.js` 5개 — 단계별 음역(앱 음역 안·흰건반에 검은건반이 섞이지 않음), 같은 음이
연달아 나오지 않음(늘 첫 음을 고르는 가짜 난수로 확인), 옥타브 채점이 4단계에서만 켜짐, 성적표의
정확도·반응 시간·혼동 짝 상위 셋, 간섭음이 문제 음역과 겹치지 않음.
`tests/music-editor.test.js` 6개 — 모듈 등록·로드 순서, **악보를 감추는 CSS**, 입력 세 갈래가 한 문으로
들어옴, 재생·따라치기와의 배타와 우클릭 메뉴, 다시 듣기 제한·간섭음·기준음, 보기 상태 저장 위치.

### 남은 것

- **이 악보에서 문제 뽑기** — 문항마다 음표 id 를 들고 있다가 결과 화면에서 악보를 다시 드러내며 틀린
  음표를 칠해 주면 "그 음이 노래의 어디였는지"를 보여 줄 수 있다. 소리만 나는 앱과 갈리는 지점이라
  다음 단계로 남긴다.
- 음색 무작위(음색 단서 제거), 학생별 기록 누적.
