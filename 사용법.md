# ClassDock 사용법 — 처음부터 끝까지

**최종 업데이트: 2026년 8월 28일**

> 이 문서는 **처음 쓰는 분**도 따라할 수 있게 클론부터 실제 사용까지 단계별로 정리한 가이드입니다.
> 자세한 기능 설명·릴리스 노트는 [README.md](README.md)를 참고하세요.

---

## 목차

1. [이 프로그램이 뭐예요?](#1-이-프로그램이-뭐예요)
2. [내 PC에 받기 — 두 가지 방법](#2-내-pc에-받기--두-가지-방법)
3. [실행하기 — 세 가지 방법 중 고르기](#3-실행하기--세-가지-방법-중-고르기)
4. [첫 사용 — 파일 열어 보기](#4-첫-사용--파일-열어-보기)
5. [파일 종류별 사용법](#5-파일-종류별-사용법)
6. [상황별 가이드](#6-상황별-가이드)
7. [자주 쓰는 단축키](#7-자주-쓰는-단축키)
8. [개발자용 — 소스 수정 후 다시 빌드](#8-개발자용--소스-수정-후-다시-빌드)
9. [제한사항 한눈에 보기](#9-제한사항-한눈에-보기)
10. [문제 해결 FAQ](#10-문제-해결-faq)

---

## 1. 이 프로그램이 뭐예요?

여러 형식의 파일(PDF·Word·Excel·PowerPoint·이미지·Python 등)을 **하나의 창에서** 보고 편집하는 오프라인 도구입니다.

- **PDF**에 서명·텍스트·필기를 올려 다른 PDF로 저장
- **Excel/CSV** 표 보기·편집·통계
- **Python** 코드 작성·실행·자동채점 (브라우저 안에서도 동작)
- **참고 문서 + 작업 문서를 나란히** 펴 놓고 공부하고, 필요할 때 참고 문서만 잠금 (분할 작업)
- **화이트보드(설명용 칠판)** 와 **PDF·코드 위 필기**
- **수업 리플레이** — 화이트보드·PDF 필기·Python 코드와 실행 결과를 녹화해 **되감아 재생**(`.lesson`)
- **과제 패키지** — 문제·시작 코드·자동채점을 한 파일(`.task`)로 배포하고 제출본(`.taskdone`)을 걷어 **재채점·성적 CSV**
- **시험지** — 객관식·주관식·이미지 문항을 `.exam` 한 파일로 배포하고, 학생이 이름·서명해 낸 제출본(`.examdone`)을 **선생님만 열어 일괄 채점·성적 CSV**
- **블록 문서** — 글·표·이미지를 한 문서에 섞어 쓰는 노션형 문서(`.mnote`)
- **악보** — 두 성부·가사·표현·반복을 넣고 바로 듣는 악보(`.msheet`), **따라치기(자판으로 음 맞추기)**, **음감 테스트(듣고 음 맞히기)**, **조옮김**, **MusicXML 가져오기·내보내기**, MIDI 건반 입력·MIDI/WAV 저장과 참고 이미지 나란히 보기
- **지도** — 배경지도 위에 표시를 찍어 이름·메모를 붙이는 수업용 지도(`.map`), 그대로 **칠판으로 보내 판서**하거나 **메모로 보내 나중에 다시 지도로 열기**, 런처에서 실제로 본 지역은 **자동 보관해 다시 오프라인으로**
- **연대표** — 사건·기간·유적지·설명·사진을 시간순으로 놓고 **기원전부터 현대까지** 발표하는 수업용 연대표(`.timeline`)
- **개념 관계도** — 개념 카드를 원인·결과·포함·비교 관계로 연결해 정리하고 발표하는 관계도(`.concept`)
- **암기 카드** — 문답·빈칸 카드를 만들고 틀린 것만 다시 보며 복습하는 암기장(`.study`)
- **파일 비교·여러 파일 찾아 바꾸기** — 두 파일의 달라진 곳을 나란히 보고, 열린 파일들(Word 본문 포함)을 한꺼번에 치환

> 기능이 어디 있는지 모를 땐 **`Ctrl+K`(명령 팔레트)** 에 하고 싶은 일을 한국어로 적어 보세요. 지금 화면에서 쓸 수 있는 기능만 추려 보여 주고 바로 실행합니다.

아무 파일도 열지 않은 시작 화면에서는 **＋ 다른 문서 만들기**를 누르면 일반 문서(노트북·빈 표·텍스트·자바스크립트), 수업 자료(화이트보드·블록 문서·악보·지도·연대표·시험지), 파일 열기(수업 리플레이)로 나누어 바로 시작할 수 있습니다. Python은 위쪽의 **Python 실습** 버튼으로 바로 만듭니다.

파일은 외부 서버로 올라가지 않습니다. 내 PC 안에서만 처리합니다.

---

## 2. 내 PC에 받기 — 두 가지 방법

### 방법 A. Git으로 클론 (개발자·소스 수정할 사람)

1. **Git이 설치돼 있는지 확인** — 명령 프롬프트(`cmd`)나 PowerShell에서 `git --version` 입력. 없으면 [git-scm.com](https://git-scm.com)에서 설치.
2. 원하는 폴더에서 클론:
   ```bash
   git clone https://github.com/songhwaseong/myOffice.git
   cd myOffice
   ```

### 방법 B. ZIP으로 다운로드 (그냥 쓰고만 싶은 사람)

1. GitHub 저장소(`https://github.com/songhwaseong/myOffice`) 접속
2. 초록색 **Code** 버튼 → **Download ZIP**
3. 받은 ZIP을 원하는 폴더에 압축 해제

> **이 단계만 끝나면 사용은 가능합니다.** 다음 섹션에서 실행 방법을 고르세요.

---

## 3. 실행하기 — 세 가지 방법 중 고르기

받은 폴더 안에 세 가지 실행 방법이 들어 있습니다. **상황에 맞게 하나만 고르면 됩니다.**

| 방법 | 누가 쓰면 좋아요? | 더블클릭할 파일 |
|------|------------------|----------------|
| **① EXE** | 가장 추천. 자동 복원·로컬 Python·정확한 PPT 변환 등 모든 기능 사용 | `ClassDock.exe` |
| **② 오프라인 HTML** | USB로 들고 다니거나 다른 PC에 파일 하나만 주고 싶을 때 | `classdock-offline.html` |
| **③ 온라인 HTML** | 개발자가 소스 수정하며 바로 확인할 때 | `classdock.html` |

### ① EXE 실행 (가장 추천)

1. `ClassDock.exe` 더블클릭
2. **처음 실행 시 Windows SmartScreen 경고**가 뜰 수 있어요 (서명되지 않은 빌드라서). 다음처럼 진행:
   - "Windows의 PC 보호" 창에서 **추가 정보** → **실행** 클릭
3. 콘솔창 없이 로컬 서버가 뜨고 기본 브라우저가 자동으로 열립니다.
4. 끝낼 때는 브라우저 탭만 닫지 말고 **트레이/작업관리자에서 `ClassDock.exe`도 종료**하세요. 또는 `desktop/stop-server.cmd`를 실행.

> EXE를 매번 누르기 귀찮으면 [§6 매번 EXE 누르기 귀찮을 때](#매번-exe-누르기-귀찮을-때) 참고.

### ② 오프라인 HTML 실행

1. `classdock-offline.html` 더블클릭 → 기본 브라우저에서 바로 열림
2. 인터넷이 없어도 PDF·문서 보기·편집은 그대로 동작
3. 단, **Python을 처음 실행할 때**는 인터넷이 필요합니다 (Pyodide 런타임 다운로드)

### ③ 온라인 HTML 실행

1. 인터넷 연결 상태에서 `classdock.html` 더블클릭
2. 라이브러리를 CDN에서 받아오므로 **첫 실행은 반드시 인터넷 필요**

---

## 4. 첫 사용 — 파일 열어 보기

> **처음 열면** 파일 열기 → 파이썬 실행 → 저장 3단계를 알려 주는 **환영 창**이 한 번 뜹니다. **✨ 파이썬 예제로 시작**을 누르면 곧장 예제 갤러리가 열려요. 나중에 다시 보려면 헤더 `?` → **처음 사용 안내 보기**.

### 파일 열기 (3가지 방법)

1. **드래그 앤 드롭** — 탐색기에서 파일을 화면으로 끌어다 놓기 (가장 빠름)
2. **사이드바 하단 [열기] 버튼** → **파일 추가** 또는 **폴더 열기**
3. **`Ctrl+O`** (파일 열기) / **`Ctrl+Shift+O`** (폴더 열기)

### 여러 작업공간으로 화면 나누기

- 화면 위쪽에는 작업공간이 **가로 탭**으로 나란히 표시되고, 현재 작업공간은 밝게 강조됩니다. 다른 탭을 누르면 바로 전환하고, 탭이 많으면 탭 줄을 가로로 스크롤합니다. **탭 위에서 우클릭**하면 전체 목록(파일 개수 포함)·새 작업공간·이름 변경·삭제가 한 메뉴에 나옵니다. 작은 화면에서는 현재 탭만 표시되므로 우클릭 메뉴의 목록으로 전환합니다.
- 작업공간마다 사이드바 파일 목록, 열린 탭과 순서, 마지막 문서, 분할 작업의 참고·작업 문서, 사이드바 접힘·검색 상태가 따로 유지됩니다.
- `Ctrl+Alt+←` / `Ctrl+Alt+→`로 이전·다음 작업공간으로 바로 이동합니다.
- 같은 원본 파일을 다른 작업공간에서 다시 열면 편집본을 복제하지 않고 **하나의 문서를 두 작업공간이 공유**합니다. 한쪽에서 수정·저장한 내용과 저장 상태가 다른 쪽에도 그대로 이어져 마지막 저장이 조용히 앞선 편집을 덮는 일을 막습니다.
- 공유 파일을 한 작업공간의 사이드바에서 닫아도 다른 작업공간의 파일과 수정 내용은 유지됩니다. 작업공간을 삭제할 때도 전용 파일은 남은 작업공간으로 옮겨 원본과 편집 내용을 보존합니다.
- 테마, 언어, 저장 폴더, 단축키, Python 터미널은 앱 전체에서 공유합니다.

> **⚠ 꼭 알아두세요 — 폴더를 여는 방식과 브라우저 권한에 따라 저장 위치가 달라집니다.**
>
> - 사이드바의 **열기 → 폴더 열기** 또는 `Ctrl+Shift+O`로 폴더를 열고 쓰기 권한을 허용하면 편집한 파일을 선택한 폴더의 원본에 바로 저장합니다.
> - **EXE에서는 Windows 폴더 선택창으로 실제 경로를 기억**하므로 프로그램을 다시 실행한 뒤에도 브라우저의 “이 사이트에서 파일을 편집” 권한창 없이 원본에 저장합니다. 이전 버전에서 열어 둔 폴더는 첫 수동 저장 때 같은 폴더를 한 번만 다시 선택하면 이후부터 조용히 연결됩니다.
> - Chrome·Edge에서는 탐색기에서 **폴더를 화면으로 드래그**해도 폴더 쓰기 핸들을 받아 원본에 저장합니다. Firefox·Safari처럼 드롭한 폴더의 쓰기 핸들을 제공하지 않는 환경에서는 EXE가 **설정 → 일반 → 자동 저장 폴더**에, 일반 HTML이 다운로드 사본으로 저장하며 드래그한 원본은 변경하지 않습니다.
> - 화면 위 파일명 옆의 **원본 저장/사본 저장** 배지에서 현재 저장 대상을 확인할 수 있습니다. 배지에 마우스를 올리면 원본·자동 저장 폴더·다운로드 중 실제 동선을 설명합니다.

### 여러 파일을 한 번에

- 폴더째 드래그하거나 ZIP을 열면 안의 파일들이 사이드바에 정리됨
- **폴더·ZIP을 처음 열면 아무 문서도 자동으로 열리지 않고 빈 화면이 뜹니다.** 사이드바가 펼쳐진 채로 기다리므로 볼 파일을 직접 고르세요(사진이 수백 장인 폴더에서 엉뚱한 파일이 열리지 않게 하는 기본값입니다). 예전처럼 첫 파일을 바로 보려면 **설정 → 일반 → '폴더·압축을 열 때 첫 파일 자동으로 열기'** 를 켭니다. 이 설정과 상관없이 **최근 작업공간 자동 복원은 보던 탭을 그대로 되살립니다.**
- 사이드바에서 파일 클릭으로 전환
- 빈 화면의 **최근 연 항목**을 누르면 보관된 파일·폴더 핸들로 다시 엽니다. 항목의 `×`나 **목록 지우기**는 목록만 지우고 디스크 파일은 그대로 둡니다.
- 사이드바 파일을 `Ctrl+클릭`해 개별 추가하거나 `Shift+클릭`해 범위 선택한 뒤 **파일 닫기** 또는 **삭제**를 실행할 수 있습니다. **삭제**는 원본 폴더 핸들이 있는 파일만 디스크에서 영구 삭제하며 한 번 더 확인합니다.
- 사이드바의 **통합 검색**(`Ctrl+Shift+F`)은 파일 이름을 즉시 거르고, 잠시 뒤 텍스트·코드 파일의 **내용까지** 찾아 줍니다(저장하지 않고 편집 중인 내용도 반영). 스캔 PDF는 글자 인식(OCR)을 한 번 돌리면 본문 검색이 됩니다.
- **최근 검색어**: 찾기·검색창은 지난 검색어를 기억합니다. 통합 검색·편집기·PDF·노트북·표·일괄 바꾸기를 **각각 다른 목록으로** 이 컴퓨터 안에만 12개까지 남기고, 창을 열면 마지막 검색어를 채워 주며 빈 칸에 포커스가 오면 목록을 펼쳐 보여 줍니다. '바꿀 내용'은 기억하지 않습니다. 여러 사람이 함께 쓰는 컴퓨터라면 **설정 → 일반**에서 끄거나 한 번에 지우세요.

### 화면 구성

```
┌─────────────────────────────────────────────┐
│ 헤더 (파일명·메모·설정)                     │
├──────────┬──────────────────────────────────┤
│          │ 탭 (열린 파일들)                 │
│ 사이드바 ├──────────────────────────────────┤
│ 파일통계 │                                  │
│  파일    │      현재 파일 내용              │
│  목록    │                                  │
│          │                                  │
└──────────┴──────────────────────────────────┘
```

- **탭은 파일이 하나만 열려 있어도 표시됩니다.** 파일을 더 열어도 화면이 위아래로 밀리지 않습니다.
- 원본에 바로 저장되는지 사본으로 저장되는지는 파일명 옆 **원본 저장 / 사본 저장** 배지와, 필요할 때 문서 위에 잠깐 겹쳐 뜨는 **노란 안내 띠**로 알려 줍니다(안내가 떠도 작업 화면 높이는 그대로입니다).

---

## 5. 파일 종류별 사용법

### PDF — 편집·서명·필기

**보기 방식 — 이어보기 / 한 장씩**
- 기본은 **이어보기**입니다. 페이지가 세로로 이어져 있어 스크롤로 훑어보고, 여러 페이지에 걸친 글자도 한 번에 선택할 수 있습니다
- 페이지 표시줄 왼쪽의 **문서 모양 아이콘**(줌 옆)을 누르면 **한 장씩 보기**로 바뀝니다. 지금 쪽 하나만 화면에 남고 **◀ ▶** 또는 `PageUp`·`PageDown`·`←`·`→` 로 넘깁니다. 수업 중 발표처럼 "지금 이 쪽"만 보여 줄 때 좋습니다
- 한 장씩 보기로 들어가면 **페이지 한 장이 화면에 다 들어오도록 배율을 한 번 맞춰** 줍니다. 그 뒤 확대·축소한 것은 그대로 두고, 이어보기로 돌아가면 들어가기 전 배율로 되돌립니다
- 페이지 번호 입력·썸네일·목차·찾기 결과로 뛰는 것도 모두 그대로 되며, 한 장씩 보기에서는 스크롤 대신 **그 쪽으로 넘어갑니다**
- 고른 보기 방식은 **모든 PDF에 함께 적용되어 기억**되므로 다음에 연 PDF도 같은 방식으로 시작합니다. 전체화면에도 같은 ◀ ▶ 가 나옵니다
- **분할 작업**에서는 **두 칸이 각자** 보기 방식을 갖습니다. 참고 칸 위의 표시줄과 작업 칸 위의 표시줄(줌·페이지·찾기·전체화면)에 같은 단추가 있어, 한쪽만 한 장씩 보고 다른 쪽은 이어봐도 됩니다. 두 표시줄은 늘 반대편 모서리에 놓여 서로 가리지 않습니다
- 분할에서 한 장씩 볼 때도 **한 장 전체가 그 칸에 들어오도록** 맞춰지고, 분할바를 끌어 칸 크기를 바꾸면 다시 맞춰집니다
- 자판(`PageUp`·`PageDown`·`←`·`→`)은 **마지막에 누른 칸**의 PDF를 넘깁니다 — 작업 칸에서 글을 쓰거나 표를 옮기는 동안 참고 PDF가 넘어가지 않도록

**기본 기능**
- 상단 메뉴 **편집** → **서명 / 텍스트 / 날짜 / ✓ 체크**로 PDF 위에 요소 추가
- 요소를 **드래그**해서 이동, **더블클릭**해서 텍스트 수정, **모서리**로 크기 조절, **Delete**로 삭제
- **다운로드** 버튼 → `원본이름_signed.pdf`로 저장 (원본은 그대로)

**페이지 정리**
- 상단 **페이지** → 썸네일 보기, 페이지 추출·삭제·순서 변경·90도 회전
- **PDF 합치기** — 현재 PDF 뒤에 다른 PDF 이어붙이기

**목차(책갈피) 만들기·편집**
- 상단 **페이지 → 목차**를 열면 기존 책갈피를 확인하고, 없는 PDF에도 **＋ 현재 페이지**로 새 책갈피를 추가
- 항목을 선택한 뒤 **이름 / 삭제 / ↑ / ↓ / ← / →**로 이름·순서·상하 계층 편집(더블클릭으로도 이름 변경)
- 페이지 순서를 바꿔도 책갈피 목적지는 유지되며, 페이지 삭제 시 해당 책갈피는 제거하고 하위 항목은 보존
- 목차 변경도 자동 복구와 `Ctrl+Z`/`Ctrl+Y`에 포함되고 **다운로드한 PDF에 실제 책갈피로 저장**

**필기 모드(✏️ 필기)**
- 펜·형광펜·지우개로 PDF 위에 직접 판서
- 필기바의 **● 녹화**로 필기 과정을 **수업 리플레이**로 저장 → 교재 위에서 판서가 되살아남(아래 🎬 참고)
- 필기바 왼쪽 끝 **⋮⋮ 핸들**을 끌어 위치 자유 배치, 좌/우 끝으로 가면 자동 세로 모드
- **🖱 선택** 도구를 누르면 그리기 일시 정지 (페이지 클릭/이동 평소처럼)
- 필기는 자동 저장돼 같은 PDF를 다시 열면 복원, 다운로드한 PDF에 그대로 구워짐

### Excel / CSV

- 셀·행·열·드래그 범위 선택 후 **합계·평균·최소·최대·개수** 자동 표시
- `Ctrl+C`로 TSV 복사 → 다른 표에 붙여넣기
- **표 위 검색창**에서 셀 찾기
- `.xlsx`는 셀 편집·필터·정렬·행/열 추가/삭제·시트 추가·복제·이름 변경·순서 변경·서식 보존 저장
- 엑셀에서 **셀 위에 삽입한 일반 그림**과 Microsoft 365의 **셀에 배치한 그림**을 표 안에 표시합니다. 원본 **열 너비·행 높이·자동 줄바꿈**도 함께 복원해 행마다 놓인 그림이 겹치지 않게 보여 줍니다. 일반 그림이 있는 현재 시트/전체 XLSX 내보내기도 그림을 보존합니다.
- `IMAGE()` 원격 그림은 오프라인·보안 정책상 주소로 자동 접속하지 않고 대체텍스트 자리표시자로 보여 줍니다. 자리표시자에 마우스를 올리면 원본 URL 또는 수식 참조를 확인할 수 있습니다.
- **셀에 배치한 그림**이나 `IMAGE()`가 있는 파일은 저장 과정에서 최신 Excel 메타데이터가 사라지지 않도록 읽기 전용으로 엽니다. 이 경우 **원본 XLSX/전체 XLSX**로 저장하면 원본 바이트를 그대로 보존하며, 손실 가능성이 있는 **현재 시트 XLSX** 분리는 제한합니다.
- 수식 입력(`=SUM(A1:A3)` 등), 함수 자동완성·**Σ 자동합계**, 채우기 핸들(숫자·요일·월·`1반` 패턴), 열 폭·행 높이 조절과 병합/병합 해제 지원
- 글꼴·색·정렬·표시 형식·채우기·테두리·서식 복사, 목록 **드롭다운**, 조건부 서식(강조·색조·데이터 막대), 선택 범위 차트와 **미니 피벗** 지원
- 선택 범위의 **📷 선택→메모**로 표 일부를 이미지로 만들어 일반 메모에 보관
- `.csv`는 큰 파일도 페이지로 나눠 보기, XLSX로 변환 가능

### Python (.py)

**기본 흐름**
1. `.py` 파일을 열거나 사이드바 **새로 만들기 → 새 파이썬 코드**
2. 코드 입력 후 **▶** 버튼 또는 `Ctrl+Enter`로 실행
3. 결과가 옆/아래 패널에 표시 (Side/Below 버튼으로 위치 토글). 결과 오른쪽 위 접기 아이콘으로 내용은 유지한 채 숨기고, Side/Below를 누르면 다시 표시

> **예제 갤러리** — 빈 편집기에는 117개 예제 갤러리가 뜹니다. **난이도 필터 칩(⭐ 입문 / ⭐⭐ 기본 / ⭐⭐⭐ 심화 / ⭐⭐⭐⭐ 응용 / ⭐⭐⭐⭐⭐ 도전)** 으로 단계별로 추리고, 검색창에서 제목·설명·개념·파일명으로 찾을 수 있습니다. 응용·도전 단계에는 다익스트라·배낭 DP·N-퀸·만델브로·A* 길찾기 같은 심화 예제가 있어요. 예제를 클릭하면 새 코드로 열려 바로 실행됩니다.

**도구바 버튼들**
- **▶ 실행** — 코드 실행. 실행 중에는 빨간 **■** 버튼으로 바뀌어 무한 반복이나 오래 걸리는 코드를 바로 멈출 수 있음
- **단계 실행** — 줄별 변수 변화를 최대 300단계까지 기록 (디버깅용)
- **진단** — 실행 없이 문법·자주 하는 실수 검사
- **채점** — 여러 테스트의 입력/기대 출력으로 자동 채점
- **.py 저장** — 코드 저장. `폴더 열기`와 Chrome·Edge에서 드래그해 원본 권한을 받은 폴더는 원본에 반영하고, 권한이 없는 환경은 자동 저장 폴더/다운로드 사본에 저장
- **편집 파일 자동 저장** — 설정 → 문서에서 켜면 Python을 포함한 편집 가능한 텍스트 기반 코드·문서를 입력이 멈춘 지 3초 뒤 원본에 저장. 이름 없는 새 파일은 첫 수동 저장으로 이름을 정한 뒤부터 동작하며, ZIP 내부 파일과 쓰기 권한이 없는 브라우저 파일은 로컬 초안 복구만 사용. 실패하면 내용을 그대로 유지하고 **지금 저장**을 안내
- **↩ 원본** — 편집 전 원본으로 되돌리기
- **PDF에 핀** — 현재 코드 줄을 PDF 위치에 연결
- **✏️ 필기** — 편집 잠그고 코드·실행결과 위에 필기 (세션 한정, 문서 닫으면 사라짐)
- **라이브러리** — 데이터분석·크롤링·DB 세트 원클릭 설치 (EXE+로컬 Python)
- **결과 / 터미널** — 실행 결과와 명령창을 전환. EXE에서는 현재 작업폴더를 유지하는 로컬 PowerShell, 일반 브라우저에서는 변수가 유지되는 Pyodide Python 콘솔로 동작. 터미널은 열려 있는 파이썬 파일들이 하나를 함께 씀
- **노트북으로 ▾** — 본문을 누르면 현재 코드를 바로 노트북으로 변환. `▾`에서 **셀 나누기**(줄번호 클릭으로 `# %%` 경계 조정) 또는 **자동분할** 선택
- **A− / A+** — 코드와 실행 결과의 문자 크기를 함께 조절. 표준 출력·오류·표·변수·진단 글자에 적용되고 그래프·이미지 크기는 유지
- **경고 표시** — 체크를 끄면 정상 종료된 실행의 경고를 숨김. 실행 중 오류 출력은 결과 확정까지 잠시 보류하며 실제 오류로 확인되면 반드시 표시. 선택은 다음 실행에도 유지

**노트북 변환**
1. **노트북으로** 본문을 누르면 현재 `# %%` 경계를 기준으로 즉시 새 노트북 탭이 열립니다.
2. 경계를 직접 정하려면 `▾ → 셀 나누기`를 누르고 편집기 왼쪽 줄번호를 클릭해 경계를 넣거나 뺍니다.
3. 상단 **분할 완료**를 누르면 변환합니다. 취소하려면 `Esc` 또는 `▾ → × 셀 나누기 취소`를 누르면 진입 전 코드로 복원됩니다.
4. `▾ → 자동분할`은 빈 줄 뒤의 최상위 문장을 기준으로 `# %%`을 자동 추가하며, 이후 셀 나누기로 세부 조정할 수 있습니다.

**입력값(`input()`) 처리**
- **EXE + 로컬 Python**: 결과 창에 입력 커서가 떠서 대화형으로 입력
- **브라우저(Pyodide)**: `input("이름: ")`처럼 순서가 고정된 입력은 **프롬프트 문구를 라벨로 단 개별 입력칸**이 떠서 채우면 됩니다. 칸에서 Enter로 다음 칸, 마지막 칸에서 Enter면 바로 실행돼요. 반복문·조건문 안처럼 값 개수가 달라질 수 있으면 **한 줄에 하나씩 적는 자유 입력칸**으로 자동 전환됩니다.

**터미널**
- **EXE**: `.py` 파일의 작업폴더에서 지속형 PowerShell 실행. 터미널을 열 때 한 번만 준비하므로 `ls`, `dir`, `cd` 같은 짧은 명령이 빠르고 작업폴더·PowerShell 변수·환경변수가 다음 명령에도 유지됨. **중지** 또는 `Ctrl+C`로 실행 중인 명령과 자식 프로세스를 함께 종료하며 다음 명령에서 새 세션을 자동 시작
- EXE의 **열기 → 폴더 열기**는 Windows에서 선택한 실제 절대경로를 기억하므로, 자동 저장 폴더 위치와 관계없이 터미널이 원본 `.py`가 있는 폴더에서 시작합니다. 탐색기에서 화면으로 드래그한 폴더는 브라우저 보안상 절대경로를 알 수 없으므로 실제 경로가 필요한 경우 **폴더 열기**를 사용하세요.
- **브라우저**: 운영체제 명령 대신 Python 문장·식을 실행. 변수와 import 상태는 이어지며 **초기화**로 모두 지울 수 있음
- **실행 중 표시**: 명령이 도는 동안 **터미널** 버튼 왼쪽에 초록 점이 깜빡입니다. 터미널을 닫아도 명령은 계속 돌기 때문에(서버를 띄워 둔 경우 등) 이 점으로 알 수 있고, 다시 열어 **중지**나 `Ctrl+C`로 끝냅니다. 터미널이 닫혀 있는 동안에는 `Ctrl+C`가 평소대로 복사로 동작합니다
- **여러 파일이 터미널 하나를 함께 사용**: 어느 `.py`에서 열어도 같은 터미널·같은 세션이라 변수·환경변수·명령 기록이 파일을 옮겨도 이어집니다. 다른 파일에서 열면 작업 폴더만 그 파일 폴더로 자동으로 옮기고(`작업 폴더를 …로 옮겼습니다` 안내), 같은 파일에서 다시 열 때는 직접 `cd`한 폴더를 그대로 둡니다. 파이썬 파일을 모두 닫으면 세션이 정리됩니다
- `Enter`는 실행, `Shift+Enter`는 여러 줄 입력, `↑`/`↓`는 명령 기록 이동
- **EXE 경로 자동완성**: 경로 일부를 입력하고 `Tab`을 누르면 일치 후보들의 공통 이름까지 먼저 채우며, 다시 누르면 다음 후보로 이동합니다. `Shift+Tab`은 이전 후보로 이동합니다. `cd`·`Set-Location`에서는 폴더만 찾고 공백이 있는 경로는 자동으로 따옴표 처리
- 로컬 명령은 PC에서 직접 실행되므로 신뢰할 수 있는 명령만 사용

**따라치기 (타자 연습)**
1. 실행 바의 **따라치기**를 누르면 지금 코드가 **흐린 교본**으로 깔립니다
2. 그 위에 처음부터 똑같이 쳐 보세요 — 맞은 글자는 제 색, **틀린 글자는 빨강**으로 표시되고 오른쪽에 **진행률·정확도**가 실시간으로 뜹니다
3. 줄 앞 들여쓰기는 자동으로 넘어가고, 연습 중에는 자동완성·자동 들여쓰기·짝 괄호 같은 도우미가 끼어들지 않습니다
4. `Esc`(또는 **그만두기**)로 언제든 종료 — 어디까지 쳤는지와 정확도를 알려 줍니다
5. **연습 중에도 파일 내용은 원본 그대로**라 실수로 저장돼 코드가 망가질 걱정이 없습니다

**편집 편의 (코드·텍스트 편집기 공통)**
- **`Ctrl+G` 줄 번호로 이동** — 작은 창에 줄 번호를 넣으면 그 줄로 바로 갑니다. 줄 수를 넘는 숫자는 마지막 줄로 당기고 알려 주며, `Esc`로 닫으면 보던 자리로 돌아옵니다. 읽기 전용 코드 보기에서도 편집 모드로 넘어가지 않고 이동합니다
- **우클릭 메뉴** — 복사·잘라내기·붙여넣기와 함께 **대문자/소문자로 변경**, **선택한 줄 중복 제거**, **모두 선택**을 쓸 수 있습니다. 고른 글자를 **Google에서 검색**(기본 브라우저의 새 탭·창에서 결과 열기)·**지도에서 검색**(지도 탭에서 그 장소 찾기)·**파일에서 검색**(사이드바 파일명·내용 검색)으로 바로 이어 찾을 수도 있습니다. Google 검색은 선택한 글자가 있을 때만 나타나며 시험 응시 화면에서는 사용할 수 없습니다
- **`Ctrl+F10` 특수문자** — 브라우저 위 편집기라 "ㅁ + 한자키"가 오지 않으므로, 우클릭 → **특수문자…** 또는 `Ctrl+F10`으로 문자표를 엽니다. ※ ○ ① ㎡ 같은 글자를 **한자키 자모(ㄱ·ㄴ·ㄷ…) 순서 그대로** 찾을 수 있고, 최근 쓴 20자를 기억합니다. `Shift+클릭`이면 창을 닫지 않고 이어서 넣습니다
- **열 편집 복사·붙여넣기** — `Alt`+세로 드래그로 만든 사각 선택을 `Ctrl+C`·`Ctrl+X`로 담고 `Ctrl+V`로 붙여넣습니다. 붙여넣는 줄 수가 선택한 줄 수와 같으면 줄별로 짝짓고, 한 줄짜리는 모든 줄에 같이 들어갑니다

> **파이썬 준비 배지** — 브라우저 실행에서는 파이썬 파일·노트북을 여는 순간 런타임을 미리 준비합니다. 헤더에 **파이썬 준비 중… → 준비 완료 ✓** 배지가 떠서 첫 실행 대기가 짧아집니다(로컬 파이썬이 있으면 표시되지 않고 바로 대화형 실행).

### 원격 터미널 (SSH, EXE 전용)

PuTTY·MobaXterm처럼 IP 주소와 Linux 계정으로 원격 서버에 접속하는 대화형 터미널입니다.

1. 왼쪽 사이드바 하단 **더보기(···) → 원격 터미널**을 누릅니다.
2. **호스트**에 서버 IP 또는 도메인, **포트**에 SSH 포트(보통 `22`), **계정**을 입력한 뒤 인증 방식을 고릅니다.
   - **비밀번호** — SSH 비밀번호를 입력합니다.
   - **개인키** — **개인키 선택…**을 눌러 OpenSSH 또는 PEM 개인키를 고릅니다. 암호화된 키라면 **키 암호**도 입력하고, 암호가 없는 키는 비워 둡니다.
3. 처음 접속하는 서버라면 SHA-256 서버 지문이 표시됩니다. 서버 관리자에게 받은 지문과 비교해 일치할 때만 **지문을 신뢰**하세요.
4. 접속되면 현재 문서는 한쪽에 그대로 남고 SSH 터미널이 반대쪽 도킹 패널에 표시됩니다. 일반 Linux 터미널처럼 방향키·`Ctrl+C`, `vim`, `top` 같은 대화형 프로그램을 사용할 수 있습니다.
5. Windows 파일을 서버로 보내려면 터미널 상단 **파일 업로드**를 누릅니다. **파일 선택…**에서 한 번에 최대 32개를 고릅니다. 원격 셸이 OSC 7 경로 정보를 보내면 현재 터미널 디렉터리가 자동으로 들어가고, 감지되지 않으면 로그인 홈 디렉터리인 `./`가 기본값입니다. 필요한 경우 셸 프롬프트에서 **현재 경로 가져오기**를 누르거나 경로를 직접 입력합니다. SSH 비밀번호 또는 암호화된 개인키의 키 암호를 다시 입력하고 **업로드 시작**을 누르면 진행률이 표시되며 전송 중에는 **업로드 취소**를 사용할 수 있습니다.
6. 가운데 분할선을 드래그해 문서와 터미널 너비를 조절합니다. **⇄**는 터미널을 왼쪽·오른쪽으로 옮기고, **접기**는 SSH 연결을 유지한 채 패널만 좁게 접습니다.
7. 끝나면 **연결 끊기** 또는 **닫기**를 누릅니다. **닫기**는 패널과 SSH 세션을 함께 종료하며 진행 중인 파일 업로드도 취소합니다.

- **IP·포트·계정·인증 방식 기억**을 켜면 네 값만 이 PC에 저장합니다. **비밀번호·키 암호·개인키 경로는 저장하지 않습니다.** 개인키를 다시 골라야 하는 경우에도 화면에는 파일명만 표시됩니다.
- 도킹 방향과 패널 너비는 이 PC에 기억합니다. 접었다 다시 펼쳐도 현재 SSH 연결과 터미널 출력은 유지됩니다.
- 터미널 상단에서 **Cascadia Mono·Consolas·D2Coding·나눔고딕코딩·시스템 고정폭** 글꼴을 고를 수 있습니다. `−/+`는 11~24px 글자 크기를 바꾸고 **줄 1.15** 버튼은 줄 간격을 `1.0 → 1.15 → 1.3 → 1.5`로 순환합니다. 설정은 이 PC에 기억되며 설치되지 않은 글꼴은 다음 고정폭 글꼴로 자동 대체됩니다.
- 연결이 끝나거나 실패하면 터미널 위쪽에 **인증 실패·연결 시간 초과·연결 거부·호스트 오류·네트워크 오류·지문 확인 실패**처럼 원인과 확인할 항목이 표시됩니다.
- **재접속**을 누르면 IP·포트·계정과 현재 실행에서 선택한 개인키는 그대로 둡니다. 비밀번호 또는 암호화된 키의 암호는 다시 입력해야 합니다. 상태 확인 요청이 반복해서 실패해도 무한 대기하지 않고 재접속할 수 있는 상태로 전환합니다.
- 원격 셸이 OSC 7을 지원하면 `cd`로 이동할 때마다 업로드 기본 경로도 갱신됩니다. OSC 7이 없으면 업로드 창의 **현재 경로 가져오기**가 현재 셸에 `$PWD`를 요청합니다. 이 버튼은 셸 프롬프트에서만 누르세요. `vim`, `top`처럼 입력을 받는 프로그램이 실행 중이면 해당 프로그램으로 명령이 들어갈 수 있습니다.
- 터미널 안에서 다른 서버로 다시 SSH 접속했거나 Docker 같은 컨테이너 셸에 들어간 경우, 감지된 경로는 업로드용 `scp.exe`가 접속하는 원래 서버와 다를 수 있습니다. 이때는 **원격 디렉터리**에 원래 서버에서 실제로 존재하고 쓰기 권한이 있는 경로를 직접 입력하세요. 직접 입력한 값은 이후 자동 감지가 덮어쓰지 않습니다. 같은 이름의 파일이 있으면 덮어쓸 수 있습니다.
- 업로드 파일의 전체 Windows 경로와 인증정보는 브라우저 저장소에 저장하지 않습니다. 파일은 ClassDock이 Windows OpenSSH의 `scp.exe`로 직접 보내며, 최신 OpenSSH에서는 서버의 SFTP 기능을 사용하므로 서버에서 SFTP가 허용되어 있어야 합니다.
- 서버 지문은 `%LOCALAPPDATA%\ClassDock\ssh\known_hosts`에 공개키로 보관합니다. 다음 접속에서 키가 달라지면 서버 재설치인지 관리자에게 확인하기 전에는 교체하지 마세요.
- 개인키 인증은 OpenSSH 개인키와 PEM 개인키를 지원합니다. 공개키(`.pub`)는 개인키가 아니므로 선택할 수 없고, PuTTY 전용 `.ppk`는 PuTTYgen에서 OpenSSH 개인키로 변환한 뒤 사용하세요.
- 개인키 인증에서는 선택한 키 외의 기본 `~/.ssh/id_*` 키와 SSH 에이전트를 사용하지 않으며 비밀번호 로그인으로 자동 전환하지 않습니다.
- 현재 버전은 비밀번호·개인키 로그인, 문서·터미널 좌우 분할, Windows 파일 업로드를 지원합니다. 원격 파일 탐색·다운로드·포트 포워딩·여러 터미널 탭은 지원하지 않습니다.
- Windows의 **OpenSSH Client** 선택적 기능이 필요합니다. 기능이 없으면 원격 터미널 창에 설치 안내가 표시됩니다.
- 오프라인/온라인 HTML은 브라우저 보안상 SSH 서버에 직접 TCP 연결할 수 없어 이 기능을 사용할 수 없습니다.

### JavaScript (.js / .mjs)

1. `.js`·`.mjs` 파일을 열거나 사이드바 **새로 만들기 → 새 자바스크립트 코드**를 선택합니다.
2. 코드 입력 후 **▶** 또는 `Ctrl+Enter`로 실행합니다. 실행은 화면과 분리된 브라우저 Worker에서 이뤄지며, 무한 반복은 10초 제한이나 빨간 **■** 버튼으로 멈출 수 있습니다.
3. **라이브러리** 버튼에서 Lodash(`_`)·Day.js(`dayjs`)·Papa Parse(`Papa`)·Math.js(`math`)를 선택합니다. 선택은 현재 문서에만 저장되고 실행·채점에 함께 적용됩니다.
4. **+ .js 파일**로 직접 받은 라이브러리를 추가할 수도 있습니다. 해당 코드는 실행할 때마다 실행되므로 출처를 신뢰하는 파일만 추가하세요(한 파일 512KB, 문서당 최대 8개·합계 1MB).
5. **⚙ 설정 → 도구 → JavaScript 탭**에서 채점과 JavaScript 라이브러리 버튼의 노출 여부를 따로 정합니다. Python 탭의 채점·Python 라이브러리 설정과는 서로 영향을 주지 않습니다.

> 내장 라이브러리는 오프라인 HTML·EXE에 포함되어 인터넷 없이 동작합니다. npm 설치와 `import ... from ...` 문은 지원하지 않으므로 각 라이브러리의 전역 이름을 사용하세요. 이 실행기는 `window`·`document`가 없는 Worker 환경이라 React·DOM 기반 차트처럼 화면을 직접 다루는 라이브러리는 HTML 미리보기에서 사용해야 합니다.

### Jupyter 노트북 (.ipynb)

- 코드 셀에서 문자를 입력하면 자동완성 후보가 셀 밖에 잘리지 않고 표시됨
- 기본 후보는 입력 직후 빠르게 표시되고, EXE+로컬 Python/Jedi의 정밀 후보가 준비되면 같은 목록이 자동 갱신됨
- EXE에서 **전체 실행 옆 ▾ → 로컬 Python 셀 커널 사용**을 선택하면 변수와 Selenium 브라우저 상태를 유지한 채 셀별로 로컬 Python을 실행할 수 있음
- `requests`·BeautifulSoup·Selenium 크롤링과 `%pip`, `%matplotlib`, `!명령`을 지원하며, 중지 또는 커널 재시작 시 Selenium 자식 프로세스도 함께 종료
- 로컬 Python이 없으면 실행 메뉴에 **로컬 Python 설치 필요**가 표시되고, Selenium 셀 실행 시 PATH 옵션을 포함한 설치·재시작 안내를 보여 줌
- 앞쪽 코드 셀에서 만든 변수·함수와 `import`도 다음 셀의 자동완성 문맥에 포함
- `Ctrl+Space`로 후보를 직접 열고 `↑`/`↓`, `Enter`/`Tab`, `Esc`로 조작
- `Ctrl+Enter`는 현재 셀 실행, `Shift+Enter`는 실행 후 다음 셀로 이동, `Ctrl+Shift+Enter`는 모든 코드 셀을 위에서부터 전체 실행. 실행 중에는 상단 **전체 실행**이 빨간 **■** 버튼으로 바뀌며 현재 셀과 남은 전체 실행을 함께 중단
- `input("이름: ")`, `input("나이: ")`처럼 순서가 고정된 셀은 문구를 차례로 보여 주고 Enter로 값을 하나씩 입력받아 마지막 값에서 자동 실행. 반복문·조건문 안의 `input()`은 값을 필요한 만큼 추가한 뒤 **입력 완료 후 실행**. 입력값은 노트북 파일에는 저장하지 않음
- 명령 모드에서 셀을 클릭해 하나를 선택하고, `Ctrl+클릭`으로 떨어진 셀을 추가 선택하거나 `Shift+클릭`으로 연속 범위를 선택
- 선택한 셀은 `Ctrl+C`로 복사, `Ctrl+X`로 잘라내기, `Ctrl+V`로 마지막 선택 셀 아래에 붙여넣기. 셀 순서와 마크다운 첨부 이미지는 유지하고 실행 결과·실행 번호·필기는 제외함. 셀 편집 중에는 기존처럼 글자 복사·붙여넣기로 동작
- 셀 도구의 **메모 아이콘**을 누르면 선택한 셀을 임시 메모에 전용 셀 카드로 보관. 여러 셀은 순서를 유지하며 실행 결과·실행 번호·필기는 제외함
- 셀의 `⋮⋮` 손잡이를 끌기 시작하면 임시 메모가 자동으로 열림. 메모장에 놓으면 셀 카드로 저장되고, 셀 카드를 노트북 셀 사이로 다시 끌거나 카드의 **노트북에 붙여넣기**를 누르면 해당 위치에 새 셀로 삽입
- 저장하지 않은 편집은 자동복구본으로 보관되며 다음에 같은 노트북을 열 때 복원 여부를 확인. 명령 모드 `Ctrl+Z`/`Ctrl+Y` 또는 상단 ↶/↷로 셀 추가·삭제·이동·형식 변경·잘라내기·붙여넣기를 되돌리기/다시 실행
- 셀 오른쪽 위의 `⋮⋮` 손잡이를 끌어 원하는 셀의 위·아래로 순서를 변경. 기존 위/아래 이동 버튼과 키보드 조작도 그대로 사용 가능
- 상단 **목차**는 마크다운의 `# 제목`을 자동 수집해 해당 셀로 이동. 셀 도구의 ▾와 실행 결과의 **출력 접기**로 긴 노트북을 정리
- 툴바 **전체 찾기** 또는 `Ctrl+F`로 코드·마크다운·raw 셀 전체를 검색하고 한 개/전체 치환. 대소문자·단어 단위·정규식 지원 (`Ctrl+Shift+H`는 현재 코드 셀만). `Enter`로 마지막 결과를 지나면 첫 결과로, `Shift+Enter`로 첫 결과를 지나면 마지막 결과로 순환. 문자열을 선택한 뒤 누르면 검색란에 자동으로 채워지고, `Esc`는 열려 있는 검색창을 모두 닫음
- 마크다운 셀은 `<h1>`, `<font color="blue">` 같은 **HTML 태그도 렌더**함(Jupyter와 동일). `<script>`·이벤트 핸들러·`javascript:` 링크 등 위험한 부분은 자동으로 제거(살균)
- 코드 셀에서 `display(...)`는 주피터처럼 동작(브라우저 커널에 자동 주입)
- 텍스트·HTML·PNG/JPEG·SVG·JSON·LaTeX뿐 아니라 **Plotly·Bokeh·Vega/Vega-Lite 인터랙티브 차트, Folium 지도, 오디오(mp3·wav·ogg 등)·비디오(mp4·webm 등)** 실행 결과도 표시(차트·지도는 격리된 iframe으로 렌더, 런타임은 인터넷 연결 시 CDN에서 로드). 오류 결과의 **오류 줄 N로 이동**을 누르면 해당 코드 셀 줄로 이동
- 셀 실행 뒤 **변수 N개**를 펼치면 학생 코드가 만든 변수의 이름·자료형·현재 값을 확인
- 폴더로 함께 연 노트북은 같은 프로젝트의 파일을 커널 작업폴더에 연결함. 자동 실행 기준은 노트북 파일이 있는 폴더이며, 상위 폴더의 데이터는 `../dataIn/result01.csv`처럼 실제 상대경로로 읽고 저장할 수 있음. 셀이 참조한 파일은 실행 직전에 사이드바 작업공간과 다시 동기화
- 셀에서 새로 만들거나 변경한 파일은 셀 출력 아래에 표시되고 최근 작업공간·자동 저장 폴더에 반영
- 노트북을 저장하면 기존 실행 출력·실행 번호·마크다운 첨부 이미지와 새 실행 결과를 함께 보존. **출력 지우기**를 누른 경우에만 저장 데이터에서도 제거
- 상단 `A− / A+` 또는 `Ctrl` `+`/`−`로 코드 셀·마크다운 셀·실행 결과 문자의 크기를 함께 조절. 마크다운은 본문뿐 아니라 제목도 같은 배율로 커지고, 이미지·그래프·영상 크기는 유지되며 선택값은 다음 실행에도 유지
- Matplotlib이 내용 없는 figure만 만든 경우 빈 흰색 이미지는 자동으로 제외. 기존 파일에 이미 저장된 빈 출력은 **출력 지우기** 후 다시 실행
- 셀의 `import`를 감지해 Pyodide 공식 패키지를 자동으로 준비하고, 그 밖의 순수 Python 패키지는 PyPI 자동 설치를 시도
- **Faker는 EXE에 내장되어 오프라인에서도 사용 가능**. 바이너리 확장이 필요한 미지원 패키지는 일반 `.py`의 로컬 Python으로 실행

### 화이트보드 (설명용 칠판)

- 사이드바 **새로 만들기 → 새 화이트보드**로 빈 칠판 열기(탭바 오른쪽 칠판 버튼 또는 `Alt+B`도 같습니다)
- **⚙ 설정 → 일반 → 시작할 때 최근 작업공간 자동 복원**이 켜져 있으면, 앱을 껐다 켜도 **각 작업공간별로** 화이트보드 목록·열어 둔 탭·마지막 판서·마지막으로 보던 탭이 그대로 되살아납니다
- 펜·형광펜·지우개·직선·화살표·사각형·원·텍스트·이미지 삽입
- 도구막대의 **∑ 수학·과학 도구상자**에서 기호·수식 사전·좌표축·기하 도형·물리·화학 스텐실을 검색해 삽입. 도구상자 창은 제목줄을 끌어 옮기고 가장자리로 크기를 바꿀 수 있으며 그 자리를 기억합니다
- 도구막대의 **◉ 집중 도구**에서 원형·사각형 **스포트라이트** 또는 상·하·좌·우 **화면 가리개**를 선택하고 **시작**을 누릅니다. 실행 중에는 같은 버튼이 **종료**로 바뀌며, 다른 모드를 누르면 효과가 즉시 전환됩니다. 집중 도구 실행 중 보드 위에서 **마우스 오른쪽 버튼**을 누르면 `조절점 숨기기/보이기`를 빠르게 전환할 수 있습니다. 스포트라이트의 **손전등 효과**를 켜면 조절점을 숨긴 발표 상태에서 빛줄기와 벡터 손전등이 밝은 영역을 따라다니고, 화면 끝에서는 여유가 있는 반대편으로 자동 이동합니다. 조절점이 보이는 동안에는 객체가 실수로 움직이거나 판서되지 않도록 보드 입력을 잠급니다. **조절점 숨기기** 후에는 드러난 영역에 판서할 수 있고, 선택·이동 도구로 밝은 영역을 끌어 스포트라이트를 옮길 수 있습니다. 가려진 영역의 입력은 계속 막습니다.
- **도형** 탭은 평면도형·입체도형·작도와 원·좌표와 그래프로 분류됩니다. 정사각형부터 원기둥·원뿔·구·각뿔, 각의 이등분선·수직이등분선, 포물선·사인 그래프까지 선택할 수 있습니다.
- **과학** 탭은 역학·전기·광학·화학·생명·지구과학으로 분류됩니다. 도르래·빗면·진자, 직렬·병렬회로와 회로 기호, 렌즈 광선도, 실험기구, 세포·DNA, 태양계·물의 순환 등을 제공합니다.
- **수식** 탭에는 기본·대수·미적분·집합·확률통계·기하·과학으로 나눈 수식 사전이 있습니다. 한글·영문·LaTeX로 검색하고 수식 틀을 누르면 현재 커서 위치에 들어가며, 선택된 입력 부분은 `Tab`/`Shift+Tab`으로 이동해 채웁니다.
- 직접 LaTeX(`\frac{a}{b}`, `x^2`, `\sqrt{x}` 등)를 쓰면 바로 미리보기됩니다. 자주 쓰는 식은 **내 수식 저장**으로 이름을 붙여 보관하고, 별표 즐겨찾기와 최근 사용 목록에서 다시 찾을 수 있습니다. `Ctrl+Enter`로 보드에 넣고, 보드의 수식을 더블클릭하면 원문을 다시 수정합니다.
- **그래프** 탭(도구막대 📈 버튼)은 **식을 실제로 계산해 그린 곡선**을 넣습니다. `y =` 칸에 `x^2 - 3x + 1`, `sin x`, `2^x`, `sqrt(x)` 처럼 쓰면 아래에 미리보기가 나오고, 식을 세 개까지 겹쳐 그릴 수 있습니다. `2x`·`sin x`처럼 곱셈 기호와 괄호를 생략해도 되고 `abs`·`sqrt`·`log`(상용로그)·`ln`을 쓸 수 있습니다. x 범위와 모눈을 정하고 y 범위는 자동에 맡기거나 직접 넣습니다. 식에 `x` 말고 다른 문자(`a` 등)를 쓰면 **슬라이더**가 생겨 "a를 키우면 어떻게 될까?"를 그 자리에서 움직여 보여 줄 수 있습니다. 아래쪽 예시(일차·이차·삼차·절댓값·반비례·지수·로그·삼각함수, 교점·접선·넓이·부등식 등)를 누르면 **커서가 있던 식 칸**에 들어갑니다. 두 번째·세 번째 칸을 먼저 클릭해 두면 그 칸에 채워지고, 이미 적어 둔 다른 칸은 그대로 남아 예시를 골라 담아 겹쳐 그릴 수 있습니다
  - **보드 슬라이더** — `a` 같은 문자를 쓴 식을 넣으면 그래프 **안에 손잡이 띠가 함께 들어갑니다**. 수업 중에 도구상자를 열지 않고 보드에서 손잡이를 바로 끌면 그 자리에서 곡선이 다시 그려집니다(−10~10, 0.1 단위). 끄는 동안 세로 눈금은 붙잡아 두어 **무엇이 달라지는지**가 보이고, 손을 뗄 때 한 번만 기록되므로 `Ctrl+Z` 한 번이면 끌기 전으로 돌아옵니다. 필요 없으면 **보드 슬라이더** 체크를 끕니다
  - **부등식 영역** — 식 칸 앞의 `y =` 를 눌러 `y >`·`y ≥`·`y <`·`y ≤` 로 바꾸면 그쪽 반평면이 옅게 칠해집니다. 등호가 없는 `>`·`<` 는 경계선을 **점선**으로 긋습니다
  - **교점 · 접선 · 넓이** — 아래 해석 줄에서 켭니다. **교점**은 두 곡선이 만나는 자리를 찾아 좌표를 찍고, **접선**은 정한 `x` 자리의 접선과 **기울기(순간변화율)** 를 적으며, **넓이**는 `a~b` 구간과 x축 사이를 칠하고 **정적분 값**을 적습니다. 넓이 옆 **직사각형** 칸에 개수를 넣으면 리만 직사각형으로 바꿔 그리고 그 합을 알려 줍니다(0이면 매끄럽게 칠합니다). 식이 둘 이상이면 **대상**에서 어느 식에 쓸지 고릅니다
  - **값의 표** — 같은 식으로 `x`·`y` 대응표를 만들어 넣습니다. 표에 넣을 `x` 시작·끝·간격을 정하고 **값의 표 넣기**를 누르면 됩니다(칸은 21개까지). 정의역 밖은 `—` 로 적고, 넣은 표도 **두 번 누르면 다시 고칠 수 있습니다**
- **차트** 탭(도구막대 📊 버튼)은 **입력한 자료로 진짜 그래프**를 만듭니다. 화면은 위에서 아래로 **차트 종류 → 제목 → 자료 → 묶음 색 → 통계 도구 → 확률 실험 → 미리보기** 순서이고, 가끔 쓰는 **통계 도구·확률 실험**은 접혀 있습니다(이름표를 누르면 펴집니다). `국어, 7` 처럼 한 줄에 하나씩(쉼표·탭·띄어쓰기 모두 가능) 적거나 표를 복사해 붙여넣고 **막대·꺾은선·원·히스토그램·산점도·상자그림** 중에서 고릅니다. 히스토그램·상자그림은 숫자만, 산점도는 `1, 60` 처럼 두 값을 적습니다
  - **상자그림**은 적어 넣은 숫자로 **다섯 수 요약**(최솟값·Q1·중앙값·Q3·최댓값)을 직접 계산해 그립니다. 수염은 사분범위의 1.5배까지 뻗고 그 밖의 값은 점으로 따로 찍습니다. 값을 **열로 더 적으면**(`번호, 1반, 2반`) 반마다 상자가 하나씩 서서 두 집단을 나란히 비교합니다
  - **추세선**(산점도) — 체크하면 **최소제곱 직선**과 상관계수 `r` 을 함께 그려, 자료가 어느 쪽으로 기우는지 한 줄로 보여 줍니다
  - **요약 카드** — 같은 자료로 **자료 수·합·평균·중앙값·최빈값·최솟값·최댓값·범위·Q1·Q3·사분범위·분산·표준편차**를 한 장의 표로 넣습니다
  - **도수분포표** — **계급·도수·상대도수·누적도수**를 표로 넣습니다. **계급 수** 칸에 숫자를 넣으면 그 개수로 나누고, 0이면 자료 수에 맞춰 자동으로 정합니다(히스토그램도 같은 값을 씁니다)
  - **자료 묶음을 여러 개 겹쳐 비교**하려면 값을 **열로 더 적습니다**. `국어, 7, 9` 처럼 적으면 막대가 나란히 서고(묶음 막대), 꺾은선은 선이 여러 개, 산점도는 점 무리가 여러 개가 됩니다
  - 첫 줄에 `과목, 1반, 2반` 처럼 **이름 줄**을 적으면 그 이름으로 **범례**가 붙습니다(첫 줄의 둘째 칸부터가 숫자가 아니면 이름 줄로 봅니다). 엑셀 표를 그대로 복사해 붙여넣어도 됩니다
  - 줄마다 열 개수가 달라도 되고, **원그래프·히스토그램은 첫 묶음만** 씁니다(부채꼴은 겹칠 수 없어서요)
  - 자료 칸 아래에 **묶음마다 색칩**이 생깁니다. 동그란 색을 눌러 원하는 색으로 바꾸면 미리보기가 바로 따라오고, 고른 색은 차트에 저장돼 다시 열어도 그대로입니다(**색 되돌리기**로 기본 색 복귀). 묶음이 하나인 막대·원그래프는 **항목마다** 색을 고릅니다
- 넣은 그래프·차트·표는 **두 번 누르면 다시 고칠 수 있고**(자리와 크기는 그대로), **분리**를 누르면 축·눈금·곡선이 각각 편집할 수 있는 선과 글자로 흩어집니다
- **교구(자·각도기·컴퍼스)** — 도구막대의 자·각도기·컴퍼스 버튼으로 화면 위에 꺼내 놓습니다. 교구는 판서가 아니라 손에 든 도구여서 저장·내보내기 그림에는 남지 않습니다(`Esc`로 치웁니다)
  - **자**: 몸통을 끌어 옮기고 **오른쪽** 동그라미로 돌립니다. **왼쪽 동그라미를 끌면 길이가 2~40cm 사이에서 늘었다 줄고**(끄는 동안 cm가 표시됩니다), 오른쪽 끝과 각도는 제자리에 있습니다. 눈금(cm) 쪽 모서리 가까이에서 그으면 획이 모서리에 붙어 곧게 나갑니다(지우개는 붙지 않습니다)
  - **각도기**: 테두리나 밑변을 끌어 옮기고 **오른쪽** 동그라미로 돌립니다. **왼쪽 동그라미를 끌면 밑변이 4~30cm 사이로 커졌다 작아지고**(눈금과 숫자도 같이 커집니다), 각을 재는 가운데 점은 제자리에 있습니다. 가운데 점에서 시작해 직선·화살표를 그으면 **1°씩** 맞춰지고 각도가 표시됩니다
  - **컴퍼스**: 가운데=바늘(옮기기), **중간 손잡이를 끌면 반지름이 2~30cm 사이로 벌어졌다 좁아지고**(끄는 동안 반지름·지름이 cm로 표시됩니다), 끝 손잡이를 돌리면 **호**가 그려지고 한 바퀴를 다 돌면 **원**이 됩니다
  - **15°** 버튼을 켜면 직선·화살표가 15°씩 맞춰집니다(`Shift`를 누르고 그어도 같습니다). 선을 긋는 동안 커서 옆에 **길이(cm)와 각도**가 표시됩니다
- **손그림 정리**(요술봉 버튼) — 켜 두면 대충 그린 동그라미·세모·네모·직선을 **반듯한 도형으로 바꿔** 줍니다. 글씨만 한 작은 획과 애매한 낙서는 건드리지 않고, `Ctrl+Z` 한 번이면 그은 그대로 돌아옵니다
- **측정**(눈금 버튼) — 도형을 고르고 누르면 **길이·각도·넓이**가 붙습니다. 직선은 길이(cm), 사각형은 `가로 × 세로 = 넓이`, 원은 반지름과 넓이, 다각형은 넓이, 꺾인 점이 하나뿐인 선은 그 자리의 각도를 잽니다. 붙인 값은 **도형이 바뀌면 따라 바뀌고**(끌어 크기를 바꾸거나 닮음 변환을 하면 즉시 갱신), 라벨을 끌어 원하는 자리에 두면 그 위치를 지킵니다. 한 번 더 누르면 뗍니다
- **변환**(변환 버튼) — 고른 도형을 **선대칭·점대칭·회전·평행이동·닮음**으로 바꿉니다. 기준점은 도형의 중심이 기본이고 **기준점 찍기**로 보드의 아무 자리나 지정할 수 있으며(빨간 ✛ 로 표시), 선대칭 축은 세로축·가로축·**자 모서리** 중에서 고릅니다. **원본 남기고 사본 만들기**를 켜 두면 원본과 나란히 놓여 대응점을 비교할 수 있고, 끄면 그 도형이 바뀝니다(붙여 둔 측정값도 함께 따라갑니다). 글자는 읽을 수 있도록 눕히지 않습니다
- **화학** 탭 — **주기율표**(118개 원소)를 족·주기 배치 그대로 보여 주고, 누르면 번호·기호·이름·원자량이 담긴 **원소 카드**가 들어갑니다. 이름·기호·번호(예: `산소`, `Na`, `26`)로 검색할 수 있습니다. 위쪽 칸에 `CH4 + O2 -> CO2 + H2O` 처럼 적으면 **계수를 맞춰**(`CH₄ + 2O₂ → CO₂ + 2H₂O`) 보드에 넣어 줍니다. `Ca(OH)2` 처럼 괄호가 있어도 되고, 맞출 수 없는 식은 이유를 알려 줍니다
  - **몰 계산(화학량론)** — 계수가 맞으면 아래에 계산 줄이 열립니다. **아는 물질**을 고르고 **양**과 단위(`g`·`mol`)를 넣으면 나머지 물질의 몰수·질량이 바로 나오고, **몰 계산표 넣기**로 **물질·계수·몰질량·몰수·질량** 표를 보드에 넣습니다(예: `CH4 + O2 -> CO2 + H2O` 에서 CH₄ 8g → CO₂ 21.95g). 몰질량은 주기율표의 원자량으로 계산합니다
- **확률 실험**(차트 탭 위쪽) — **동전·주사위·주사위 2개·무작위 수·주머니·스피너**를 정한 횟수만큼 실제로 굴려 자료를 만들고 평균·최빈값을 알려 줍니다. 그대로 **차트 넣기**를 누르면 결과 그래프가 보드에 들어갑니다
  - **주머니·스피너**는 **구성** 칸에 `빨강 3, 파랑 2` 처럼 적습니다. 주머니는 **뽑기** 개수를 2 이상으로 두면 한 번에 여러 개를 뽑아 `빨강1·파랑1` 같은 **조합**을 세고, **되돌려 넣기**를 끄면 **비복원 추출**이 됩니다(파랑이 2개뿐이면 `파랑3`은 나오지 않습니다). 스피너는 적은 수만큼 **넓이(확률)** 가 커집니다
  - **누적 그래프**를 누르면 방금 한 실험을 다시 굴리면서 **누적 상대도수**를 꺾은선으로 그리고, 이론값을 아는 실험이면 점선으로 함께 그어 **큰 수의 법칙**을 눈으로 보여 줍니다
- **수 모형** 탭 — 초등 수업용 모형을 값만 넣어 만듭니다. **분수**(`3/4, 2/3` 처럼 적으면 막대나 원으로 나란히 놓고 크기 비교, 가분수는 전체를 여러 개), **자릿값 블록**(천 덩어리·백 판·십 막대·일 칸으로 수를 펼치기), **수직선 뛰어세기**(뛰는 크기를 음수로 적으면 거꾸로), **양팔 저울**(x 상자와 1 블록으로 등식을 보여 주고 풀 수 있으면 `x = 4`까지 적기)
- **과학 계산** 탭 — 값을 넣으면 계산해서 그려 줍니다.
  - **렌즈·거울 광선도**: 볼록·오목렌즈와 오목·볼록거울에서 `1/a + 1/b = 1/f` 로 상의 자리를 구해 **광선 세 개**와 상을 긋고, **상거리·배율·실상/허상·정립/도립·확대/축소**를 적습니다(허상은 점선)
  - **퍼넷 사각형**: `Aa × Aa` 는 3 : 1, `AaBb × AaBb` 는 9 : 3 : 3 : 1 — 배우자를 만들어 칸을 채우고 **유전자형·표현형 비율**까지 셉니다
  - **회로 계산**: 저항을 `6, 3, 2` 처럼 적고 전압을 넣으면 **직렬·병렬 회로 그림**과 함께 합성저항·전체 전류, 저항마다의 전압(직렬)·전류(병렬)를 붙입니다
- **벡터 합성**(도구막대·우클릭 메뉴) — **같은 점에서 그린 화살표 두 개** 중 하나를 고르고 누르면 평행사변형과 **합력 화살표**가 붙고 크기(cm)·각도가 적힙니다. 합성에 쓰인 화살표는 **끝점에 손잡이**가 생겨 끌어 길이·방향을 바꿀 수 있고, 그때마다 합력이 따라 바뀝니다. 원본 화살표를 지우면 합력도 함께 사라지고, 한 번 더 누르면 뗍니다
- **지도 넣기**(도구막대의 접힌 지도 그림) — 지도 고르기 창이 열립니다. 배경지도를 고르고 끌거나 **장소 이름·좌표**를 적은 뒤 **검색**(또는 `Enter`)으로 자리를 잡고 **이 화면 넣기**를 누르면 지금 보이는 지도가 **그림으로 보드에 들어옵니다**. 장소를 검색해 나타난 **빨간 점과 장소명·주소 이름표도 그림에 함께 들어갑니다**. 들어온 뒤에는 여느 이미지처럼 옮기고 크기를 바꾸며 그 위에 바로 판서할 수 있습니다. 마지막에 보던 자리를 기억하므로 다음에 열면 거기서 시작합니다. 지도를 계속 고쳐 가며 쓰려면 이 창 대신 **지도 문서(`.map`)** 를 만들고 그쪽의 **🖊️ 칠판으로**를 쓰세요
- **환율 넣기**(도구막대의 ₩ 동전 그림) — 고시환율 표나 환율 추이 선그래프를 **이 칠판에** 넣습니다. 자세한 내용은 [💱 환율](#-환율--고시환율-보기환전-계산추이-그래프)을 보세요. ClassDock.exe로 열었을 때만 나타나는 버튼입니다
- 도구상자 항목은 클릭하면 보드 가운데에, 끌어 놓으면 원하는 위치에 삽입. 교육 도형은 선명한 벡터 그룹으로 한꺼번에 이동·크기 조절하고, 선택 후 **분리**를 누르면 선·원·글자 같은 구성 요소를 개별 수정
- **선택·이동** 도구로 이미지·직선·화살표·사각형·원·텍스트를 클릭해 옮기고 `Delete`로 삭제
- 만든 텍스트는 **선택·이동 도구로 더블클릭**해 내용을 다시 수정
- 선택한 이미지·교육 도형은 도구막대의 **↔ / ↕** 로 **좌우·상하 반전**. 그룹으로 묶인 교육 도형은 안쪽 구성 요소까지 함께 뒤집히고, 뒤집은 상태는 저장·복구·리플레이에 그대로 남습니다
- **화면 확대·축소·이동** — 도구막대의 **− / 100% / +** 또는 **마우스 휠**로 0.25~4배까지 조절합니다(휠은 포인터가 가리킨 지점을 기준으로 확대). `Space`를 누른 채 드래그하거나 **휠(가운데) 버튼 드래그**로 화면을 옮기고, 가운데 **100%** 를 누르면 원래 배율·위치로 돌아옵니다. 보드 내용 자체는 그대로이고 보는 화면만 움직입니다
- 도구막대 왼쪽 끝 **⋮⋮ 핸들**을 끌면 상/우/하/좌 4면 중 가장 가까운 변으로 자동 도킹(우클릭 메뉴의 **도구막대 위치**에서 골라도 됩니다)
- 자주 쓰지 않는 버튼은 **⚙ 설정 → 도구 → 화이트보드 탭**에서 조절합니다. 왼쪽 **비노출**과 오른쪽 **노출** 목록 사이로 필요한 도구를 옮기며, 여러 항목은 `Ctrl`·`Shift`로 함께 고를 수 있습니다. 저장하면 열려 있는 보드에도 바로 적용됩니다. 되돌리기·다시 실행은 항상 남고, 전체 도구막대 숨기기와 우클릭 메뉴도 그대로 쓸 수 있습니다
- 색 버튼 오른쪽 **배경** 버튼으로 보드 바탕 바꾸기 — **색 · 무늬 · 배경 그림** 세 가지를 한 판에서 정합니다. 고른 배경은 그 보드에만 적용되고 탭을 닫았다 열어도 유지됩니다. 어두운 배경으로 바꾸면 펜 색이 자동으로 흰색으로 맞춰집니다
  - **색** — 흰색·연회색·크림·칠판 초록·검정 프리셋과 직접 고르기
  - **무늬** — **모눈종이·점 모눈·좌표평면·줄 노트·오선지·원고지**를 바탕에 깝니다. 사진이 아니라 그때그때 그리는 선이라 400%로 확대해도 뭉개지지 않고, **간격·진하기·색**을 조절할 수 있습니다(색 **자**는 바탕색에 맞춰 자동 — 흰 종이엔 짙은 선, 칠판엔 흰 선). 좌표평면의 축은 고른 순간 화면 한가운데에 놓이고, 좌표평면을 한 번 더 누르면 지금 보는 자리로 옮겨집니다
  - **배경 그림** — 사진·학습지 스캔을 보드 화면 자체로 깔고 그 위에 바로 판서합니다. **채움/맞춤/원본/타일**로 놓는 방법을 고르고, **흐리기**로 연하게 만들면 판서가 잘 보입니다. 배경 그림을 쓰는 동안에는 그림과 판서가 어긋나지 않도록 화면이 100% 위치에 고정되어 휠 확대·축소와 빈 공간 끌기 이동이 잠깁니다. 창 크기가 바뀌면 배경은 새 화면 크기에 자동으로 다시 맞춰집니다. **타일**은 그림 한 칸을 보드 전체에 반복해 깔며(무늬처럼), **칸 크기**로 촘촘함을 조절합니다. 넣을 때 자동으로 크기를 줄여 담으므로 사진이 커도 보드가 무거워지지 않습니다
  - 이미 보드에 올려 둔 그림은 **우클릭 → 배경으로**로 바꿀 수 있습니다. 선택 항목에서 빠지고 화면을 채우는 고정 배경이 되어 더 이상 끌거나 크기를 바꿀 수 없습니다(`Ctrl+Z`로 되돌리면 다시 그림으로 올라옵니다)
- 새로 여는 보드의 기본 배경색·무늬는 **⚙ 설정 → 문서 → 화이트보드**에서 정합니다(이미 만들어 둔 보드는 그대로). 배경 그림은 보드마다 따로 고릅니다
- `Ctrl+Z` / `Ctrl+Y` 되돌리기/다시 실행 (배경은 보드 속성이라 되돌리기 대상이 아닙니다)
- **지우개는 판서만 지웁니다** — 바탕에 깔아 둔 무늬·배경 그림은 그대로 두고 그 위에 쓴 글씨만 지웁니다
- **PNG/PDF 내보내기·인쇄·메모로 보내기** — 화면에 보이는 배경(색·무늬·그림)은 담되, 수업 진행용 스포트라이트·가리개는 제외하고 원본 보드를 저장합니다. 인쇄는 도구막대·탭이 아니라 **판서 그림 한 장**을 인쇄하고, 우클릭 메뉴의 **출력·공유**에도 같은 버튼이 있습니다

#### 보드 우클릭 메뉴

보드 아무 곳에서나 **마우스 오른쪽 버튼**을 누르면 도구막대와 같은 기능을 한자리에 모은 메뉴가 뜹니다(`Esc`나 바깥 클릭으로 닫힘).

| 묶음 | 항목 |
|------|------|
| **선택 항목** (누른 자리에 항목이 있을 때) | 편집(텍스트·수식·그래프·차트·표·수 모형·과학 계산) · 복사 · 잘라내기 · 붙여넣기 · 복제 · 앞으로 / 뒤로 / 맨 앞으로 / 맨 뒤로 · **배경으로**(그림) · 좌우 반전 · 상하 반전 · **측정 / 측정 떼기** · **합성 / 합성 떼기**(화살표) · **변환** · 분리 · 삭제 |
| **보드 작업** | 붙여넣기(누른 자리에) · 이미지 · 수학·과학 · **그래프** · **차트** · **주기율표** · 배경 · 축소 / 100% / 확대 · 집중 도구 · 전체 지우기 |
| **교구·정리** | 자 · 각도기 · 컴퍼스 · 15° 맞추기 · 손그림 정리 · 교구 치우기 (켜 둔 것은 눌린 상태로 보입니다) |
| **출력·공유** | PNG 저장 · PDF 저장 · 인쇄 · 메모로 |
| **수업 기록** | ● 녹화 시작 / ■ 정지 |
| **도구막대** | 편집 도구막대 숨기기 / 보이기 |
| **도구막대 위치** | 위 · 오른쪽 · 아래 · 왼쪽 |
| **필기·도형 도구 / 색상·굵기 / 크기 직접 입력** | 도구막대와 같은 도구·색·굵기, 글자 크기(12~72px)와 교육 도형 크기(%) 숫자 입력 |
| **이력** | 되돌리기 · 다시 실행 |

- 항목 위에서 누르면 그 항목이 선택되고, `Ctrl+C`·`Ctrl+X`·`Ctrl+V`로도 복사·잘라내기·붙여넣기가 됩니다. 붙여넣기는 **우클릭한 자리**에 들어갑니다
- **편집 도구막대 숨기기**로 판서 화면만 남길 수 있습니다. 숨김 여부와 도구막대 위치는 **모든 보드에 함께 적용되어 기억**되며, 다시 켜는 방법은 이 우클릭 메뉴뿐입니다
- 집중 도구 실행 중에는 메뉴 맨 위에 **집중 도구** 묶음(원형·사각형·위치 초기화·조절점 숨기기·종료)이 함께 뜹니다
- **측정**은 잴 수 있는 도형에서만 뜨고, 이미 붙여 두었으면 **측정 떼기**로 바뀝니다. **변환**은 변환 창을 열어 고른 도형에 바로 적용합니다
- **교구·정리** 묶음은 빈 곳을 눌렀을 때 뜹니다(도형을 고른 상태에서는 선택 항목 메뉴가 대신 뜹니다)

### 수업 리플레이 (판서 되감아 보기)

화이트보드에 쓴 판서를 **시간 순서대로 녹화**해 나중에 되감아 볼 수 있습니다. 영상이 아니라 벡터(선·도형) 기록이라 파일이 작고 인터넷 없이 동작합니다. **화이트보드**, **PDF 위 필기**, **Python 코드·실행 결과**를 같은 타임라인에 녹화할 수 있습니다.

1. 화이트보드 도구막대 오른쪽 끝 **● 녹화**를 누르고 판서를 시작 (버튼이 **■ 정지**로 바뀌며 깜빡임)
2. 다 쓰면 **■ 정지** → 자동으로 **리플레이 화면**이 열림
3. 재생 바에서 **▶/⏸ 재생·정지**(스페이스), **탐색 슬라이더**로 원하는 순간으로 이동, **0.5~4× 속도**, **⟲ 처음부터**
   - 재생 중 마지막 획은 실제로 **그려지는 것처럼** 나타납니다
4. **💾 저장**으로 `.lesson` 파일 저장 → 결석한 학생에게 전달하거나 다음 수업에서 다시 재생
5. 저장한 `.lesson`은 **더블클릭·드래그**하거나 사이드바 **새로 만들기 → 리플레이 열기(.lesson)**로 다시 엽니다

> 펜·형광펜·지우개·도형·텍스트·이미지 삽입·되돌리기까지 모두 기록됩니다. 판서를 안 하고 설명만 하는 시간은 용량을 늘리지 않아 **녹화 시간 자체에는 제한이 없습니다**(용량은 판서량에 비례).

**PDF 교재 위 필기 녹화**도 같은 방식입니다.

1. PDF를 열고 **✏️ 필기**로 필기바를 켠 뒤, 필기바 오른쪽 **● 녹화**를 누릅니다
2. 교재에 펜·형광펜으로 설명하며 판서(페이지를 넘겨 가며 여러 쪽도 가능)
3. **■ 정지**를 누르면 **교재 페이지 위에서 필기가 되살아나는** 리플레이가 열립니다 — 페이지 전환·지우기까지 순서대로 재생
4. 화이트보드 리플레이와 똑같이 **💾 저장**으로 `.lesson`을 남깁니다

> PDF 필기 리플레이는 판서한 **페이지의 배경 이미지**를 함께 담으므로 화이트보드 리플레이보다 파일이 큽니다(페이지 수·해상도에 비례).

**Python 코드·실행 결과 녹화**도 지원합니다.

1. Python 편집기 실행 바의 **● 녹화**를 누릅니다.
2. 코드를 고치거나 실행하면 코드 변화, 표준 출력·오류·그래프가 시간순으로 기록됩니다.
3. **■ 정지** 후 열리는 리플레이에서 오른쪽 코드·결과 패널과 판서 타임라인을 함께 되감아 봅니다. Python만 녹화한 경우에도 코드 패널 단독 리플레이로 저장됩니다.

### 과제 패키지 (.task) — 만들고 · 풀고 · 검수하기

> **지도 문제도 같은 파일 형식입니다.** 지도 화면의 **🎯 지도 문제**로 만드는 "여기가 어디?" 위치 찾기 문제도 `.task`로 나가고, 제출본(`.taskdone`)·검수·일괄 재채점·성적 CSV를 아래와 똑같이 씁니다(채점만 코드 실행이 아니라 거리 계산). 자세한 사용법은 [지도 (.map)](#-지도-map--표시를-찍어-만드는-수업-지도) 절을 보세요.

**문제 설명 + 시작 코드 + 자동채점 테스트**를 `.task` **한 파일**로 묶어 배포하는 기능입니다. 서버·인터넷 없이 USB나 메신저로 파일만 전달하면, 학생은 더블클릭으로 풀고 선생님은 제출본을 다시 채점해 확인합니다.

**선생님 — 과제 만들기**

1. 파이썬(`.py`) 파일을 열고 코드를 작성한 뒤, 실행 바 **⋯ 더보기 → 채점**에서 테스트(입력값·기대 출력)를 만듭니다
2. 채점 창의 **[📦 과제로 내보내기]** 를 누르면 **과제 빌더**가 열립니다
3. **과제 제목 · 출제자 · 문제 설명**(마크다운 지원, **미리보기** 버튼으로 확인)을 적고, **시작 코드**(학생에게 처음 보여줄 코드)를 다듬습니다
4. 테스트마다 **숨김** 체크 가능 — 숨김 테스트는 학생에게 **통과/실패만** 보이고 입력·기대 출력은 가려집니다
5. 학생 코드가 읽을 **데이터 파일**(csv·txt 등, 합계 8MB)도 첨부할 수 있습니다 — 시작 코드와 같은 폴더에 있는 것처럼 `open('data.csv')`로 읽힙니다
6. **[👀 학생 화면 미리보기]** 로 학생이 여는 그대로 확인(알림의 **[이어서 만들기]** 로 빌더 복귀) → **[📦 .task 내보내기]**
7. 명령 팔레트(`Ctrl+K`)의 **"과제 파일 만들기 (.task)"** 로도 이 흐름에 바로 들어갈 수 있습니다

**학생 — 과제 풀기**

1. 받은 `.task` 파일을 **더블클릭·드래그**로 엽니다 → **문제 설명은 왼쪽 참고 화면에 고정**, **시작 코드는 편집기**로 열립니다
2. 편집기 위 **과제 바**(📦 제목 · 통과 n/m)에서 진행 상태를 보며 코드를 완성합니다. **[문제 보기]** 로 닫았던 문제를 다시 띄울 수 있습니다
3. **[✓ 채점]** — 과제에 담긴 테스트로 자동 채점되어 테스트별 통과/실패와 출력 비교가 표시됩니다(몇 번이든 다시 채점 가능)
4. 다 풀면 **[📤 제출본 내보내기]** → 이름 입력 → `과제제목_이름.taskdone` 파일이 만들어집니다(EXE는 저장 폴더에 기록 + [폴더 열기], 브라우저는 다운로드). 채점을 안 했거나 채점 후 코드를 고쳤으면 **현재 코드로 자동 재채점 후** 내보냅니다
5. 만들어진 `.taskdone` 파일을 선생님께 제출(USB·메신저·학급 게시판 등)

**선생님 — 제출본 검수**

- **한 명씩**: 학생의 `.taskdone`을 더블클릭하면 **검수 화면**이 열립니다 — 학생·제출 시각·**제출본이 신고한 점수**·채점 환경·**파일 검사**(수정·손상 감지)와 제출 코드를 한눈에. 같은 과제의 원본 `.task`가 열려 있으면 **[🔁 재채점]** 이 활성화되어 제출 코드를 **선생님 파일의 테스트로 다시 채점**하고 신고 점수와의 **✓ 일치 / ⚠ 불일치**를 알려줍니다. **[새 탭에서 편집기로 열기]** 로 학생 코드를 직접 실행·단계 실행해 볼 수도 있습니다
- **반 전체 한 번에**: **새로 만들기 → 제출본 일괄 검수(.taskdone)** (명령 팔레트에서도 가능) → **[+ 제출본 추가]** 로 여러 `.taskdone`을 한 번에 선택 → 원본 `.task`를 연 상태에서 **[▶ 전체 재채점]** → 표에 학생별 재채점 점수와 일치/불일치가 채워집니다 → **[⬇ 성적 CSV]** 로 엑셀에서 바로 열리는 성적표 내보내기. 각 행의 **[자세히]** 로 단독 검수 화면을 엽니다

> **제출본에 적힌 점수는 참고용입니다.** 파일 검사(봉인)는 단순 수정·손상만 감지할 수 있어, 확실한 확인은 **선생님 컴퓨터에서의 재채점**으로 하세요 — 점수를 고친 제출본도 재채점에서 불일치로 드러납니다. 과제 파일을 배포 후 수정했다면 "과제 버전이 다름" 경고가 함께 표시됩니다.
> **숨김 테스트의 한계**: `.task`는 텍스트(JSON) 파일이라 마음먹은 학생은 내용을 열어볼 수 있습니다. 변별이 중요한 평가라면 숨김 테스트에만 의존하지 마세요.
> 체험용 샘플이 `docs/` 폴더에 있습니다 — `샘플-두수의합.task` + 제출본 3종(정답/오답/점수 조작). 과제와 제출본들을 함께 열고 일괄 검수를 눌러 보세요.

### 시험지 (.exam) — 출제 · 응시 · 채점

**객관식·주관식 문항(이미지 포함)** 으로 시험지를 만들어 배포하고, 학생이 풀어 **이름·서명**과 함께 낸 답안을 **선생님만 열어 채점**하는 기능입니다. 파일 세 종류를 씁니다.

| 파일 | 무엇 | 누가 가지나 |
|---|---|---|
| `.examkey` | **원본** — 문항 + **정답** + 채점 열쇠. 통째로 선생님 암호로 잠깁니다 | 선생님만 |
| `.exam` | **배포본** — **정답이 아예 들어 있지 않은** 시험지. 열기 암호를 걸 수 있습니다 | 학생에게 배포 |
| `.examdone` | **제출본** — 답안·서명이 봉인된 파일. **선생님 열쇠로만 열립니다** | 학생이 제출 |

**선생님 — 시험지 만들기**

1. **새로 만들기 → 새 시험지 만들기(.exam)** (명령 팔레트 `Ctrl+K` 에서도 가능)
2. **시험지 제목·출제자**를 적고 **[＋ 객관식] · [＋ 주관식]** 으로 문항을 추가합니다. 모든 문항은 **1점**입니다
3. 문항마다 **지문**을 쓰고 **[🖼 이미지 추가]** 로 그림·사진을 넣습니다(문항당 4장, 전체 합계 12MB). 보기에도 이미지를 붙일 수 있습니다
4. **객관식**은 보기 왼쪽 동그라미로 **정답**을 고르고, **주관식**은 정답을 적습니다 — 여러 정답은 `잎|leaf` 처럼 `|` 로 구분하고, **띄어쓰기·대소문자 무시** 옵션을 끌 수 있습니다
5. **[💾 원본 저장(.examkey)]** — 처음 저장할 때 **8자 이상의 선생님 암호**를 정합니다. 이 암호가 정답과 채점 열쇠를 잠그고, 나중에 제출본을 여는 열쇠가 됩니다
6. **[📤 배포본 만들기(.exam)]** — 정답을 뺀 학생용 파일을 만듭니다. 최신 원본이 저장되지 않았다면 먼저 저장하며, 이때 **열기 암호**(시험 시작 때 학생에게 알려줄 암호)를 걸지 물어봅니다
7. 만든 `.exam` 만 USB·메신저로 배포하세요. `.examkey` 는 선생님 컴퓨터에만 두면 됩니다

**학생 — 시험 보기**

1. 받은 `.exam` 을 **더블클릭·드래그**로 엽니다(열기 암호가 걸려 있으면 입력)
2. **객관식은 보기를 클릭**, **주관식은 직접 입력**합니다. 답은 **자동으로 임시 저장**되고 제출 전까지 몇 번이든 고칠 수 있습니다
3. 맨 아래에 **이름**을 적고 **서명**을 그립니다
4. **[📤 제출하기]** → 확인 → `시험제목_이름.examdone` 파일이 만들어집니다(EXE는 저장 폴더에 기록 + [폴더 열기], 브라우저는 다운로드)
5. 그 파일을 선생님께 냅니다

> **제출하면 그 컴퓨터에서는 시험지가 잠깁니다.** 잠금은 파일이 아니라 **시험지 단위**라서, 같은 시험지의 배포본을 새로 만들어 다시 나눠 줘도 이미 제출한 컴퓨터에서는 계속 "제출이 끝났습니다" 화면만 보입니다. 재시험이나 잘못 낸 제출을 되돌리려면 아래 **[🔑 제출 기록 초기화]** 를 쓰세요.

**제출 기록 초기화 (선생님)**

학생 컴퓨터에서 그 시험지를 열면 "제출이 끝났습니다" 화면에 **[🔑 제출 기록 초기화]** 가 있습니다. 푸는 방법은 두 가지입니다.

- **초기화 코드로** (선생님이 자리에 없어도 됨) — 배포본을 만들 때 화면에 뜬 **8자리 코드**(예: `ABCD-2345`)를 그 학생에게만 불러 주세요. 학생이 [초기화 코드 넣기]에 입력하면 풀립니다. 코드를 잊었으면 **채점 화면에서 원본(`.examkey`)을 연 뒤 [🔓 초기화 코드]** 로 다시 볼 수 있습니다(늘 같은 코드입니다)
- **원본 파일로** — [원본 파일 고르기]에서 그 시험지의 **`.examkey`** 를 고르고 **선생님 암호**를 넣습니다. 둘 다 맞아야 하고, 다른 시험지의 원본으로는 풀리지 않습니다

> **코드는 다시 풀게 할 학생에게만 불러 주세요.** 반 전체가 알면 그 시험지에 한해 학생이 스스로 잠금을 풀 수 있습니다(정답이 새는 것은 아닙니다 — 배포본에는 코드도 정답도 들어 있지 않습니다).

잠금이 풀리면 그 컴퓨터에서 다시 풀고 제출할 수 있습니다. **이미 낸 `.examdone` 파일과 선생님 PC가 받아 둔 제출은 그대로 남으므로**, 다시 제출하면 채점표에 같은 학생의 제출이 두 건으로 보입니다 — 채점할 것만 남기세요.

**선생님 PC로 바로 내기 (같은 교실 네트워크)**

파일을 옮기지 않고 제출을 선생님 PC로 곧장 보낼 수 있습니다.

1. **선생님**: 채점 화면의 **[📡 제출 받기 시작]** 을 누릅니다. 처음 켤 때 **Windows 방화벽 허용**이 필요합니다
2. 화면에 뜬 **주소(예: 192.168.0.12 : 17650)** 와 **제출 코드 6자리**를 학생들에게 불러 줍니다
3. **학생**: 제출 상자에서 **[선생님 PC로 바로 보내기]** 를 켜고 주소·코드를 입력합니다. **[연결 확인]** 으로 미리 확인할 수 있습니다
4. **[📤 제출 확정하기]** 를 누르면 바로 전송되고 **접수번호**가 표시됩니다. 선생님 채점표에는 즉시 나타납니다
5. 받은 제출은 선생님 저장 폴더의 **[제출함]** 에도 파일로 남습니다

> **연결이 안 되면 파일로 저장됩니다.** 학교 무선망이 단말끼리의 통신을 막아 두는 경우가 있어, 연결에 실패하면 지금까지처럼 `.examdone` 파일이 만들어집니다 — 그 파일을 선생님께 내면 됩니다. 코드를 잘못 입력한 경우에는 파일을 만들지 않고 다시 입력하도록 알려 줍니다.

**선생님 — 채점**

1. **새로 만들기 → 시험 채점(.examdone)** (또는 `.examdone` 파일을 더블클릭)
2. **[🔑 원본 .examkey 열기]** → **선생님 암호** 입력
3. **[＋ 제출본 추가]** 로 반 전체의 `.examdone` 을 한 번에 선택하면 **자동 채점**됩니다 — 객관식은 정답 대조, 주관식은 정답 목록과 비교
4. 표에 학생별 **점수 / 확인 필요** 문항 수가 나옵니다. **[답안 보기]** 로 문항별 학생 답·정답·**서명**을 보고, **○ 정답 / ✗ 오답** 을 눌러 직접 고칠 수 있습니다(주관식 표현 차이는 여기서 판단)
5. 수동 O/X 결과는 제출본별로 자동 저장되어 채점 화면을 닫아도 유지됩니다. **[⬇ 성적 CSV]**는 현재 시험, **[📚 누적 성적 CSV]**는 저장된 모든 시험의 학생별 점수를 내보냅니다

> **제출하면 학생도 답안 파일을 다시 못 엽니다.** 제출본은 시험지에 들어 있던 **공개키로 봉인**되고, 그 짝인 개인키는 선생님 암호로 잠긴 `.examkey` 안에만 있습니다. 제출 순간 해당 기기의 임시 답안이 지워지고 시험지가 잠깁니다. 브라우저 저장소를 지우거나 다른 기기를 쓰면 재제출할 수 있으므로, 같은 학생의 복수 제출은 채점표에서 확인해야 합니다.
> **선생님 암호를 잊으면 제출본을 영영 열 수 없습니다** — 복구 경로가 없습니다(있으면 그게 곧 뒷문이라서). 시험지마다 암호를 따로 정했다면 어느 시험지의 암호인지 함께 적어 두세요.
> **정답은 배포본에 없습니다.** 학생이 `.exam` 을 텍스트로 열어봐도 정답 자체가 파일에 없습니다. 다만 시험 중 화면 캡처·검색 같은 부정행위는 이 도구가 막지 못하니 교실 운영으로 함께 관리하세요.
> **시험 기능은 EXE(ClassDock)에서 사용하세요.** 브라우저가 `file://` 로 연 페이지에서는 암호 기능을 막기 때문에 시험지를 만들 수도, 제출할 수도 없습니다.

### 대기 화면 (화면보호기)

일정 시간 아무 입력이 없을 때 전체 화면으로 **내 영상**, **웹 페이지**, **시계 애니메이션**을 띄우는 기능입니다. **기본은 꺼져 있고, 설정에서 켤 때만** 동작합니다.

1. 헤더의 **설정**을 열고 **대기 화면(화면보호기)** 을 체크
2. **표시까지 대기 시간**(1·3·5·10·20분) 선택
3. **보여줄 내용** 선택
   - **영상 · 기본 애니메이션** — **영상 선택**으로 지정(없으면 시계 애니메이션). **영상 지우기**로 되돌림
   - **웹 주소(URL)** — 주소를 넣고 **미리보기 · 테스트**로 실제로 뜨는지 확인
4. **저장**을 누르면 적용 — 그 시간만큼 입력이 없으면 대기 화면이 뜨고, **아무 키·클릭·마우스 이동**에 즉시 사라집니다
5. **▶ 지금 시작** — 설정에서 바로 누르거나 **`Ctrl+F12`**(설정 → 단축키에서 변경 가능)를 누르면 **모니터 전체 화면**으로 즉시 대기 화면이 켜집니다(쉬는 시간에 영상 틀어두기). 해제하면 전체 화면도 함께 종료. 유휴 자동 표시는 브라우저 보안상 창 안에서만 뜨지만, F11·⛶ 전체화면 상태라면 자동 표시도 모니터 전체를 덮습니다

> **권장 형식은 MP4(H.264)·WebM** 입니다. MOV·MKV·AVI·WMV 등은 브라우저에서 재생이 안 될 수 있어, 고르면 경고가 뜨고 그래도 쓰면 재생 실패 시 시계 애니메이션으로 대체됩니다.
> **영상은 이 브라우저(앱)에만 저장**되어 프로그램(exe·HTML) 용량은 늘지 않습니다. 영상이 없거나 못 읽으면 자동으로 시계 애니메이션을 씁니다.
> **작업 중에는 뜨지 않습니다**: 파이썬·노트북 실행 중, 동영상·오디오 재생 중, 파일 처리 중, 설정 등 대화상자·미리보기(iframe) 사용 중이면 대기 화면을 미룹니다.

#### 웹 주소(URL)로 띄우기

예: 실시간 바람 지도 `https://earth.nullschool.net/ko/#current/wind/surface/level` — 인터넷에 연결된 상태에서 그 페이지를 대기 화면 가득 띄웁니다.

> **⚠ 로그인된 페이지는 넣지 마세요.** 대기 화면은 이 컴퓨터 브라우저의 로그인 상태를 그대로 씁니다. 메일·성적·개인 페이지를 걸면 **교실 화면에 그 내용이 그대로 뜹니다.** 로그인이 필요 없는 공개 페이지만 쓰세요.
> **안 되는 사이트가 많습니다.** 네이버·구글·유튜브 메인처럼 다른 화면 안에 넣는 것을 막아 둔 곳은 열리지 않습니다(성공 여부는 주소마다 다름). **저장 전에 반드시 `미리보기 · 테스트`** 로 확인하세요.

**유튜브 영상 틀기** — 유튜브 주소를 그대로 붙여 넣으면 주소칸 옆에 **`유튜브 → 퍼가기 주소`** 버튼이 나타납니다. 누르면 열리는 주소로 바꿔 주고, 무엇을 바꿨는지 아래에 알려 줍니다.

- `youtube.com/watch?v=…`, `youtu.be/…`, `shorts/…`, `live/…`, `playlist?list=…` 모두 인식합니다
- **한 영상 반복**은 `loop` 만으로는 안 되고 자기 자신을 `playlist` 로 지정해야 하는데, 이걸 대신 붙여 줍니다
- **`mute`(음소거)** 도 켭니다 — 소리가 켜져 있으면 브라우저가 자동재생을 막습니다. 대기 화면은 어차피 무음입니다
- 주소에 시작 시간(`t=1m30s`)이 있으면 그대로 옮깁니다

> **주소를 바꿔도 안 되는 영상이 있습니다.** 업로더가 **퍼가기를 막아 둔 영상**, **연령 제한** 영상, 지역·저작권 제한 영상은 유튜브가 자기 화면에 "동영상을 재생할 수 없음"을 띄웁니다. 이건 프로그램이 미리 알 수 없어서, **미리보기로 실제 재생되는지 눈으로 확인**하는 것이 유일한 방법입니다.
> **안 열리면 알아서 대체합니다.** 인터넷이 끊겼거나 사이트가 막아 두었으면 저장해 둔 **영상**, 그것도 없으면 **시계 애니메이션**으로 자동 전환됩니다. 대기 화면이 빈 검은 화면으로 남지 않습니다.
> **페이지는 볼 수만 있고 조작은 안 됩니다.** 화면 위에 투명 막을 덮어 마우스·키보드 입력을 대기 화면 해제에만 씁니다(그래야 아무 키나 눌러 돌아올 수 있습니다). 그 페이지가 앱 화면을 다른 곳으로 옮기거나 파일을 내려받는 것도 막아 둡니다.
> **계속 인터넷을 씁니다.** 지도·영상처럼 끊임없이 그려지는 페이지는 CPU도 계속 쓰므로, 사양이 낮은 교실 PC라면 영상 쪽이 더 가볍습니다.

### 블록 문서 (.mnote) — 글·표·이미지를 한 문서에

1. 사이드바 **새로 만들기 → 새 블록 문서** (또는 `Ctrl+K` → "새 블록 문서", 폴더 우클릭 메뉴)
2. **+ 글 / + 표 / + 이미지**로 블록을 쌓습니다. 글은 그냥 타이핑하고, 표는 행·열을 늘려 채우고, 이미지는 파일 선택·붙여넣기·드래그로 넣은 뒤 크기(작게/중간/크게/가득)와 설명을 붙입니다
3. 블록 순서는 위/아래 버튼이나 손잡이 드래그로 바꾸고, 필요 없는 블록은 지웁니다
4. `Ctrl+S`로 저장하면 **`.mnote` 파일 하나**가 만들어집니다. 이미지도 파일 안에 함께 담기므로 그 파일만 옮기면 어디서든 그대로 열립니다
5. `Ctrl+Z`/`Ctrl+Y` 되돌리기, **맞춤법** 검사, 표 셀·글 블록의 **특수문자(`Ctrl+F10`)** 를 모두 쓸 수 있습니다
6. 사이드바 **통합 검색**은 블록 안 글자까지 찾아 그 블록으로 데려다줍니다
7. 다른 곳에 붙여넣으려면 **HTML로 내보내기**(한글·워드에 붙이기 좋음) 또는 **마크다운으로 내보내기**를 쓰세요

> 표 하나는 최대 50행 × 20열, 이미지 한 장은 25MB까지입니다. 이미지가 파일 안에 들어가므로 사진이 많으면 `.mnote` 용량이 커집니다.

### 악보 (.msheet) — 음표를 놓고 바로 들어 보기

1. 사이드바 **새로 만들기 → 새 악보** (또는 `Ctrl+K` → "새 악보", 폴더 우클릭 메뉴)
2. 도구상자에서 **음표 길이**(온·2분·4분·8분·16분)를 고르고 오선 위에 마우스를 올리면 오른쪽에 `입력 위치: 솔4 (G4) · 4분음표`처럼 실제로 들어갈 계이름이 보입니다. 포인터가 십자선으로 바뀐 자리에서 클릭하면 음표가 들어가며 소리도 함께 납니다. 가득 찬 마디나 음역 밖에서는 붉은 점선·금지 포인터와 이유가 표시됩니다. 마우스 대신 **⌨ 자판 작곡**을 켜면 계이름 키를 누르는 대로 현재 길이·옥타브의 음표가 이어서 들어갑니다. 방금 만든 음표가 선택된 상태에서 `←`를 누르면 한 단계 짧게, `→`를 누르면 한 단계 길게 바뀌며 `16분 ↔ 8분 ↔ 4분 ↔ 2분 ↔ 온음표` 사이를 조절합니다. 바꾼 길이는 다음 음표에도 이어지고, 마디의 남은 박자를 넘는 길이는 적용되지 않습니다. 마디가 차면 빈 마디를 자동으로 만들고 다음 오선까지 계속 이어지며 `Esc`로 끝냅니다. 기본 자판은 `A S D F G H J = 도 레 미 파 솔 라 시`, 검은건반은 `W E T Y U`, `Z/X`는 옥타브 내림/올림입니다. `1~5`는 온·2분·4분·8분·16분음표, `R`은 음표/쉼표 전환, `.`은 점/겹점 전환, `[ \\ ]`는 내림표/제자리표/올림표, `=`은 빈 마디 추가, `` ` ``는 새 오선 추가입니다
3. 위쪽 **파트**에서 `＋`를 누르면 피아노·기타·실로폰·하프·플루트·클라리넷 등 악기 파트를 필요한 만큼 추가할 수 있습니다. 목록에서 파트를 고르면 그 악기의 오선으로 전환되며, 옆 이름 칸·**음색**·파트 음량을 각각 바꿀 수 있습니다. 이름 옆 **이조 악기** 칸에서는 `B♭ (클라리넷·트럼펫)`, `E♭ (알토색소폰)`, `F (호른)`, `B♭ 테너`, `E♭ 바리톤`을 고를 수 있습니다. 이조 악기는 악보의 `도`를 불면 다른 음이 나기 때문에 파트마다 조표가 다릅니다. 악기를 고르면 **소리를 그대로 둘지, 적힌 음을 그대로 둘지** 묻습니다 — `소리 유지(옮겨 적기)`는 지금 소리를 지키려고 악보와 조표를 자동으로 옮겨 적어 주므로 **다장조 악보에서 클라리넷 파트보를 바로 만들 수 있고**, `적힌 음 그대로`는 이미 이조된 악보를 옮겨 적어 둔 경우에 씁니다. 파일에는 늘 **연주자가 보는 음**이 담기고 재생·WAV·MIDI 에서만 실제 소리로 내려 울립니다. 상태 줄에 `B♭ 기보 라장조 · 실음 다장조`처럼 두 조표를 함께 보여 줍니다. **M**은 그 파트만 음소거하고 `−`는 현재 파트를 삭제합니다(마지막 한 파트는 남습니다). 피아노 파트는 **🎼 단일 오선**을 눌러 **🎹 피아노 대보표**로 바꿉니다. **오른손**을 고르면 높은음자리표, **왼손**을 고르면 낮은음자리표에 입력됩니다. 한 오선에서 서로 다른 리듬이 겹치면 **성부 1·성부 2**를 바꿔 각각 입력합니다. 각 파트의 두 손과 두 성부는 합주에서 동시에 재생되며 박자 채움도 성부별로 표시됩니다. 다시 단일 오선으로 바꾸면 왼손 음표를 지울지 먼저 확인합니다
4. 한 자리에 여러 음을 넣으려면 먼저 기준 음표를 고르고 **＋화음**을 켠 뒤, 같은 손·같은 마디의 다른 음높이를 누릅니다. 화음 전체가 한 번에 들립니다. **－화음음**은 마지막으로 더한 음을 뺍니다. 이어지는 같은 음 두 개 중 앞 음을 고르고 **⌒ 붙임줄**을 누르면 길게 이어서 소리 나며, **코드 기호**에 `Cm7`, `B♭`, `E♭/G`처럼 입력하면 음표 위에 표시됩니다
5. 음표를 고른 뒤 **⌒ 이음줄**, **가사 ▾**, **셈여림**, **연주 기호**(스타카토→악센트→테누토), **3잇단**, **운지**, **페달**을 사용할 수 있습니다. **|: 반복 시작**, **:| 반복 끝**, **1·2번 괄호**는 현재 마디에 붙으며 재생 순서에도 반영됩니다. **마디 설정**에서는 첫 마디의 못갖춘마디 박 수와 이 마디부터 바뀌는 박자·조표·빠르기를 입력합니다
6. **가사 ▾** 메뉴에서 가사를 넣습니다. **1절 이어치기**를 누르면 위쪽에 입력 줄이 생기고, 한 음절을 친 뒤 `Space`(또는 `Enter`)를 누를 때마다 다음 음표로 옮겨 갑니다. 빈 칸에서 `Backspace`를 누르면 앞 음표로 돌아가 고칠 수 있고 `Esc`로 끝냅니다. **한 줄 붙여넣기**는 `학교 종이 땡땡땡`처럼 한 줄을 넣으면 한글은 글자 하나씩, 영어는 `hap-py`처럼 붙임표로 나눈 자리마다 음표에 차례로 들어갑니다. **쉼표와 붙임줄로 이어진 음표는 자동으로 건너뜁니다.** **절 추가**로 최대 6절까지 쌓을 수 있고, 절을 추가하면 오선 아래에 줄이 하나 더 생기며 단마다 왼쪽에 `1.` `2.` 절 번호가 붙습니다. 아직 비어 있는 절은 고른 음표와 상관없이 늘 첫 음표부터 들어가고, 이미 가사가 있는 절은 고른 음표부터 이어서 고칩니다. **마지막 절 없애기**는 그 절의 가사까지 함께 지우며 `Ctrl+Z`로 되돌릴 수 있습니다. 절이 여럿인 악보는 그림·인쇄·MusicXML에도 모든 절이 함께 나갑니다.
7. **조판 ▾** 메뉴에는 악보를 남에게 나눠 줄 때 필요한 세 가지가 있습니다. **마디 번호**는 `끄기`·`단마다(줄 첫 마디)`·`5마디마다`·`모든 마디` 중에서 고르며 기본은 단마다입니다. 못갖춘마디(여린내기)에는 번호를 붙이지 않고 그 다음 마디가 1번이 되며, **재생 구간 칸(`마디 3 ~ 6`)도 화면에 찍히는 그 번호를 그대로 씁니다.** **한 줄 마디 수**를 `4마디씩`처럼 고정하면 화면 폭과 상관없이 늘 같은 자리에서 줄이 바뀌어, 인쇄본을 나눠 줘도 모두가 같은 마디를 봅니다(좁은 화면에서는 마디를 조금 좁혀서라도 한 줄에 담습니다). **연습 기호**는 `A`·`B` 같은 글자를 마디 위 네모 상자에 붙여 "B부터 다시"가 되게 합니다. **자동으로 매기기**는 도돌이 시작·1번 괄호·＋오선 자리에 A부터 차례로 붙이고(그런 표시가 없으면 8마디마다), 손으로 적어 둔 기호는 그대로 두고 그 글자를 건너뜁니다. 연습 기호는 마디의 성질이라 **모든 악기 파트에 같은 자리로 붙습니다.** 마디 번호와 연습 기호는 그림·인쇄에도 함께 나갑니다.
8. **점**(한 번 더 누르면 겹점), **쉼표**, **♯ ♭ ♮**, **지우개**를 도구상자에서 켜고 끕니다. 악보를 **마우스 오른쪽 버튼으로 누르면** 손·성부 선택, 음표 길이·임시표·쉬운 도레미 입력, 화음·붙임줄·이음줄·가사·표현 기호, 반복·마디 설정, 대보표·마디·오선·초기화, 재생·연습, 저장·내보내기, **편집 도구막대 숨기기·보이기** 메뉴를 바로 쓸 수 있습니다. 새 악보에서는 기본적으로 꺼져 있는 **계이름**을 켜면 각 음표 아래에 `도·레·미·파♯`가 표시됩니다
9. `＋마디`/`－마디`로 마디를 더하거나 지우고, `＋오선`/`－오선`으로 마지막에 악보 한 단을 추가하거나 제거합니다. 연습 내용을 한꺼번에 지우려면 **↺ 초기화**를 누릅니다. 확인 후 빈 1마디로 바뀌지만 제목·빠르기·박자·조표·음색과 대보표 설정은 유지되며 `Ctrl+Z`로 되돌릴 수 있습니다
10. **음표를 누르면 음표 또는 화음 전체가 들립니다.** 일반 상태에서 위아래로 드래그하거나 `↑`/`↓`를 누르면 오선의 줄·칸에 맞춰 높이가 바뀝니다(`Shift`와 함께 누르면 한 옥타브). `←`/`→`로 이웃 음표로 이동하고 `Delete`로 지웁니다. **위치 조정** 또는 `Alt+드래그`로 음표를 좌우로 조금 옮길 수 있습니다
11. **▶ 전체 재생**으로 음소거하지 않은 모든 악기 파트를 합주로 듣고, **▶ 현재 파트**로 편집 중인 악기만 확인합니다. 대보표에서는 **▶ 오른손**·**▶ 왼손**으로 한 손씩 따로 연습합니다. **마디 3 ~ 6**처럼 범위를 적고 **▶ 이 구간만**으로 부분만 들을 수도 있습니다. 반복선과 1·2번 괄호는 전체·구간 재생에 반영됩니다. 연습할 때는 속도 **50%·75%·100%**, **1234 준비**, **♩ 메트로놈**을 쓸 수 있습니다
12. **🎯 따라치기**를 누르면 악보가 흐려지고, 자판을 건반 삼아 음을 차례로 눌러 봅니다. 기본값은 **A S D F G H J = 도 레 미 파 솔 라 시**, 검은건반은 **W E T Y U**이며 숫자 **1~7**도 도~시로 쓸 수 있습니다. **⚙ 설정 → 단축키 → 악보 자판**에서 계이름마다 원하는 키를 지정하면 자판 작곡·따라치기·음감 테스트에 함께 적용됩니다. **옥타브는 달라도 맞은 것으로 봅니다** — 계이름만 맞으면 됩니다. 맞으면 그 음표가 제 색으로 살아나며 악보에 적힌 높이 그대로 소리가 나고, 틀리면 그 자리가 빨개지며 `누른 음: 라 · 이 자리는 솔`처럼 알려 줍니다(틀린 키로는 진도가 나가지 않습니다). 쉼표는 자동으로 넘어가고, `Backspace`로 한 음 되돌아가면 그 자리 빨간 표시가 지워져 정확도가 도로 올라갑니다. **마디 3 ~ 6** 범위와 대보표의 **양손·오른손·왼손** 고르기를 그대로 따르며, 자판 대신 **쉬운 입력**의 도레미 버튼이나 **전자 피아노(MIDI 건반)** 로 눌러도 됩니다 — 재생 바의 **🎹 MIDI 입력**을 켜면 되고, 이미 허락해 둔 건반이 연결돼 있으면 연습을 시작할 때 **🎹 켜기** 단추가 달린 안내가 뜹니다. 옥타브를 통과시키므로 25·32건반짜리 작은 키보드로도 악보 전체를 연습할 수 있습니다. 다 누르면 정확도·걸린 시간·분당 음 수를 알려 주고, `Esc`로 언제든 그만둡니다. 연습하는 동안에는 악보가 바뀌지 않도록 편집 도구가 잠깁니다. 악보 우클릭 메뉴의 **재생·연습 → 따라치기(자판으로 음 맞추기)** 로도 시작할 수 있습니다
13. **🎧 음감 테스트**는 반대로 **악보를 감추고 소리만** 들려줍니다. 옆에서 **단계**(1단계 흰건반 한 옥타브 → 2단계 흰건반 두 옥타브 → 3단계 검은건반까지 → 4단계 옥타브까지 맞히기)와 **문항 수**(5·10·20문제)를 고르고 누르면 됩니다. 들은 음의 계이름을 따라치기와 같은 사용자 지정 악보 자판으로 누르거나 화면의 계이름 단추, **MIDI 건반**으로 누릅니다. 앞 음과 견주어 맞히지 못하도록 **문제 사이에 낮은 간섭음**이 한 번 지나가고, **다시 듣기는 한 문제에 한 번**(`Space`)만 됩니다. 틀려도 정답을 들려주고 다음 문제로 넘어가며(따라치기와 다른 점입니다), 다 풀면 **정확도·평균 반응 시간·자주 헷갈린 음 짝**이 나옵니다 — **↻ 한 번 더**로 새 문제를 바로 뽑고 **닫기**(`Esc`)로 악보에 돌아옵니다. **🎵 기준음**을 켜면 시작할 때 가온다(도4)를 들려주어 **상대음감** 연습이 되고, 끄면 기준 없이 **절대음감** 연습이 됩니다. 4단계에서는 계이름을 누른 뒤 옥타브(숫자 `4`·`5` 또는 화면 단추)를 한 번 더 누릅니다. 단계·문항 수·기준음은 모든 악보에 함께 적용되어 기억되고, 악보 우클릭 메뉴의 **재생·연습 → 음감 테스트(듣고 음 맞히기)** 로도 시작할 수 있습니다
14. 위쪽에서 **♩빠르기**(40~208), **박자**(2/4·3/4·4/4·6/8), **조표**(장조·단조 각각 올림/내림표 7개까지), **음색**을 바꿉니다. 새 악보는 실제 녹음을 사용하는 **피아노(추천)** 가 기본이며, **기타(나일론)·실로폰·하프·플루트·클라리넷**과 합성음도 선택할 수 있습니다. **신디사이저**를 고르면 아래에 `🎛 신디사이저 음색 만들기`가 열립니다. **리드·패드·베이스·플럭·오르간** 프리셋에서 시작해 사인·삼각·사각·톱니 파형, **Attack·Decay·Sustain·Release**, 필터의 **Cutoff·Resonance**, **코러스·딜레이·리버브**를 조절합니다. 값을 바꾸면 사용자 설정으로 바뀌고 현재 파트에 따로 저장되며, 음표 미리듣기·전체 합주·WAV 저장에 같은 소리가 적용됩니다. 제목 줄이 복잡할 때는 패널 제목을 눌러 접습니다. 재생 줄의 **🥁 반주**에서 **기본 드럼·동요·발라드·행진·왈츠·록**을 고르면 박자표에 맞춘 킥·스네어·하이햇이 악보와 함께 재생됩니다. 이어서 **드럼만·드럼+베이스·전체 반주**를 고를 수 있습니다. 베이스와 전체 반주는 음표 위에 입력한 코드 기호를 읽으며, 전체 반주의 코드 악기는 **피아노·기타** 중에서 고릅니다. `C`, `Cm`, `G7`, `B♭maj7`, `E♭/G`, `F#m7b5`, `Csus4`, `Dadd9` 같은 장·단·7·6·9·sus·dim·aug·add9 및 슬래시 코드를 지원합니다. 첫 코드 전에는 드럼만 재생되고, 입력한 코드가 다음 코드까지 이어집니다. `N.C.` 또는 해석할 수 없는 코드 기호에서는 코드 반주를 멈춰 잘못 짐작해 연주하지 않습니다. 발라드는 4/4·6/8, 행진은 2/4·4/4, 왈츠는 3/4, 록은 4/4에서 고를 수 있고, 맞지 않는 스타일은 목록에서 비활성화됩니다. 악보 중간에 박자가 바뀌어 스타일이 맞지 않는 마디는 기본 드럼으로 이어집니다. 옆 음량으로 드럼·베이스·코드 반주의 크기를 함께 맞출 수 있으며 선택과 음량은 악보에 저장됩니다
15. 노래가 아이들 목소리에 너무 높거나 낮으면 **조옮김**을 누르고 `온음(장2도) 올리기 — 라장조`처럼 간격을 고릅니다. 음표와 조표가 함께 움직여 **멜로디는 그대로이고 높이만 바뀝니다**(옆의 **조표**는 반대로 음표를 제자리에 두고 조표만 바꿉니다). 반음~완전5도와 한 옥타브를 위아래로 고를 수 있고, 화음·두 손·두 성부·중간에 바뀌는 조표까지 함께 옮깁니다. 권장 음역(오른손 G3~C6·왼손 C2~C5)을 벗어나는 음이 생기면 먼저 물어보며 `Ctrl+Z`로 되돌릴 수 있습니다. 악보 우클릭 메뉴에도 같은 **조옮김**이 있습니다
16. **🎹 MIDI 입력**(악보 우클릭 메뉴의 **다음 입력 도구 → MIDI 건반으로 입력**도 같습니다)을 켜고 연결된 MIDI 건반을 누르면 현재 파트의 손·성부에 입력됩니다. 거의 동시에 누른 음은 화음으로 묶입니다. **⬇ MIDI**는 각 악기를 프로그램·채널·음량이 분리된 **형식 1 다중 트랙**으로 저장해 다른 음악 프로그램과 전자 악기에서 편집·재생할 수 있습니다
17. 사진이나 스캔 악보를 옮겨 적을 때 **🖼 악보 이미지 참고**로 이미지를 열면 악보 옆에 나란히 표시되고 50~250%로 확대할 수 있습니다. 이 기능은 원본을 보며 정확히 입력하도록 돕는 참고 창이며 음표를 자동 판독하지는 않습니다
18. 악보 오른쪽의 **− / ＋**로 50~200% 범위에서 확대·축소하고 **맞춤**으로 100%에 돌아옵니다. `Ctrl+마우스 휠`도 쓸 수 있으며, 확대된 악보는 오선 밖 여백을 끌어 상하좌우로 이동합니다
19. 도구가 많아 악보가 좁게 느껴지면 위쪽 **▤ 도구 숨기기**(자판 `H`)로 **편집 도구·쉬운 입력·재생** 세 줄을 접어 악보만 넓게 볼 수 있습니다. 제목·저장·되돌리기가 있는 맨 윗줄은 남아서 **▤ 도구 보이기**로 언제든 다시 펴며, 접은 동안에도 악보 우클릭 메뉴에 같은 기능이 모두 있습니다. 접은 상태는 **모든 악보에 함께 적용되어 기억**됩니다. **⛶ 전체화면**(헤더 ⛶ 또는 `Ctrl+K` → "문서 영역 전체화면")에서는 **맨 윗줄까지 접혀 악보만 남습니다** — `H`나 우클릭 메뉴로 도구를 다시 부를 수 있고, 전체화면을 나가면 들어가기 전 상태로 돌아옵니다

자주 쓰지 않는 버튼은 **⚙ 설정 → 도구 → 악보 탭**에서 왼쪽 **비노출**과 오른쪽 **노출** 목록 사이로 옮깁니다. 음표 길이·임시표·재생처럼 한 기능을 이루는 여러 버튼은 한 항목으로 함께 조절됩니다. 저장·되돌리기·다시 실행·도구 보이기는 항상 남고, 저장하면 열려 있는 악보에도 바로 적용됩니다. 계이름·옥타브·음표 길이·쉼표·점음표·임시표·마디 추가·오선 추가 자판은 **⚙ 설정 → 단축키 → 악보 자판**에서 모두 바꿀 수 있으며 같은 키를 두 기능에 중복 지정할 수 없습니다.

20. MuseScore 같은 다른 악보 프로그램에서 만든 **`.musicxml` 또는 압축형 `.mxl`** 파일은 편집 도구의 **📂 MusicXML**에서 고르거나, 일반 파일 열기로 열거나, 창에 끌어다 놓으면 모든 악기 파트를 편집 가능한 새 `.msheet` 악보로 가져옵니다. 파트 이름과 두 성부·화음·붙임줄·이음줄·가사·표현 기호·셋잇단음표·반복·마디별 설정도 지원하며 **⬇ MusicXML**로 모든 파트를 다시 내보낼 수 있습니다
21. **⬇ WAV 저장**을 누르면 음소거하지 않은 모든 악기 파트와 드럼·베이스·코드 자동 반주를 섞은 연주 결과가 **소리 파일(.wav)** 로 저장됩니다. **🖨 인쇄**에서 프린터 대신 **PDF로 저장**을 고르면 현재 선택 파트의 악보 PDF가 됩니다. `Ctrl+S`는 전체 파트가 든 `.msheet`를 저장하고 `Ctrl+Z`/`Ctrl+Y`로 되돌립니다
22. **🎧 연습 음원**은 합창·합주 연습에 나눠 줄 음원을 파트마다·템포마다 한 번에 만듭니다. 누르면 설정 줄이 열리고 **모든 파트마다 한 벌**(또는 지금 파트만), **나머지 파트 음량**(`내 파트만`·20%·40%·60%), **템포**(100%·85%·70% 중 복수), **메트로놈**, **한 마디 준비**를 고른 뒤 **만들기**를 누릅니다. 내 파트는 그대로 두고 나머지 파트와 반주만 작게 깔기 때문에 **박자와 길이는 합주와 완전히 같아** 함께 맞춰 부르기 좋습니다. 한 벌이면 `학교종 - 소프라노 (100%).wav`로 바로 저장되고, 여러 벌이면 **`학교종 - 연습음원.zip` 한 장으로 묶어** 저장합니다(파일을 연달아 내려받으면 브라우저가 막습니다). 만드는 동안 `3/6 · 알토 85% 만드는 중…`처럼 진행률이 보이고 **그만두기**로 멈출 수 있으며, 한 번에 12벌까지 만들 수 있습니다.

> 피아노·실로폰·하프·플루트와 클라리넷 음원, 그리고 나일론 기타 음원은 `tonejs-instruments`에서 음역에 맞게 선별한 실제 녹음입니다. 피아노·실로폰·하프·플루트는 VSCO 2, 기타는 `quartertone`의 Classical Guitar Multisample이 출처이며, `tonejs-instruments`의 [CC BY 3.0](https://creativecommons.org/licenses/by/3.0/) 조건에 따라 사용합니다. 자세한 표기는 각 `src/assets/<악기>/ATTRIBUTION.md`에 있습니다.

**자판 단축키** — `1`~`5` 음표 길이, `R` 쉼표, `.` 점, `H` 도구막대 접기·펴기, `↑↓←→` 이동, `Delete` 지우기, `Esc` 선택 풀기, `Ctrl`+`+`/`-` 확대·축소, `Ctrl+0` 100%

**자판 작곡·따라치기 자판** — 기본값은 `A S D F G H J` 도 레 미 파 솔 라 시, `W E T Y U` 검은건반(도♯ 레♯ 파♯ 솔♯ 라♯), `Z/X` 옥타브 내림/올림입니다. 자판 작곡에서는 `←/→` 선택 음표 짧게/길게, `1~5` 음표 길이, `R` 음표/쉼표, `.` 점/겹점, `[ \\ ]` 임시표, `=` 마디 추가, `` ` `` 오선 추가도 씁니다. **설정 → 단축키 → 악보 자판**에서 계이름과 작곡 도구 키를 바꿀 수 있습니다. 따라치기에서는 `1`~`7`도 도 레 미 파 솔 라 시로 쓸 수 있고, `Backspace` 한 음 뒤로, `Esc` 그만두기입니다

**음감 테스트 자판** — 계이름은 설정한 악보 자판과 `1`~`7`, `Space` 다시 듣기(문제마다 한 번), `Esc` 그만두기. 4단계에서 옥타브를 물을 때는 숫자 `4`·`5`가 옥타브입니다

> 각 오선은 독립 성부 두 개까지 지원합니다. 꾸밈음과 3잇단 이외의 복잡한 잇단음표는 아직 없으며, 붙임줄은 바로 다음의 같은 높이 음에만 연결됩니다.
> MusicXML은 첫 파트와 각 오선의 성부 두 개까지 가져옵니다. 프로그램마다 표현 방식이 다른 고급 표기는 단순화될 수 있습니다. Finale·Sibelius·MuseScore의 전용 파일은 해당 프로그램에서 먼저 MusicXML로 내보내세요.
> 조표를 바꾸면 **임시표 없이 적혀 있던 음은 새 조표를 따라갑니다**(다장조 파 → 사장조 파샵). 일부러 ♯·♭를 붙여 둔 음은 그대로 둡니다.
> 박자를 바꿔서 마디가 넘치면 아래에 경고가 뜹니다 — 음표를 지우거나 `Ctrl+Z`로 되돌리세요.

### 지도 (.map) — 표시를 찍어 만드는 수업 지도

1. 사이드바 **새로 만들기 → 새 지도** (또는 `Ctrl+K` → "새 지도", 폴더 우클릭 메뉴). 처음에는 남한 전체가 한 화면에 들어오는 자리에서 시작합니다
2. 마우스로 끌어 옮기고 휠로 확대·축소합니다. 위쪽 칸에 **장소 이름**(예: `경복궁`)을 적고 **검색**을 누르면 **후보 목록이 칸 아래로 펼쳐지고, 그중 하나를 눌러야 그 자리로 이동**해 **임시 빨간 점**으로 보여 줍니다(검색만으로는 지도가 움직이지 않습니다 — 첫 후보가 엉뚱할 때 보던 자리를 잃지 않게). `Enter`를 눌러도 같으며, 첫 줄이 미리 짚어져 있어 `Enter`를 한 번 더 누르면 첫 후보로 갑니다. `↓`·`↑`로 다른 후보를 고를 수 있습니다. 후보는 **많아야 8개**까지 나옵니다. **후보가 하나뿐이면 고를 것이 없으므로 곧장 그 자리로 이동합니다.** 빨간 점은 검색 위치 안내라 지도 파일이나 칠판 그림에는 들어가지 않습니다. **`37.5665, 126.978`** 처럼 위도·경도를 적으면 목록 없이 바로 이동합니다. 빨간 점과 이름표는 지도를 눌러도 남아 있고 `Esc`로 지웁니다(표시 추가·그리기 중이거나 영역을 고른 상태에서는 그쪽 취소가 먼저입니다)
   - 검색칸을 누르면 **최근 검색어**가 아래로 펼쳐집니다(최근 8개). 글자를 치면 그 글자가 든 기록만 남고, `↓`·`↑`로 고른 뒤 `Enter`로 검색합니다. 줄 오른쪽 `×`는 그 한 줄만, 맨 아래 **검색 기록 지우기**는 전부 지웁니다. 검색어는 지도 파일이 아니라 이 컴퓨터에만 남습니다
   - **글에서 고른 지명을 그대로 검색** — 어떤 문서에서든 지명을 마우스로 긁고 **오른쪽 버튼 → 지도에서 검색**을 누르면, 지도 탭으로 건너가 그 낱말이 검색칸에 들어가고 **주소 후보 목록**이 칸 아래에 펼쳐집니다(후보가 하나뿐이면 곧장 그 자리로 이동). 열어 둔 지도가 있으면 **가장 최근에 보던 지도 탭**으로 가고, 없으면 새 지도를 하나 엽니다. 고른 글자가 없거나 문단째 긁었을 때(40자 초과)는 메뉴가 흐리게 보입니다
     - **쓸 수 있는 곳** — 텍스트·코드 문서(보기·편집 모두), PDF, Word(`.docx`), 한글·PowerPoint 미리보기, 노트북 셀, 표(스프레드시트) 셀, 메모장·블록 문서(글·표 셀), 파일 비교 화면 등 **글자를 고를 수 있는 화면 대부분**입니다
     - **보기 전용 화면**에서는 글자를 고른 뒤 **고른 글자 위에서** 오른쪽 버튼을 눌러야 `복사 · Google에서 검색 · 지도에서 검색 · 파일에서 검색` 메뉴가 뜹니다. Google 검색은 공백을 정리한 300자 이하 선택에서만 나타나고 기본 브라우저의 새 탭·창으로 결과를 엽니다. 시험 응시 화면에서는 검색 항목을 내놓지 않습니다. 아무 글자도 고르지 않았거나 고른 글자에서 떨어진 곳을 누르면 브라우저 기본 메뉴가 그대로 나옵니다(그림 저장 같은 기능을 막지 않기 위해서입니다)
     - 같은 메뉴의 **파일에서 검색**은 고른 글자를 사이드바의 **파일명·내용 검색**에 넣고 찾습니다. 접어 둔 사이드바는 자동으로 펴지고, 검색어가 통째로 골라진 채 놓이므로 결과를 보며 바로 고쳐 칠 수 있습니다. 열려 있는 문서의 **본문까지** 훑으므로 "이 낱말이 어느 파일에 또 있지?"를 그 자리에서 확인할 수 있습니다(80자까지)
     - 화이트보드·지도·악보처럼 **자기 우클릭 메뉴가 있는 화면**은 원래 메뉴를 그대로 씁니다
   - 국내 상세주소·건물 이름을 더 자세히 찾으려면 **설정 → 일반 → 지도 검색**에서 **카카오 — 개인 API 키**를 고릅니다. Kakao Developers에서 만든 앱의 **REST API 키**를 넣고 **키 저장·연결 시험**을 누르세요. 연결에 성공하면 카카오 검색이 즉시 적용됩니다. **Admin 키는 사용하지 않습니다.**
   - 카카오 검색은 **주소 검색 → 장소명 검색** 순서로 시도하고, 키가 없거나 응답을 받지 못하면 OpenStreetMap 검색으로 자동 전환합니다. 화이트보드의 **지도 넣기** 검색에도 같은 설정이 적용됩니다.
   - 키를 넣으면 **주소 CSV 좌표 찾기·주소 자동 채우기·지역 통계**가 더 정확해지고, **🏫 주변 시설**은 카카오에만 있는 기능이라 키를 넣기 전에는 버튼이 흐리게 보입니다(눌러 보면 켜는 법을 알려 줍니다).
   - `ClassDock.exe`에서 **이 Windows 사용자 계정에 암호화하여 기억**을 켜면 키는 Windows 계정 보호 기능으로 암호화되어 이 PC의 현재 사용자만 읽을 수 있게 저장됩니다. HTML·브라우저 설정·작업공간·백업에는 들어가지 않습니다. 체크를 끄면 이번 실행 동안만 기억하며, **키 지우기**로 즉시 삭제할 수 있습니다.
   - 일반 브라우저 창과 주소창 없는 **앱 모드**가 서로 다른 브라우저로 열리더라도, EXE가 검색 공급자 선택을 함께 보관해 카카오 설정을 자동으로 이어 줍니다.
3. **배경지도**는 네 가지입니다 — **일반 지도**(길·건물 이름이 가장 자세함), **지형(등고선)**(산과 높이), **흑백(판서용)**(색이 옅어 그 위에 그리기 좋음), **위성 사진**. 배경마다 확대할 수 있는 한계가 달라, 덜 자세한 배경으로 바꾸면 확대가 자동으로 한 단계 물러납니다
4. **🖼️ 내 지도**로 학교 배치도·교실 평면도·박물관 안내도 같은 PNG/JPG/WebP 이미지를 고르면, 그 이미지가 **현재 보고 있는 지도 범위에 맞춰져 오프라인 배경**으로 들어갑니다. 원본은 20MB 이하만 받고 긴 변 2400px 안으로 정리해 `.map` 파일에 함께 담습니다. 배경지도 선택에서 일반 지도와 내 지도 이미지를 오갈 수 있고 **이미지 지우기**로 제거합니다
5. **📍 표시 추가**를 누른 뒤 지도를 클릭하면 그 자리에 핀이 생기고 편집 풍선이 열립니다. **이름**(예: 우리 학교)과 **메모**(수업 설명·관찰 기록)를 적고 **색 여섯 가지** 중에서 고릅니다. 잘못 눌렀으면 `Esc`로 추가를 취소합니다
   - **🏷️ 이름 보이기** — 표시 이름은 평소 마우스를 올려야 보입니다. 이 단추를 켜면 **모든 표시의 이름이 지도 위에 늘 붙어** 있어, 주변 시설을 여러 곳 넣었을 때 어디가 어디인지 한눈에 들어옵니다. 이름표가 서로 포개져 못 읽는 일이 없도록 **확대 13단계보다 멀리서는 잠시 숨겼다가** 다가가면 다시 나타납니다. 켠 상태는 **지도 파일에 저장되어** 나눠 준 지도에서도 그대로 나옵니다(옛 지도는 꺼진 채로 열립니다). 표시가 200개를 넘으면 지도가 느려지므로 켜지지 않고 그 까닭을 알려 줍니다. 칠판·PNG·인쇄로 보낼 때는 이 단추와 상관없이 **언제나 모든 이름이 그림에 새겨집니다**
   - **🧩 표시 묶기** — 표시가 20개 이상이고 확대 13단계 이하로 멀리 보일 때, 가까이 겹친 핀을 **개수가 적힌 한 묶음**으로 합쳐 지도를 읽기 쉽게 합니다. 묶음을 누르면 그 표시들이 보이는 범위로 확대됩니다. 켜고 끈 상태는 이 컴퓨터에 기억되지만 지도 파일 내용은 바꾸지 않습니다
   - **🧵 표시 잇기** — 표시가 두 개 이상이면 **표시 목록의 순서대로 점선으로 이어** 전체 거리를 보여 줍니다. 표시를 옮기거나 지우고, 목록의 `▲▼`로 순서를 바꾸면 선도 곧바로 따라갑니다. 목록에서 감춘 묶음은 선에서도 빠지며, 켠 상태는 지도 파일에 저장됩니다. 표시가 수십 개라 선이 엉켜 보이면 표시 목록에서 발표 순서를 다듬으세요
   - **🚗 길찾기 비교·설정** — 표시 목록 순서를 **실제 도로를 따라** 이어 주행 거리와 예상 시간을 보여 주며, 같은 창에서 **직선 연결 거리와 자동차 경로의 차이**를 비교합니다. `추천 경로 / 최단 시간 / 최단 거리`, 피할 길(페리·유료 도로·자동차 전용 도로·어린이 보호 구역·유턴), 차량 유종·하이패스, 대안 경로, 출발·도착 뒤집기를 고를 수 있습니다. 대안 경로를 켜면 최대 세 경로를 지도 선과 비교 카드로 보여 주며 **통행료·택시 예상 요금**도 함께 표시합니다. 첫 경로는 출발지·경유지·도착지 사이의 **구간별 거리와 예상 시간**도 표로 보여 줍니다
      - 도로 선은 현재 교통 상태에 따라 **초록(원활)·노랑(서행)·주황(지체)·빨강(정체)·검정(사고·통행 불가)·회색(정보 없음)**으로 나뉩니다. 선에 마우스를 올리면 도로명·속도·구간 거리를 확인할 수 있습니다. 설정 창의 **순서별 주행 안내**에는 출발·좌회전·우회전·도착 같은 안내가 차례로 나오며, 한 줄을 누르면 지도에서 그 위치로 이동합니다. 이 정보는 이미 받은 길찾기 응답을 이용하므로 별도 API 호출을 추가하지 않습니다
      - **경유지 순서 자동 최적화**는 출발지와 도착지는 그대로 두고 가운데 경유지만 바꿔, 가능한 모든 순서 중 **직선 연결 거리가 가장 짧은 순서**를 고릅니다. 실제 도로 길이로 수십 번 다시 요청하지 않으므로 API 쿼터를 더 쓰지 않습니다
      - 길을 찾은 뒤 **경로를 도형으로 저장**을 누르면 파란 일반 거리선으로 지도 파일 안에 고정됩니다. 고정한 경로는 인터넷과 키 없이 다시 볼 수 있고, **지도 자료 내보내기 → GPX**로 GPS·운동 기록 앱에 보낼 수 있습니다. 고정하지 않은 실시간 자동차 경로는 다음에 지도를 열 때 다시 찾습니다
      - **카카오 지도 검색을 켜고 REST API 키를 넣어야** 쓸 수 있습니다(위 2번). 장소 검색에 쓰던 **같은 키**를 사용하므로 추가 키는 필요하지 않습니다. 키가 없거나 카카오 검색이 꺼져 있으면 버튼 자체가 비활성화되고, 마우스를 올리면 필요한 설정을 알려 줍니다. 키를 저장하거나 지우면 열려 있는 지도 버튼 상태도 바로 바뀝니다. 길찾기 호출은 장소 검색과 별도로 하루 무료 몫이 매겨집니다
      - 한 번에 이을 수 있는 표시는 **7개까지**입니다(카카오 경유지 상한). 보이는 표시가 그보다 많으면 **현재 설정한 순서의 앞 7개만** 길을 찾고, 선 이름표에 그 사실을 적습니다. `출발·도착 순서 뒤집기`를 켜면 목록의 뒤쪽 표시부터 7개를 사용합니다
      - 설정과 켠 상태는 지도 파일에 저장되지만, **도형으로 저장하지 않은 길 자체는 저장하지 않습니다** — 다음에 열 때 다시 찾아오므로 표시를 옮긴 뒤에도 선이 옛 자리에 남지 않습니다. 인터넷이 없는 자리에서 열면 고정한 경로만 남습니다
6. **📏 거리선**은 경로의 점을 차례로 찍고 `Enter`·**마우스 오른쪽 버튼**(또는 같은 버튼 다시 누르기)으로 끝내 전체 거리를 표시합니다. **▱ 면적 영역**도 꼭짓점을 3개 이상 찍어 같은 방식으로 끝내면 면적을 표시합니다. 점을 하나 찍고 나면 **마지막 점부터 마우스까지 점선**이 따라와 다음 구간이 어디로 이어질지 미리 보이고(거리선은 파랑, 면적은 초록), 점선은 그리기가 끝나면 사라져 저장되는 도형에는 들어가지 않습니다. 그리는 중의 오른쪽 클릭은 지도 빠른 메뉴 대신 **완료**이며, 점이 모자라면 브라우저 메뉴가 뜨지 않고 `점을 2개(면적은 3개) 이상 찍어 주세요` 안내만 나옵니다. 선·영역을 누르면 이름·색을 바꾸거나 지울 수 있고 `Esc`로 그리기를 취소합니다. 면적 영역을 다시 누른 뒤 **▣ 영역 표시 분석**을 고르면 경계 안의 표시 개수·색상별 개수·목록을 보고 CSV로 저장하거나 메모 표로 보낼 수 있습니다
7. **축척 막대와 방위표**는 지도 오른쪽 아래에 늘 붙어 있고, 확대·축소에 따라 `500 m`·`2 km`처럼 눈금이 자동으로 바뀝니다. 그림으로 굳혀 칠판·메모로 보내거나 PNG·인쇄로 내보낼 때도 **함께 새겨지므로**, 학습지에 붙인 지도에서도 거리를 가늠할 수 있습니다
8. **🌐 위경도 격자**를 누르면 위선·경선이 눈금으로 그려지고 가장자리에 `37.5°N`·`127°E` 같은 이름표가 붙습니다. 확대할수록 간격이 잘아지고(30°→10°→…→0.001°), **적도(0°)와 본초자오선(0°)은 붉고 굵게** 따로 이름이 붙습니다. 격자를 켠 상태는 **지도 파일에 저장되어** 다음에 열 때도, 나눠 준 지도에서도 그대로 나옵니다(옛 지도는 꺼진 채로 열립니다)
9. **자료 들이기**는 지도용 CSV, 연대표용 CSV·Excel(`.xlsx`), GeoJSON·GPX·KML을 자동으로 구분해 표시와 도형을 한꺼번에 추가합니다.
   - **지도용 CSV** — 첫 줄에 `이름,위도,경도,메모,색상,주소,전화번호,시도,시군구` 열을 사용합니다(최대 5MB·5,000개. 열 이름은 `장소`·`설명`·`소재지`·`연락처`나 `name`·`lat`·`lng` 같은 영문도 알아봅니다). **CSV 내보내기**는 현재 표시를 Excel에서 열 수 있는 같은 형식으로 저장하므로, 내보낸 파일을 그대로 다시 들여와도 주소·전화번호·지역이 살아 있습니다. 색상은 `red`, `blue`, `green`, `amber`, `purple`, `slate` 또는 한국어 색 이름을 씁니다
   - **연대표용 CSV·Excel** — `시작, 종료, 제목, 분류(또는 유형), 유적지(또는 장소), 유적지 주소(또는 장소 주소), 설명(또는 메모), 색상` 열을 읽습니다. 연대표 화면의 **표 내보내기**로 만든 CSV와 Excel을 그대로 고를 수 있습니다. 장소가 있는 항목만 시간순으로 지도에 들어가며, 제목은 표시 이름으로, 날짜·분류·장소·주소·설명은 표시 메모로 옮겨집니다. 연대표의 `rose` 색은 지도의 `red`로 바뀌고 나머지 색은 유지됩니다. Excel은 최대 50MB이며 시트에 붙인 사진도 지도의 사진 합계 12MB 안에서 표시 사진으로 연결합니다
   - **GeoJSON·GPX·KML** — 다른 GIS·GPS·지도 프로그램에서 만든 점, 경로, 영역을 읽습니다(최대 5MB·항목 5,000개). **지도 자료 내보내기**로 현재 표시·경로·영역을 같은 세 형식 중 하나로 저장할 수 있습니다. GeoJSON은 색·메모까지 가장 잘 보존하고, GPX는 GPS·운동 기록 앱과 경로를 주고받을 때, KML은 Google Earth 등과 주고받을 때 알맞습니다
   - **좌표가 없어도 됩니다.** 지도 표의 `주소`(또는 `소재지`·`도로명주소`)나 연대표 표의 장소·주소로 지도 검색을 실행해 좌표를 찾아 표시로 올립니다. 주소도 좌표도 없이 `이름`만 있는 지도 표의 줄은 이름을 장소로 보고 찾습니다. 한 번에 **최대 200줄/항목**까지 찾고, 찾는 동안 **자료 들이기** 버튼이 **그만두기**로 바뀌어 언제든 멈출 수 있습니다. 좌표를 적었는데 값이 잘못된 지도 표의 줄(예: 위도 `999`)은 자료 오류로 보고 검색하지 않고 건너뜁니다
   - 인터넷과 ClassDock 런처가 필요하고, 줄마다 한 번씩 검색하므로 시간이 걸립니다(OpenStreetMap 검색은 제공처 정책상 1초에 한 줄). 카카오 키를 넣어 두면 훨씬 빠르고 국내 주소를 더 정확히 찾습니다
   - **🧾 표로 메모**는 같은 표를 파일로 내려받는 대신 **메모장에 표 블록으로** 바로 넣습니다. 이때 표는 CSV와 달리 **수업에서 바로 읽을 장소 정보**만 추려 `이름 · 업종·카테고리 · 도로명 주소 · 지번 주소 · 전화번호 · 카카오 장소 상세 링크 · 위도 · 경도` 여덟 칸으로 나갑니다(지역 통계용 시도·시군구는 좁은 메모창에서 밀리지 않도록 CSV에만 남습니다). 파일을 내렸다 다시 여는 걸음 없이 메모에서 **복사 · ⬇ CSV · 편집기로 · 변환**으로 이어지고, 표 안에서 이름·메모를 고쳐 쓸 수도 있습니다. 메모 표 한 개에 담기는 줄에는 상한이 있어(199개) 표시가 그보다 많으면 **담긴 수와 빠진 수를 알려 줍니다** — 전부 필요하면 **CSV 내보내기**를 쓰세요
10. **📮 주소 자동**을 켜 두면 새로 찍는 표시의 **이름 칸에 그 자리의 주소**가 채워집니다(직접 적어 둔 이름은 덮지 않습니다). 켜 두지 않아도 표시의 편집 풍선에서 **이 자리 주소 넣기**로 한 표시씩 부를 수 있습니다. 카카오 키가 있으면 도로명 주소와 건물 이름을, 없으면 OpenStreetMap 역검색 결과를 씁니다. 이 설정은 지도 파일이 아니라 이 컴퓨터에 남아 다음에 열 때도 이어집니다
11. **🔎 장소 정보**를 켜 두면(기본으로 켜져 있습니다. ClassDock 런처가 아닌 브라우저로 연 화면에서는 되물을 길이 없어 버튼이 흐리게 보이고, 눌러 보면 까닭을 알려 줍니다) 지도를 클릭한 자리에 **그곳이 어디인지** 풍선으로 뜹니다 — 건물·시설 이름, 갈래(예: 지하철역), 도로명·지번 주소, 전화번호. 카카오 검색을 켰을 때 이름과 갈래·전화까지 나오고, OpenStreetMap만으로도 이름과 주소는 나옵니다. 풍선의 **📍 표시로 넣기**를 눌러야 지도에 남으므로, 눌러 보기만 해서는 문서가 바뀌지 않습니다
   - 표시 풍선·장소 정보 풍선 또는 지도 우클릭 메뉴의 **🚶 로드뷰**는 그 좌표의 카카오맵 로드뷰를 **전용 창 하나**로 엽니다. 다른 표시의 로드뷰를 누르면 창을 새로 늘리지 않고 이미 연 창을 그 좌표로 바꿔 앞으로 가져옵니다. 창을 직접 닫았다면 다음에 다시 만들며, **추가 API 키는 필요 없고** 인터넷 연결만 필요합니다
   - 배경지도가 그림(타일)이라 **누른 건물을 지도에서 집어내는 것이 아니라 누른 좌표를 되물어** 알아냅니다. 그래서 건물 한가운데를 누를수록 잘 맞고, **확대 15단계 이상**에서만 동작합니다(그보다 멀면 몇 백 미터를 가리켜 엉뚱한 주소가 나옵니다)
   - 이름이 없는 자리에서는 **지하철역**만 한 번 더 찾아봅니다. 그 밖의 시설은 풍선의 **🏫 주변 시설**이나 도구막대 쪽에서 갈래를 골라 찾습니다
   - 카카오 검색으로 이름과 갈래까지 찾은 자리에서는 풍선에 **🗺 카카오맵 상세 보기**가 함께 나와, 영업시간·메뉴·리뷰 같은 상세 페이지를 앱 안에서 바로 펼쳐 봅니다(오른쪽 위 **새 창에서 열기**로 브라우저로 꺼낼 수도 있습니다)
   - 풍선이 떠 있을 때 지도를 누르면 풍선이 닫히고, 한 번 더 누르면 그 자리를 다시 찾습니다. 인터넷과 ClassDock 런처가 필요합니다
12. **🏫 주변 시설**(카카오 검색이 꺼져 있으면 흐리게 보이고, 눌러 보면 켜는 법을 알려 줍니다)은 지금 보고 있는 **지도 가운데를 기준으로 반경 안의 시설**을 한 번에 표시로 넣습니다. 창을 열면 갈래 **18가지가 체크박스로 펼쳐져** 있어 무엇을 찾을 수 있는지 한눈에 보입니다 — 학교, 학원, 어린이집·유치원, 병원, 약국, 지하철역, 주차장, 주유소·충전소, 편의점, 대형마트, 음식점, 카페, 숙박, 중개업소, 공공기관, 은행, 문화시설, 관광명소. 처음에는 아무것도 골라져 있지 않으므로 **찾을 갈래를 체크**하고(하나를 고를 때까지 `찾아서 넣기`는 흐리게 있습니다) 반경(500m~3km)을 고른 뒤 누르면 가까운 곳부터 찾아 넣고, 각 표시의 메모에 **갈래 이름·주소·중심에서의 거리**가 함께 적힙니다. **반경 원도 그리기**를 켜 두면 그 반경이 면적 영역으로 그려져 **원의 넓이**까지 함께 보여 줍니다. 넣은 직후 뜨는 알림의 **되돌리기**를 누르면 방금 넣은 표시와 반경 원만 도로 빠집니다
   - 갈래는 **한 번에 다섯 개까지** 함께 고를 수 있습니다 — "우리 동네에 학교와 병원과 편의점이 각각 몇 곳인가"를 한 번에 볼 수 있습니다. 다섯을 채우면 나머지 갈래는 흐려지고, 이미 고른 것을 풀면 다시 켜집니다
   - **전체 최대** 칸에서 이번에 넣을 **지도 전체의 개수**를 고릅니다(30·45·75·100곳, 기본 75곳). 고른 수는 갈래끼리 고르게 나뉘어, 갈래 이름 밑에 `3/5갈래 · 갈래당 약 25곳`처럼 지금 얼마씩 들어갈지가 미리 보입니다. 가까운 차례로 오므로 갈래당 몫은 곧 **가장 가까운 그만큼씩**이라는 뜻이고, 카카오가 한 갈래에 내주는 45곳을 넘겨 나뉘지는 않습니다
   - 갈래 이름 옆의 **색 점**이 지도에 들어갈 색입니다. 갈래마다 정해진 색이 있지만 같이 고른 갈래끼리 색이 겹치면(학교·학원은 둘 다 파랑) **다른 색으로 자동으로 비껴 주어** 지도에서 갈래가 구분됩니다
   - 갈래를 여러 개 고르면 반경 원 이름은 `주변 시설 1km`가 되고, 알림도 `3갈래 32곳을 …`처럼 뜹니다. 갈래 하나만 고르면 예전처럼 그 갈래 이름이 그대로 붙습니다
   - 주변 시설 표시의 풍선에서 **🗺 카카오맵 상세 보기**를 누르면 같은 검색에서 넣은 시설을 한 모달에서 이어서 볼 수 있습니다. 위쪽의 **‹ · ›**와 `현재 / 전체` 순번으로 이전·다음 시설을 넘기며(`←`·`→` 자판도 같고, **마지막에서 다음을 누르면 처음으로 돌아오는 순환 목록**이라 단추가 흐려지지 않습니다), 다른 때 검색해 넣은 시설은 섞이지 않습니다. `Esc`로 닫습니다
   - 이 18가지는 **카카오가 나눠 둔 갈래 전부**이며 새로 만들 수는 없습니다. 목록에 없는 것은 **직접 찾기** 칸에 말을 적어 찾습니다(예: `로또`, `빵집`). 적어 넣으면 갈래 체크박스가 흐려지고 그 말로 같은 반경 안을 훑어 가까운 곳부터 최대 45곳까지 넣고, 표시 이름표와 반경 원 이름도 그 말로 붙습니다. 칸을 비우면 다시 갈래로 찾습니다
13. **🧭 지역 통계**는 표시마다 **시도·시군구**를 채우고 지역별 개수를 셉니다. **지역 채우기**를 누르면 지역이 비어 있는 표시만 좌표로 물어 채우고(진행 중에는 같은 버튼이 **그만두기**), 기준을 **시군구/시도**로 바꿔 볼 수 있습니다. **칠판으로 차트**를 누르면 그 표를 **새 화이트보드의 자료 차트**로 보냅니다 — 그림이 아니라 도구상자가 만드는 것과 같은 차트라 칠판에서 다시 고치고 되돌릴 수 있습니다. 채운 지역은 `.map` 파일과 CSV 내보내기에도 함께 담깁니다
14. 찍어 둔 핀은 **끌어서 옮기고**, 다시 누르면 풍선이 열려 고칠 수 있습니다. 풍선의 **표시 지우기**로 뺍니다
    - 풍선의 **📷 사진 넣기**로 그 자리에서 찍은 **사진 한 장을 지도 파일 안에 담습니다**(답사·관찰 기록). 사진은 긴 변 1280px·JPEG로 줄여 담고 한 장 약 660KB, 한 지도에 모두 합해 12MB까지입니다. 사진이 붙은 핀에는 파란 점이 생기고, 사진을 누르면 크게 볼 수 있습니다. **인터넷 없이도 보이고**, 지도 파일 하나만 건네면 사진까지 함께 갑니다(그만큼 파일이 커집니다)
15. **🧾 표시 목록**을 누르면 지도 오른쪽에 표시 목록이 펼쳐집니다. 표시가 수십·수백 개일 때 지도 위에서 하나를 찾는 대신 목록에서 다룹니다
    - **이름으로 찾기** — 위쪽 칸에 글자를 적으면 이름·메모·시도·시군구에 그 글자가 든 표시만 남습니다
    - **줄을 누르면 그 자리로** 지도가 옮겨 가며 편집 풍선이 열립니다. 줄 오른쪽 `✕`는 그 표시만 지웁니다(되돌리기로 살릴 수 있습니다)
    - **묶음별로 감추기** — 표시가 들어온 갈래(직접 찍은 표시 / 주변 시설)가 둘 이상이면 위에 묶음 단추가 나옵니다. 누르면 그 묶음이 지도에서 잠시 사라지고, 그대로 칠판·PNG·인쇄로 보내면 **감춘 표시는 그림에도 들어가지 않습니다**. 감춘 것은 지워진 것이 아니라 보기 방식이므로 파일에는 그대로 남습니다
    - 한 번에 300줄까지 그리고, 아래에 `보임 · 모두` 개수를 적어 둡니다. 목록을 펴 둔 상태는 이 컴퓨터에 남아 다음에 열 때도 이어집니다
    - 줄의 **▲▼로 순서를 바꿉니다.** 이 순서가 곧 **발표 순서**이고, 지도 파일에 그대로 저장됩니다
16. **🎬 발표 모드**는 표시를 **목록 순서대로 하나씩** 보여 줍니다. 지도가 그 자리로 옮겨 가고 아래에 **이름·메모·사진**이 큰 카드로 뜹니다 — 역사 진격로, 답사 코스, 실크로드처럼 "순서가 곧 이야기"인 수업 자료가 지도 파일 하나로 발표 자료가 됩니다
    - `→`·`Space`로 다음, `←`로 이전, `Esc`(또는 **끝내기**)로 마칩니다. 발표 중에는 도구막대와 표시 목록을 감추고, 끝내면 **발표 전에 보던 자리로 돌아옵니다**
    - 목록에서 **감춰 둔 묶음은 발표에서도 빠집니다.** 발표는 보기만 하는 일이라 문서를 고친 것으로 보지 않습니다
17. **🎯 지도 문제**는 찍어 둔 표시로 **"여기가 어디?" 위치 찾기 문제(.task)** 를 만듭니다 — 과제 패키지와 같은 파일 형식이라 배포·제출·검수 흐름을 그대로 씁니다
    - 표시 하나가 문제 하나입니다. 창에서 **낼 문제를 고르고 문제 글을 고치며**(기본값은 표시 이름), **허용 오차**(100m~5km)를 정합니다. 지금 보고 있는 배경지도·자리·격자가 문제에 함께 담기고, 내 지도 이미지도 넣을 수 있습니다
    - 학생은 `.task` 파일을 열어 **지도를 눌러 답합니다**(답은 끌어서 고칠 수 있고 `←`·`→`로 문제를 넘깁니다). **✓ 채점**은 정답 자리에서 허용 오차 안이면 정답으로 세고, 문제마다 얼마나 빗나갔는지 알려 줍니다(정답 자리는 보여 주지 않습니다). **📤 제출본 내보내기**로 `.taskdone`을 만듭니다
    - 학생 화면에는 편집 도구가 나오지 않고, **학생이 찍은 답은 지도 문서에 적히지 않습니다**
    - 선생님은 제출본을 열어 **학생이 찍은 좌표와 빗나간 거리**를 보고, 원본 `.task`를 연 상태에서 **🔁 재채점**(거리 계산이라 실행 없이 즉시)으로 확인합니다. **제출본 일괄 검수**의 전체 재채점·성적 CSV도 그대로 됩니다
    - 정직한 한계: 정답 좌표는 문제 파일 안에 들어 있어 파일을 열어 보면 보입니다(숨김 테스트와 같습니다). 확실한 기준은 선생님 쪽 재채점입니다
18. **🧹 지우기**는 한꺼번에 정리하는 길입니다. **주변 시설로 넣은 것만**(직접 찍은 표시는 그대로 두고 주변 시설이 넣은 표시·반경 원만) 또는 **모두 지우기** 중에서 고릅니다. 지운 뒤에는 되돌릴 수 없으니 확인 창에서 개수를 보고 고르세요
19. **↶ 되돌리기 · ↷ 다시 실행**(`Ctrl+Z` / `Ctrl+Shift+Z`·`Ctrl+Y`)으로 표시 추가·이동·삭제, 거리선·면적, 이름·색·메모·사진 변경, 발표 순서 바꾸기, 자료 들이기, 주변 시설 넣기, 위경도 격자·이름 보이기·표시 잇기·길찾기 설정과 켜고 끄기, 🧹 지우기까지 한 단계씩 되돌립니다. 이름 칸이나 편집 풍선 안에서는 **글자 되돌리기**가 먼저이고, 지도를 움직이거나 확대한 것은 단계로 쌓이지 않습니다. 주소를 좌표로 바꾸는 일처럼 여러 줄이 몇 분에 걸쳐 들어오는 작업은 **통째로 한 단계**입니다. 되돌리기 기록은 문서를 닫으면 사라집니다
20. `Ctrl+S`로 저장하면 **`.map` 파일 하나**가 만들어집니다. 중심 좌표·확대 단계·표시(지역·사진·발표 순서 포함)·거리선·면적 영역·위경도 격자와 이름 보이기·표시 잇기·길찾기 켜짐과 상세 설정, 사용자 지도 이미지가 함께 들어갑니다. 이미지를 넣은 지도는 그만큼 파일이 커질 수 있습니다
    - **아직 저장하지 않았어도** 편집 내용은 잠시 뒤 작업공간에 자동으로 담겨, 실수로 새로고침하거나 창을 닫아도 **다음에 열 때 그 지도 탭이 그대로 돌아옵니다.** 표시를 찍기 전의 빈 새 지도도 마찬가지입니다. 설정의 **자동 저장·복원**을 끄면 이 사본도 남기지 않습니다(그때는 새로고침 전에 확인창만 뜹니다)
21. **🖊️ 칠판으로**를 누르면 지금 보이는 지도가 **그림으로 굳어 새 화이트보드에 올라갑니다.** 표시 이름·거리·면적과 출처가 그림에 함께 새겨지고, 확대·이동 단추와 편집 풍선은 빠집니다. 칠판에서는 그 위에 바로 판서할 수 있고, **지도 문서는 그대로 남아** 계속 고칠 수 있습니다. 같은 지도를 두 번 보내면 서로 다른 칠판이 열립니다
22. **📋 메모로**를 누르면 지금 보이는 지도가 **그림과 함께 메모장의 그림 블록**으로 들어갑니다. 칠판으로 보낼 때와 같은 그림(표시 이름·거리·면적·출처가 새겨진)이지만, 그 안에 **지도 자체가 함께 담겨** 메모의 **✏️ 지도로**를 누르면 새 탭에서 다시 편집할 수 있습니다. 고친 뒤 다시 **📋 메모로**를 누르면 새 블록이 생기지 않고 **그 블록이 제자리에서 바뀝니다**(화이트보드·악보와 같은 방식). 사용자 지도 이미지를 넣어 지도가 아주 큰 경우에는 그림만 들어가고 되열기는 되지 않는다고 알려 줍니다. **앱을 껐다 켜도** 자동 복원으로 되살아난 지도는 같은 블록으로 돌아갑니다(파일 이름을 바꾸면 연결이 끊겨 다음에는 새 블록이 생깁니다). 그 지도 탭이 이미 열려 있는 채로 메모의 **✏️ 지도로**를 누르면 새 탭 대신 그 탭으로 가되 **메모 그림과 같은 자리로 옮겨** 줍니다 — 그 탭에서 표시를 더 찍어 그림과 내용이 달라졌다면 **메모 그림으로 열기 / 열린 탭 보기**를 물어봅니다
23. **📷 PNG 저장**은 지금 보이는 지도를 **그림 파일 하나로 내려받습니다**(파일 이름은 지도 제목). **🖨️ 인쇄**는 같은 그림을 종이로 보냅니다 — 머리글의 인쇄 단추와 `Ctrl+P`도 같은 길로 갑니다. 화면을 그대로 찍으면 도구막대만 나오므로, 칠판으로 보낼 때와 같은 그림(표시 이름·거리·면적·축척·방위·격자·출처가 새겨진)을 만들어 그것만 찍습니다. 배경지도가 다 뜬 뒤에 누르면 빈 칸 없이 나옵니다
24. 도구가 많아 지도가 좁게 느껴지면 위쪽 **▤ 도구 숨기기**(자판 `H`)로 **편집 도구 줄**을 접어 지도를 넓게 볼 수 있습니다. 제목·검색·되돌리기·저장이 있는 맨 윗줄은 남아서 **▤ 도구 보이기**로 언제든 다시 펴며, 접은 동안에도 지도 우클릭 메뉴에 같은 기능이 있습니다. 접은 상태는 **모든 지도에 함께 적용되어 기억**됩니다. **⛶ 전체화면**(헤더 ⛶ 또는 `Ctrl+K` → "문서 영역 전체화면")에서는 **맨 윗줄까지 접혀 지도만 남습니다** — `H`나 우클릭 메뉴로 도구를 다시 부를 수 있고, 전체화면을 나가면 들어가기 전 상태로 돌아옵니다

자주 쓰지 않는 버튼은 **⚙ 설정 → 도구 → 지도 탭**에서 왼쪽 **비노출**과 오른쪽 **노출** 목록 사이로 옮깁니다. 저장·되돌리기·다시 실행·도구 보이기는 항상 남고, 저장하면 열려 있는 지도에도 바로 적용됩니다. 숨긴 기능도 지도 우클릭 메뉴에서는 계속 사용할 수 있습니다.

25. EXE로 지도를 보면 **화면에 실제로 표시된 배경이 자동으로 보관**됩니다. 다시 방문한 지역은 받아 둔 타일 범위에서 인터넷 없이 열립니다. **🗂️ 오프라인 지도**에서 지금까지 받아 둔 양을 확인하고 **비울** 수 있습니다. 공개 지도 서버 정책에 따라 보지 않은 지역이나 더 자세한 확대 단계를 미리 일괄 다운로드하지는 않습니다

> **인터넷이 필요한 곳은 배경지도뿐입니다.** 연결이 없으면 배경만 비어 보이고 표시·메모·저장은 그대로 동작합니다. 전에 실제로 보아 자동 보관된 지역이라면 배경도 나옵니다.
> **내 지도 이미지**는 인터넷 없이 항상 표시됩니다. 다만 이미지를 넣을 때 현재 화면의 위·경도 범위에 맞춰 놓는 방식이므로, 평면도처럼 실제 지도 축척과 다른 그림에서 표시되는 거리·면적 값은 참고용입니다.
> 배경지도는 **종류마다 캐시가 따로**입니다. 전에 일반 지도로 본 지역이라도 수업에서 처음 위성으로 바꾸면 그 배경은 비어 보일 수 있습니다.
> 지도를 조금 움직였다고 **저장 안 됨(●)** 이 켜지지는 않습니다. 표시·이름·배경지도를 바꿀 때만 켜지며, 저장할 때는 그 순간 보고 있던 자리가 함께 담깁니다.

> 장소 이름 검색에는 **인터넷과 ClassDock 런처가 필요합니다**(받아 둔 지역이라도 검색은 인터넷을 씁니다). 인터넷이 끊기면 지도 위에 **새 지역은 표시할 수 없고 이전에 저장된 지역만 볼 수 있다**는 안내가 나타납니다. 연결이 돌아오면 현재 지도를 자동으로 다시 요청합니다. OpenStreetMap 검색은 제공처 정책상 글자를 칠 때마다가 아니라 **검색** 또는 `Enter`를 눌렀을 때만 찾고, 잇단 검색 사이에는 1초를 둡니다. HTML을 직접 연 화면에서도 좌표 이동은 그대로 쓸 수 있습니다.

> 배경지도는 **OpenStreetMap**(일반·지형)·**CARTO**(흑백)·**Esri**(위성)에서 받아 옵니다. 장소 이름 검색은 기본적으로 **Nominatim**(OpenStreetMap)을 쓰며, 설정하면 **카카오 로컬 REST API**를 먼저 사용합니다. 만들어진 그림에 출처가 함께 적히는 것은 배경지도의 이용 조건입니다.

### ⏳ 연대표 (.timeline) — 사건을 시간순으로 만들고 발표하기

역사·사회·국어·과학사처럼 **사건의 앞뒤 관계가 중요한 수업 자료**를 만들거나, 날짜·시각·장소를 시간순으로 정리한 **여행 일정**을 계획합니다. 인터넷이 없어도 만들고 열 수 있으며, 사진까지 `.timeline` JSON 파일 하나에 들어갑니다.

1. 사이드바 **새로 만들기 → 새 연대표(.timeline)** 를 누릅니다. 폴더를 원본 저장으로 열었다면 폴더 우클릭 → **새 연대표**로 그 폴더 안에 바로 만들 수도 있습니다.
2. 제목 옆 **일반 연대표 / 여행 일정**에서 문서 용도를 고릅니다. 여행 일정을 고르면 사건·분류·유적지 같은 화면 용어가 일정·유형·장소로 바뀌며, 저장한 문서를 다시 열어도 용도가 유지됩니다.
3. 위쪽 **＋ 사건** 또는 **＋ 일정**을 눌러 제목과 시작 날짜를 적습니다.
   - 연도: `1945`
   - 연월: `1945-08`
   - 날짜: `1945-08-15`
   - 날짜와 시각: `2026-08-21 09:30` (`YYYY-MM-DD HH:mm`, 24시간제)
   - 기원전: `기원전 300`, `BC 300`, `BCE 300`
   - 대략적인 시기: `약 1592`, `경 1592`, `~1592`
4. 여러 해에 걸친 사건이나 일정은 **종료**도 적습니다. 종료는 시작보다 빠를 수 없습니다.
5. 일반 연대표의 **분류**에는 `정치`, `문화`, `과학`, `문학`처럼 적습니다. 여행 일정의 **유형**에는 `이동`, `관광`, `식사`, `숙박`이 입력 후보로 나오며 다른 유형도 자유롭게 적을 수 있습니다. 색을 함께 고르면 카드와 시간축을 갈래별로 구분하기 쉽습니다.
6. 사건과 관련된 **유적지·유적지 주소** 또는 여행 일정의 **장소·장소 주소**를 적을 수 있습니다. 카드나 발표 화면의 `📍 장소 · 주소`를 누르면 열어 둔 최근 지도 탭(없으면 새 지도)으로 이동해 주소를 검색합니다. 주소가 없으면 장소명으로 찾습니다. 장소 검색에는 인터넷과 ClassDock 런처가 필요합니다.
7. 설명 또는 메모는 최대 4,000자까지 적을 수 있습니다. **사진 넣기**는 PNG·JPG·WebP를 긴 변 1280px JPEG로 줄여 파일 안에 담습니다. 사진 한 장은 약 660KB, 한 연대표의 사진 합계는 40MB까지입니다(사진 100장 안팎). 외부 이미지 주소는 받지 않아 인터넷이 끊겨도 그대로 보입니다.
8. 사건·일정 카드는 **두 번 누르거나 키보드로 선택한 뒤 `Enter`**를 누르면 다시 고칩니다. 카드·시간축 점·개요 점·오른쪽 목록 항목을 **마우스 오른쪽 버튼으로 누르면** 수정, 같은 날짜·시각 안에서 먼저/나중 표시, 지도에서 장소 찾기, 삭제를 바로 실행할 수 있습니다(장소가 없는 항목에는 지도 메뉴가 나오지 않습니다). 선택한 카드는 `Delete`로 지울 수 있으며 지우기 전 확인합니다. 시작 날짜·시각이 같은 항목이 여러 개라면 우클릭 메뉴나 도구막대의 `↑ / ↓` 또는 `Alt+↑ / Alt+↓`로 그 안의 순서를 바꿉니다. 서로 다른 시각의 항목은 실제 시간순을 유지합니다.
9. 보기 방식은 두 가지입니다.
   - **읽기 좋게 균등**: 사건 간격을 고르게 벌려 글을 읽기 쉽게 표시합니다.
   - **시간 간격대로**: 실제 시간 차이에 비례해 배치합니다. 같은 시기에 몰린 사건은 누를 수 있도록 최소 간격을 둡니다.
10. `− / 100% / ＋` 또는 `Ctrl`+마우스 휠로 확대·축소합니다. 카드·글자·사진·시간축이 모두 함께 바뀌며, `Ctrl`+마우스 휠은 포인터가 놓인 지점을 중심으로 확대합니다. 카드·날짜 점·시간축이 아닌 **빈 여백에서는 손바닥 커서**가 나타나며, 마우스 왼쪽 버튼으로 끌어 긴 연대표를 가로·세로로 옮길 수 있습니다. 배율과 화면 위치는 보기 상태라 문서 내용과 되돌리기에 영향을 주지 않습니다.
11. 사건이나 일정이 많을 때는 **▤ 개요**를 누릅니다. 모든 항목이 한 화면의 작은 점으로 표시되며, 점을 누르면 상세 보기로 돌아가 해당 카드를 가운데로 이동합니다. 개요에서는 항목 수와 실제 시간 분포를 빠르게 훑을 수 있습니다.
12. **☷ 목록**에서 일반 연대표는 제목·분류·유적지·주소·설명을, 여행 일정은 제목·유형·장소·메모를 검색합니다. 항목을 누르면 시간축의 해당 카드로 이동합니다. 사이드바의 파일 내용 검색도 사진 데이터는 빼고 글자만 찾습니다.
13. **표 들이기**는 **CSV와 엑셀(`.xlsx`)** 을 받아 일반 연대표의 `시작, 종료, 제목, 분류, 유적지, 유적지 주소, 이미지 파일명, 설명, 색상` 또는 여행 일정의 `시작, 종료, 제목, 유형, 장소, 장소 주소, 이미지 파일명, 메모, 색상` 열을 읽습니다. 최소한 **시작과 제목** 열이 필요하고, 날짜·시각이나 제목이 잘못된 줄은 제외한 수를 알려 줍니다. `이미지 파일명`에는 `광복.jpg` 또는 `사진/광복.jpg`처럼 적습니다(`이미지 파일`, `사진 파일명`도 인식). 항목을 가져온 뒤 **이미지 폴더**를 눌러 사진이 든 폴더를 고르면 PNG·JPG·WebP를 파일명 또는 하위 경로로 찾아 자동 압축해 연결합니다. 같은 파일명이 여러 하위 폴더에 있으면 잘못 연결하지 않도록 경로를 적은 항목만 붙이며, 연결된 사진은 이후 `.timeline` 파일 안에 저장됩니다. **엑셀 파일은 시트에 붙여 넣은 사진까지 함께 가져옵니다** — 사진이 놓인 칸의 줄을 그 줄의 항목으로 보고 자동으로 줄여 넣으므로, 이미지 파일명 열도 이미지 폴더 고르기도 필요 없습니다(한 줄에 여러 장이면 첫 장만 씁니다. 엑셀 365의 ‘셀에 배치’ 이미지와 `IMAGE()` 함수 그림은 아직 읽지 못하니 셀 위에 얹는 일반 삽입을 쓰세요). 날짜 칸은 엑셀 날짜 서식·수식 결과·서식 글자 모두 읽습니다. **표 내보내기**에서는 가벼운 UTF-8 **CSV** 또는 **Excel(`.xlsx`)** 을 고릅니다. Excel은 선택한 문서 용도에 맞는 열 이름을 쓰고, 첫 행 고정·필터·열 너비·설명 줄바꿈을 적용하며 사진을 같은 행의 `사진` 열에 넣습니다. 기원전·대략적인 시기·날짜와 시각은 바뀌지 않도록 원문 글자로 저장합니다.
14. 연대표의 **카드를 한 번 누르면** 그 항목부터 발표 화면으로 크게 보고, **두 번 누르면** 기존처럼 수정 화면을 엽니다. 일반 연대표의 **▶ 발표** 또는 여행계획의 **▶ 일정 보기**는 선택한 항목부터 나머지 항목을 시간순으로 한 장씩 크게 보여 줍니다. **날짜·시각은 카드 왼쪽 맨 위에 작게** 두고, 제목·설명·사진이 화면을 넓게 씁니다. 사진이 없으면 제목을 키운 집중형 화면이 되고, 있으면 사진과 내용을 나란히 보여 주며 작은 화면에서는 위아래로 바뀝니다. **사진은 카드 높이에 맞춰 자동으로 줄어들어** 큰 사진을 넣어도 스크롤이 생기지 않습니다. 설명이 길면 글 영역만 스크롤되며 스크롤 막대는 감추고 아래쪽에 작은 `⌄` 표시가 나타납니다(끝까지 내리면 사라집니다). **유적지·장소는 오른쪽 아래 지도 단추**로 들어갑니다. 위쪽 진행 막대로 전체 순서를 확인하고, `→`·`Space`로 다음, `←`로 이전, `Esc`로 끝냅니다.
15. **🖨 인쇄**는 항목을 세로 목록으로 정리해 인쇄하며, 인쇄 창에서 PDF로 저장할 수 있습니다.
16. `Ctrl+Z` / `Ctrl+Y`(`Ctrl+Shift+Z`)로 항목 추가·수정·삭제, 순서 변경, 표 들이기, 제목·문서 용도·보기 방식 변경을 되돌립니다.
17. `Ctrl+S`로 저장합니다. 아직 저장하지 않은 새 연대표와 편집 중인 내용도 잠시 뒤 작업공간에 자동으로 담겨, 갑자기 종료해도 다음 실행에서 복원됩니다.

> 한 연대표에는 사건을 최대 1,000개까지 넣을 수 있습니다. 시간 비례 보기에서 수천 년 간격과 하루 간격이 함께 있으면 가까운 사건들이 붙을 수 있으므로, 발표 자료는 **균등 보기**가 읽기 편합니다.

### 🕸️ 개념 관계도 (.concept) — 개념을 연결해 이해하기

역사 사건의 인과관계, 과학 개념의 포함 관계, 작품의 인물 관계처럼 **무엇이 왜 연결되는지** 카드와 연결선으로 정리합니다. 사진을 포함해도 `.concept` JSON 파일 하나로 저장되어 인터넷 없이 열 수 있습니다.

1. **새로 만들기 → 새 개념 관계도(.concept)** 를 누르고 위쪽 **＋ 개념**으로 카드를 만듭니다.
2. 개념 이름·분류·색·설명과 사진을 넣습니다. **카드를 한 번 누르면** 발표 화면처럼 해당 카드와 직접 연결된 관계를 크게 봅니다. `Esc`나 **닫기**로 돌아오며, 카드를 두 번 누르거나 카드의 `⋯`를 누르면 다시 고칩니다.
3. **＋ 관계**에서 시작·도착 개념과 `원인 → 결과`, `상위 → 하위`, `비교`, `근거·뒷받침`, `관련` 중 하나를 고릅니다. 연결선에 `때문에`, `포함한다` 같은 말을 따로 표시할 수도 있습니다.
4. **표·개요**는 이미 엑셀 표나 개요 글로 적어 둔 것을 카드 하나하나 만들지 않고 그대로 관계도로 바꿉니다. **표 파일 고르기**는 CSV·엑셀(`.xlsx`)의 첫 시트를 읽으며, 첫 줄의 열 이름에서 `개념`(또는 `출발`·`상위`)과 `대상`(또는 `도착`·`하위`)을 알아보고 `관계`·`연결선`·`분류`·`설명`·`색` 열이 있으면 함께 씁니다. 대상 열이 없는 표는 한 줄이 카드 하나이고 `상위` 열이 그 카드의 부모가 됩니다. 관계 열이 없으면 열 이름으로 짐작해 `원인`/`결과` 표는 인과로, `상위`/`하위` 표는 포함 관계로 잇습니다. 쌍반점(`;`)·탭으로 나뉜 표와 CP949로 저장된 한글 엑셀 CSV도 그대로 읽습니다. **개요 글** 칸은 들여쓰기 한 단계가 `상위 → 하위` 한 단계이고 한 줄을 `제목 | 설명 | 분류`로 적을 수 있어, 회의록이나 목차를 붙여 넣고 **이 글로 만들기**를 누르면 관계도가 됩니다(글머리 기호·번호는 알아서 뗍니다). 창을 열면 지금 관계도의 개요가 이 칸에 들어 있어 **복사**·**.txt 저장**으로 가져갈 수 있고, **관계 CSV 저장**은 관계 하나가 한 줄인 표로 내보내 엑셀에서 고친 뒤 그대로 다시 들일 수 있습니다(관계가 없는 카드도 빠지지 않습니다). **넣는 방법**을 `더하기`로 두면 이름이 같은 카드는 새로 만들지 않고 그대로 두므로 표를 고쳐 여러 번 들여도 카드가 두 벌이 되지 않고, `바꾸기`는 지금 카드를 지우고 새로 만듭니다(`Ctrl+Z`로 되돌아옵니다). 넣은 뒤에는 고른 배치로 자동 정렬합니다.
5. 카드를 끌어 자유롭게 배치합니다. 아래쪽이나 오른쪽 끝으로 옮기면 캔버스가 자동으로 늘어나며, 화면 가장자리까지 끌면 그 방향으로 화면이 따라 움직입니다. **자동 정렬 ▾**에서는 `위→아래 가계도`, `방사형`, `원형`, `왼쪽→오른쪽`, `격자형` 중 배치를 고르고 간격을 좁게·보통·넓게 조절할 수 있습니다. **정렬 뒤 화면에 맞춤**을 켜면 전체 관계도가 보이도록 배율도 조절합니다. 방사형은 선택한 카드가 있으면 그 카드를 중심으로, 없으면 들어오는 방향 관계가 없는 시작 카드를 중심으로 펼칩니다.
6. `Ctrl`을 누른 채 마우스 휠을 돌리거나 터치패드를 오므리고 펼치면 **커서가 가리키는 곳을 중심으로 35~200% 확대·축소**됩니다. 터치 화면에서는 **두 손가락을 오므리고 펼쳐** 같은 방식으로 확대·축소하며, 두 손가락을 함께 움직이면 확대하면서 옮길 수도 있습니다. 위쪽 `− / 배율 / ＋` 버튼으로도 조절할 수 있고, 가운데 배율 버튼은 **100%로 되돌리면서 관계도를 화면 가운데로** 옮깁니다.
7. 카드·연결선이 아닌 **빈 여백을 끌면 손바닥 커서**가 나타나고 관계도 전체가 따라옵니다. 관계도가 화면보다 작아도 **가운데나 오른쪽 등 원하는 자리로 자유롭게 옮길 수 있습니다.** 터치 화면에서는 한 손가락으로 끌면 됩니다. 마우스 휠은 위아래로, `Shift`+휠은 좌우로 옮기며, 관계도를 한 번 누른 뒤에는 방향키(`Shift`+방향키는 크게)로도 움직입니다. 너무 멀리 옮겨 관계도를 놓쳤다면 `Home` 키를 눌러 **전체가 보이도록 화면에 맞춤**하거나 가운데 배율 버튼을 누르면 됩니다. 배율과 화면 위치는 보기 상태라 문서 내용과 되돌리기에 영향을 주지 않습니다.
8. 카드 왼쪽 위 숫자는 발표 순서입니다. **① 순서**에서 목록을 끌거나 `↑`·`↓`를 눌러 바꿉니다. **관계 따라 자동 순서**는 원인→결과·상위→하위·근거 방향을 따라 단계별로 정하고, **화면 위치순**은 왼쪽에서 오른쪽·위에서 아래 순서로 정합니다.
9. 위쪽 **효과**에서 `페이드`, `확대`, `슬라이드`, `연결선 그리기`, `효과 없음` 중 하나를 고릅니다. 선택한 효과와 발표 순서는 `.concept` 파일에 함께 저장됩니다. Windows의 동작 줄이기 설정이 켜진 PC에서는 애니메이션을 자동으로 생략합니다.
10. **전개 발표**는 빈 관계도에서 시작합니다. `Space` 또는 `→`를 누를 때마다 다음 카드와 이미 나온 카드 사이의 연결선이 나타납니다. `Shift+Space` 또는 `←`는 한 단계 전으로, `Home`·`End`는 처음·끝으로 이동하며 `Esc`로 끝냅니다. 아래쪽 `− / 배율 / ＋` 또는 `Ctrl`+마우스 휠로 35~200% 확대·축소하며, 가운데 배율 버튼과 `Ctrl+0`은 100%로 되돌립니다. 전개 중 나온 카드를 누르면 해당 카드를 크게 볼 수 있습니다.
11. **▶ 큰 카드**는 선택한 카드부터 개념을 한 장씩 크게 보여 주고, 현재 카드와 직접 연결된 관계를 아래에 함께 표시합니다. `←`·`→`·`Space`로 이동하고 `Esc`로 끝냅니다.
12. 검색창은 개념 이름·분류·설명을 찾고, 앱의 통합 파일 검색도 사진 데이터는 빼고 카드와 관계의 글자만 검색합니다.
13. `Ctrl+Z` / `Ctrl+Y`로 카드·관계·배치·발표 설정 변경을 되돌리고, `Ctrl+S`로 저장합니다. 미저장 내용은 작업공간 복구본에도 들어갑니다.

> 개념은 최대 300개, 관계는 최대 800개까지 넣을 수 있습니다. 연결선을 두 번 누르면 관계를 고치거나 삭제할 수 있습니다.

### 🧠 암기 카드 (.study) — 틀린 것만 다시 복습하기

학생이 직접 질문·정답·해설을 모으고 반복해서 확인하는 오프라인 암기장입니다. 단어장, 공식 암기, 오답 노트처럼 과목과 관계없이 사용할 수 있습니다.

1. **새로 만들기 → 새 암기 카드(.study)** 를 누르고 **＋ 카드**로 질문과 정답을 적습니다. 태그·해설·사진도 넣을 수 있습니다.
2. 카드 유형을 **문답** 또는 **빈칸**으로 고릅니다. 빈칸 카드는 `물의 화학식은 {{H2O}}이다`처럼 가릴 말을 `{{`와 `}}`로 감싸면 학습할 때 `＿＿＿＿`로 보입니다. 빈칸이 여러 개면 정답을 한꺼번에 보여 줍니다.
3. 위쪽 조건에서 **전체 / 오늘 복습 / 틀린 카드 / 헷갈린 카드 / 새 카드**를 고르고 필요하면 **순서 섞기**를 켠 뒤 **▶ 학습 시작**을 누릅니다.
4. 질문을 생각한 뒤 **정답 보기**를 누르고 `몰라요`, `헷갈려요`, `알아요`로 스스로 평가합니다. 키보드에서는 `Space` 또는 `Enter`로 정답을 열고 숫자 `1`·`2`·`3`으로 평가합니다.
5. `몰라요` 카드는 오늘 다시 볼 카드, `헷갈려요`는 다음 날, `알아요`는 3일→7일→14일→30일 순으로 간격이 늘어납니다. 결과와 다음 복습일은 `.study` 파일 안에 함께 저장됩니다.
6. **CSV 들이기**는 `질문, 정답` 열을 필수로 읽고 `설명, 태그, 유형` 열도 받을 수 있습니다. **CSV 내보내기**로 다른 표 프로그램과 왕복할 수 있으며 줄바꿈·쉼표가 든 내용도 보존합니다.
7. 목록 검색과 앱 통합 검색은 질문·정답·해설·태그를 찾습니다. `Ctrl+Z` / `Ctrl+Y`로 편집과 학습 평가를 되돌리고 `Ctrl+S`로 저장합니다.

> 한 암기장에는 카드를 최대 1,000장까지 넣을 수 있습니다. 자기평가는 점수를 매기는 시험이 아니라 다음에 무엇을 다시 볼지 정하는 기록입니다.

### 파일 비교 (diff) — 뭐가 달라졌는지 보기

1. 비교할 파일을 **둘 다 열어 둔 뒤** `Ctrl+K` → **"두 파일 비교"** 로 두 파일을 고릅니다
2. 지금 고친 내용이 저장된 원본과 어떻게 다른지만 보고 싶다면 `Ctrl+K` → **"저장본과 비교"**
3. 결과는 새 탭에 열리며, **나란히 보기 / 한 줄로 보기**를 전환할 수 있습니다
4. 바뀐 줄 안에서 **실제로 달라진 글자만** 따로 강조되고, 똑같은 줄이 길게 이어지는 구간은 접어서 차이만 보여 줍니다
5. **공백 무시** 옵션을 켜면 들여쓰기·줄 끝 공백만 다른 줄은 같은 줄로 봅니다

> 과제 패키지(`.task`)에서는 학생의 제출 코드와 시작 코드를 바로 비교할 수 있습니다.
> 비교 화면은 원본에서 언제든 다시 만드는 화면이라 저장 대상이 아니고, 자동 복원에도 남지 않습니다. 파일 하나가 5MB·5만 줄을 넘으면 비교하지 않습니다.

### 여러 파일 찾아 바꾸기

1. 바꿀 파일들을 열어 둔 상태에서 `Ctrl+K` → **"여러 파일 찾아 바꾸기"**
2. 찾을 말과 바꿀 말을 넣습니다 — **대소문자 구분**, **정규식**(그룹 치환 `$1` 사용 가능)을 지원합니다
3. **미리보기**에 파일별로 바뀔 줄이 뜹니다. 체크를 풀어 원하지 않는 파일은 빼세요
4. **적용**하면 각 파일의 저장 위치로 **대화상자 없이 조용히 저장**됩니다
5. 잘못 바꿨으면 **되돌리기** 한 번으로 방금 바꾼 내용을 전부 복원합니다

> 대상은 **열려 있는 텍스트·코드 문서와 Word(`.docx`)·PowerPoint(`.pptx`)** 입니다. PDF·Excel·한글 같은 보기 전용 문서와 노트북은 제외됩니다.
> 빈 문자열에도 일치하는 정규식(`a*` 등)은 모든 자리에 끼어들어 위험하므로 막습니다.

#### Word(.docx)·PowerPoint(.pptx)를 함께 바꿀 때

Word·PowerPoint 파일은 같은 창에서 **본문 글자**가 함께 바뀝니다. 미리보기는 줄 번호 대신 `문단 12`(Word)·`슬라이드 3`(PowerPoint)처럼 보여 주고, 파일 이름 옆에 **`Word 본문`**·**`슬라이드`** 표시가 붙습니다.

- 바꾼 자리 말고는 **글꼴·색·표·이미지·머리말이 그대로** 남고, **원래 그 파일에 제자리로 저장**됩니다(사본을 만들지 않습니다). 잘못 바꿨으면 **되돌리기**로 원래 파일로 돌아갑니다.
- **머리말·꼬리말·각주(Word)와 발표자 노트(PowerPoint)는 기본적으로 바꾸지 않고**, `머리말 2곳은 바꾸지 않아요`처럼 개수만 알려 줍니다. 함께 바꾸려면 **설정 → 문서 → "머리말·꼬리말·각주·발표자 노트도 함께 바꾸기"** 를 켜세요. 이번 한 번만 다르게 하려면 찾아 바꾸기 창의 **`머리말·꼬리말·노트 포함`** 체크를 쓰면 됩니다.
- **메모(검토 의견)·차트·도형 안 글자는 어느 쪽이든 바꾸지 않습니다.** 슬라이드 마스터·레이아웃의 "제목을 입력하십시오" 같은 빈 자리 안내문과 자동으로 채워지는 슬라이드 번호도 건드리지 않습니다.
- 바꿀 수 없는 문서는 목록에서 사라지지 않고 **이유와 함께 흐리게** 남습니다 — 암호가 걸린 문서, 내용 컨트롤이 연결된 문서, 40MB가 넘는 문서입니다.
- **변경 내용 추적이 켜진 문서**도 기본적으로 건너뜁니다. 이 프로그램이 바꾼 내용은 **Word의 변경 이력에 남지 않기** 때문입니다. 그래도 바꾸려면 **설정 → 문서 → "변경 내용 추적이 켜진 문서도 바꾸기"** 를 켜세요.
- `.pptx`는 PowerPoint 설치 여부와 관계없이 **간이 미리보기로 바로 열립니다.** 정확한 모양이 필요하면 문서 위 **정확한 PDF로 열기**를 누르세요. 정확 변환본은 별도 PDF 탭으로 열리므로 원래 PPTX 탭에서는 슬라이드 찾아 바꾸기를 계속 사용할 수 있습니다. 항상 자동 변환하려면 **설정 → 문서 → "PowerPoint 파일을 열 때 자동으로 정확 변환"** 을 켜세요.
- 저장할 위치를 찾지 못하면(브라우저로만 연 파일 등) **아무것도 바꾸지 않고 건너뜁니다.** Word·PowerPoint 문서는 편집기가 없어서, 화면만 바뀌면 "바뀐 줄 알았는데 파일은 그대로"가 되기 때문입니다.

### 💱 환율 — 고시환율 보기·환전 계산·추이 그래프

`Ctrl+K` → **"환율"** 로 엽니다. **인증키 없이도 바로 쓸 수 있고**, 인증키를 넣으면 한국수출입은행 **고시환율**까지 볼 수 있습니다.

| 출처 | 키 | 나오는 값 |
|---|---|---|
| **ECB 참고환율** (기본) | 필요 없음 | 매매기준율에 해당하는 참고환율 |
| **수출입은행 고시환율** | 무료 인증키 필요 | 매매기준율 + **송금 보낼 때 / 받을 때** |

1. **표** — 통화별 환율이 뜹니다. 위쪽 칸에 `달러`·`USD`처럼 치면 그 통화만 남습니다. **표의 줄을 누르면** 그 통화가 아래 계산기와 추이에 함께 걸립니다
2. **환전 계산** — 금액을 넣으면 바로 바뀝니다. **⇄** 를 누르면 원 → 외화 방향으로 뒤집히며, 고른 통화 칸이 좌우로 옮겨 가 **어느 쪽에서 어느 쪽으로 가는지** 보입니다
3. **추이** — 통화와 기간(7·14·30일)을 고르고 **칠판 그래프로** 를 누르면 새 화이트보드에 선그래프가 그려집니다. 그 위에 바로 판서할 수 있습니다
4. **내보내기** — **복사**(엑셀·한글에 바로 붙여넣기) · **CSV로 저장** · **표 편집기로**(새 탭의 xlsx). 넷 다 **화면에 보이는 대로** 나갑니다 — 거르기 칸에 친 대로 걸러진 줄만 나갑니다

#### 칠판에서 바로 넣기

화이트보드 도구막대의 **환율 버튼**(₩ 동전 그림, 지도 버튼 바로 옆)을 누르면 같은 창이 뜨지만, **새 칠판을 만들지 않고 지금 보고 있는 칠판에** 넣습니다. 아래 버튼이 **표 넣기**·**그래프 넣기**로 바뀝니다.

- 넣은 표와 그래프는 도구상자로 만든 교구와 같은 묶음이라 **크기 조절·좌우 반전·분리·되돌리기**가 그대로 됩니다
- 지금 쓰고 있는 **펜 색**으로 그려지므로 어두운 배경 칠판에서도 보입니다
- 표는 **화면에 보이는 대로** 들어갑니다 — 위 거르기 칸에 `달러`를 쳐 놓고 넣으면 달러만 들어갑니다(통화 29종을 통째로 올리면 글자가 뭉개져 못 읽습니다). 복사·CSV·표 편집기도 같은 규칙입니다

> 환율은 런처가 대신 받아 와야만 되는 기능이라, **이 버튼은 ClassDock.exe로 열었을 때만 나타납니다.** 오프라인 HTML이나 일반 브라우저로 연 칠판에는 아예 보이지 않습니다 — 눌러도 안 되는 버튼을 두지 않으려는 것입니다.

> **일본 엔·인도네시아 루피아는 100단위로 고시**됩니다. 표에는 `100 JPY = 950.12원`처럼 고시된 그대로 나오고, 계산기는 1단위 값으로 계산하므로 어느 쪽으로 넣어도 결과가 맞습니다. 출처를 바꿔도 단위는 그대로라 같은 줄을 같은 뜻으로 읽을 수 있습니다.
> **고시가 없는 날**(주말·공휴일, 그리고 **오전 11시 무렵의 그날 고시 전**)은 가장 가까운 고시일 값을 보여 주며 "고른 날짜는 고시가 없어…"라고 밝힙니다. 오전에 열어 둔 창은 11시가 지난 뒤 **다시 받기**를 누르면 그날 값으로 갱신됩니다.
> **인터넷이 끊겨도** 전에 받아 둔 값을 계속 볼 수 있습니다(런처가 이 PC에 저장해 둡니다). 이때는 "저장해 둔 값입니다"라고 함께 표시합니다.

#### 수출입은행 인증키 넣기

1. 한국수출입은행 홈페이지의 **오픈 API** 안내에서 인증키를 무료로 발급받습니다
2. **설정 → 일반 → 환율** 에서 인증키를 넣고 **키 저장·연결 시험**
3. **"이 Windows 사용자 계정에 암호화하여 기억"** 을 켜면 다음 실행에도 남고, 끄면 프로그램을 닫을 때 사라집니다

> 인증키는 **HTML·브라우저 설정·작업공간 어디에도 저장하지 않고** ClassDock 런처에만 전달합니다(지도 검색의 카카오 키와 같은 규칙). 환율 조회 자체도 **ClassDock.exe로 열었을 때만** 됩니다 — 수출입은행 API가 브라우저에서 직접 부르는 것을 허용하지 않기 때문입니다.
> 수출입은행 API는 **하루 1,000회** 조회 한도가 있습니다. 한 번 받은 값은 이 PC에 저장해 두고 다시 받지 않으므로 수업 중에 한도를 걱정할 일은 거의 없지만, 30일 추이는 하루씩 받아오므로 그만큼 조회가 쌓입니다.

### 텍스트·이미지·노트북 셀 메모

1. 상단 **메모** 또는 `Ctrl+M`으로 메모장 열기
2. 메모가 많아 안이 헷갈리면 **탭에 마우스를 올려** 본문 앞 세 줄 미리보기를 확인
3. 메모가 많아지면 탭바의 **▦ 목록**으로 모든 메모를 카드로 펼쳐 제목·색·수정 시각·본문 앞부분을 한눈에 확인. **전체 내용 보기**를 켜면 모든 메모의 글·이미지·표·노트북 셀을 읽기 전용 세로 흐름으로 이어 확인할 수 있음. 각 카드의 **⤢ 크게 보기**는 해당 카드 하나만 화면 가득 펼치고, 같은 버튼의 **⤡ 이전 크기** 또는 `Esc`로 목록에 복귀. **편집**이나 카드 제목을 누르면 해당 메모 편집 화면으로 이동. 위쪽 검색창에 치면 제목·본문이 맞는 메모만 남으며 일치한 내용이 강조됨(`Enter`는 첫 결과로 이동, `Esc`는 검색어 → 목록 순으로 닫힘). 명령 팔레트(`Ctrl+K`)의 **메모 목록·검색**으로도 바로 열림
4. **+ 글 블록**으로 글 영역을 추가하고 **+ 이미지**로 이미지 파일 삽입. **+ 표**로 표 블록을 추가하며, 표 아래 **복사**(탭 구분 → 엑셀·한글에 붙여넣기) · **⬇ CSV** · **편집기로**(새 탭의 표 편집기) · **변환**(JSON·XML·마크다운 등)으로 표를 바깥으로 꺼낼 수 있음. 표 하나는 **최대 200행 × 20열**이되 **칸 3,000개**를 넘지 못하므로, 열이 많은 표는 행이 그만큼 먼저 막힘(예: 20열이면 150행). 상한에 닿으면 그 까닭을 알려 줌
5. 글·이미지·표·노트북 셀 블록 도구막대의 **⤢**를 누르면 해당 블록 하나만 화면 가득 펼쳐 편집·확인. 같은 자리의 **⤡** 또는 `Esc`로 원래 메모에 복귀
6. 글 영역에 이미지를 `Ctrl+V`로 붙여넣거나 로컬 파일·웹 이미지를 메모장에 드롭
7. 이미지 블록의 `↑` `←` `→` `↓` 버튼으로 설명 글의 위·왼쪽·오른쪽·아래에 즉시 배치
8. **작게/중간/크게/가득**으로 표시 크기를 바꾸고 **블록 ↑/↓** 또는 `⋮⋮` 드래그로 문서 순서 변경
9. 이미지 옆 입력란에 설명 작성. 글·이미지·배치 정보는 이 컴퓨터에 자동 저장
10. 기존 텍스트 메모는 새 형식으로 자동 이전되며, **텍스트 복사**는 이미지 이름을 포함한 읽기 쉬운 텍스트로 복사
11. 블록 도구막대의 **자물쇠 아이콘**을 누르면 읽기 전용으로 바뀌어 내용·배치·이동·삭제를 막음. 잠긴 자물쇠 아이콘을 다시 누르면 편집 가능
12. 이미지 블록의 **📋 복사**(클립보드 → 다른 프로그램에 붙여넣기), **💾 파일로**(EXE는 저장 폴더, 브라우저는 다운로드), **✏️ 편집기로**(이미지 편집 탭으로 열어 자르기·표시 후 '📷 메모로'로 다시 넣기)로 메모 속 이미지를 재사용. 잠긴 블록에서도 사용 가능
    - 화이트보드·악보·지도에서 **메모로** 보낸 그림에는 **✏️ 화이트보드로 · ✏️ 악보로 · ✏️ 지도로** 버튼이 함께 붙습니다. 누르면 그림이 아니라 **원래 문서가 새 탭으로 되살아나고**, 고친 뒤 다시 **메모로**를 누르면 그 블록이 제자리에서 바뀝니다. 같은 블록을 두 탭으로 열지는 않습니다
    - 화이트보드·지도·악보는 이 연결이 **탭 구성과 함께 저장**되어, 앱을 껐다 켜고 자동 복원으로 열린 뒤에도 같은 블록으로 돌아갑니다. 가리키던 블록을 지웠다면 다음에 **메모로**를 누를 때 새 블록이 생깁니다
    - 그 블록과 이어진 지도·악보 탭이 **이미 열려 있으면** 새 탭을 만들지 않고 그 탭으로 갑니다. 내용이 같으면 지도는 **메모 그림과 같은 자리(중심·확대)로 옮겨** 그림 속 그곳이 보입니다. 그 탭을 열어 둔 채 다른 곳을 편집해 **내용이 그림과 달라졌다면** 물어봅니다 — **메모 그림으로 열기**(그 탭의 연결을 끊고 그림 속 지도를 새 탭으로, 열려 있던 지도는 그대로 남습니다) 또는 **열린 탭 보기**
13. 노트북에서 선택한 셀의 메모 아이콘을 누르거나 `⋮⋮` 손잡이를 메모장에 드롭하면 **코드/마크다운/Raw 셀 카드**로 별도 저장
14. 셀 카드의 **노트북에 붙여넣기**를 누르면 현재 노트북의 선택 셀 아래에 삽입. 카드 손잡이를 노트북 셀 사이에 직접 드롭하면 정확한 위치에 삽입
15. 셀 카드는 최대 200개까지 보관하며 코드·마크다운 첨부·일반 메타데이터를 유지하고 실행 결과·실행 번호·필기는 저장하지 않음

> **맞춤법 검사:** 글 블록과 이미지 설명의 **맞춤법** 버튼을 누르면 외부 서버 전송 없이 현재 내용을 검사합니다. 제안 표현을 눌러 교정하고, **이번만 무시**하거나 고유명사를 **사용자 사전에 추가**할 수 있습니다. TXT·Markdown 편집기와 노트북 마크다운 셀에서도 같은 기능을 사용할 수 있으며, 코드 파일은 주석과 문자열만 검사합니다.

> 잠긴 블록이 하나라도 있으면 해당 메모 탭 삭제와 **모두 지우기**도 차단됩니다. 먼저 각 블록의 잠금을 해제하세요.

> 웹 이미지가 사이트 보안 정책(CORS·로그인·`blob:` 주소) 때문에 드롭되지 않으면 웹에서 **이미지 복사** 후 메모장에서 `Ctrl+V`를 사용하세요.

### 이미지 메모

1. 상단 **이미지 메모**를 눌러 창 열기
2. 화면을 캡처한 뒤 `Ctrl+V`로 붙여넣기 — 같은 창에 여러 장을 계속 추가할 수 있음
3. 이미지 메모 창 어디에나 이미지 파일이나 웹 이미지를 드롭 가능. 필요 없는 이미지는 카드 오른쪽 위 `×`로 삭제
4. 기본으로 켜진 **자동 저장**이 붙여넣기·드롭 후 1.4초 뒤 동작
   - EXE: 자동 저장 폴더 아래 `이미지메모/날짜_시간/`에 개별 이미지로 실제 저장
   - 일반 브라우저: IndexedDB에 임시복구본을 저장하고 다음 실행 때 복원
   - 실패한 이미지는 미저장 상태로 남겨 자동 재시도
5. EXE에서는 자동저장 실패 또는 자동저장을 끈 경우에만 **다시 시도/지금 저장** 버튼 표시
   - 일반 브라우저의 **파일로 다운로드**: 한 장은 이미지 파일, 여러 장은 ZIP으로 다운로드
6. EXE를 다시 실행하면 현재 자동 저장 폴더의 최근 이미지 메모를 자동으로 불러옴
   - 저장된 카드의 `×`는 저장된 이미지 파일도 함께 삭제
7. 이미지 카드를 클릭하면 큰 미리보기로 확인
   - 마우스 휠 또는 `−`/`+`로 확대·축소
   - 확대된 이미지를 드래그해 이동
   - **화면 맞춤**, **100%**, 더블클릭 전환과 `Esc` 닫기 지원
8. 카드의 **현재 메모로 보내기**를 누르면 일반 메모의 현재 탭에 같은 이미지를 넣고 설명·배치를 추가할 수 있음

### 참고 문서 + 작업 문서 같이 보기 (분할 작업)

1. 참고할 PDF·코드·표·문서·이미지 등을 열고 상단 **분할 작업** 클릭
2. 사이드바에서 `.py`·노트북·표 등 작업할 파일 선택 → 참고 문서와 작업 화면이 나란히 표시
3. 넓은 화면에서는 분할 경계의 **↕/↔** 버튼으로 좌우·상하 방향을 전환하고, 분할바를 드래그해 방향별 비율을 조정합니다. 가운데 교체 버튼 또는 분할바 더블클릭으로 좌우·위아래 위치를 바꿀 수 있습니다. 좁은 화면에서는 위/아래로 나뉩니다.
4. 각 칸 왼쪽 위에 **역할 칩**(참고·작업 + 파일명)이 떠 있어 어느 쪽이 어느 역할인지 바로 보입니다. 평소엔 반투명하고 마우스를 올리면 선명해집니다.
5. 기본 상태는 기존 학습 화면처럼 양쪽에서 모두 편집할 수 있습니다. 참고 문서를 보기 전용으로 쓰려면 **참고 칩의 잠금** 버튼을 누르세요. 잠금 중에도 보기·스크롤·텍스트 및 표 선택·복사가 가능합니다.
6. 참고와 작업을 서로 바꾸려면 두 칸 사이 가운데의 **⇄ 버튼**을 누릅니다(분할바 더블클릭은 역할은 그대로 두고 좌우 위치만 바꿉니다).
7. 두 칸 모두 PDF여도 됩니다. **각 칸 오른쪽 위 표시줄**에서 그 칸만의 줌·페이지 이동·찾기·보기 방식(이어보기 ↔ 한 장씩)을 다룹니다. 자판(`PageUp`·`PageDown`·`←`·`→`)은 마지막에 누른 칸의 PDF를 넘깁니다.
8. **사이드바·상단 탭에서 고른 파일은 마지막에 클릭한 칸에 열립니다.** 바꾸고 싶은 칸을 한 번 누른 뒤(옅은 테두리로 표시) 파일을 고르세요. 파일을 본문으로 끌면 좌우 모드에서는 왼쪽/오른쪽, 상하 모드에서는 위쪽/아래쪽 칸에 바로 놓을 수 있습니다. 반대 칸에 떠 있는 파일을 고르면 두 칸이 서로 바뀌고, 이미 그 칸에 떠 있는 파일이면 그대로 유지됩니다. 참고 칸으로 연 파일도 상단 탭에 추가되며, 참고 문서의 탭은 테두리로 표시됩니다.

### 이미지

- 회전·좌우/상하 뒤집기·자르기·PNG/JPG/PDF 저장, 되돌리기/다시 실행(50단계)
- **✏️ 표시** — 펜·형광펜·화살표·사각형·텍스트로 이미지 위에 표시, **▦ 모자이크**로 개인정보(이름·얼굴) 가리기. 표시는 저장할 때 이미지에 그대로 구워짐
- **🖱 선택** — 그린 표시를 클릭해 선택 → **드래그로 이동**, `Delete` 삭제, 텍스트 **더블클릭으로 수정**. 텍스트는 넣자마자 자동 선택되어 바로 위치 조정 가능. `Ctrl+Z`/`Ctrl+Y` 되돌리기 지원
- **자르기** — 영역을 드래그한 뒤 **모서리 핸들로 조절·안쪽 드래그로 이동**, 자유·1:1·4:3·16:9 비율 프리셋
- **보정** — 자동보정·밝기·대비·채도·선명도·노이즈완화 + **크기 줄이기(½·⅓·폭 입력)** + **JPG 품질 슬라이더** + 2x/4x 고화질 확대
- **📷 메모로** — 현재 편집본을 일반 메모에 이미지 블록으로 넣기. **자르기 영역을 선택해 두면 그 부분만 발췌**(원본은 그대로), 표시·모자이크·보정 포함
- EXE에서는 저장 시 권한 팝업 없이 **저장 폴더(내 문서\ClassDock 저장)**에 기록되고 완료 알림에서 바로 **폴더 열기** 가능 (일반 브라우저는 다운로드)

### 영상·오디오·자막

- 영상(`.mp4`·`.webm`·`.mov`·`.mkv`·`.avi` 등)과 오디오(`.mp3`·`.wav`·`.m4a`·`.flac` 등)를 일반 문서처럼 탭에서 재생합니다. 브라우저 기본 재생 컨트롤로 탐색·배속·전체화면을 사용하세요.
- 영상 도구바의 **속도 칸**(0.5×~2×)으로 재생 속도를 바꿉니다. 어학 듣기·시범 동작은 느리게, 복습은 빠르게 쓰세요. `[`/`]` 키로도 한 칸씩 옮길 수 있고(오디오에서도 동작), 변환본으로 갈아타도 고른 속도가 유지됩니다.
- `←`/`→`로 **10초씩 되감기·건너뛰기**를 합니다. 영상을 클릭해 두지 않아도 되고(클릭하면 재생이 멈춥니다), 오디오에서도 똑같이 동작합니다. 브라우저 기본 화살표(±5초)와 겹치지 않게 처리했습니다.
- 영상 도구바의 **📷 장면 캡처**는 현재 프레임을 일반 메모에 넣고, 메모를 쓸 수 없으면 PNG 파일로 저장합니다.
- 영상 도구바의 **작은 창**(또는 `Alt`+`P`)은 영상만 떼어 화면 맨 위 작은 창으로 띄웁니다. 다른 자료를 보거나 판서하는 동안 계속 재생됩니다. 크롬·엣지에서만 되고(파이어폭스에서는 버튼이 보이지 않습니다), 이 작은 창에는 아래의 **자막 크기** 설정이 적용되지 않습니다.
- `.srt`·`.vtt`·`.smi` 자막은 같은 이름의 영상과 자동 연결됩니다. 영상 탭의 **자막 열기**로 직접 고를 수도 있고, 표시/숨기기와 글자 크기 조절을 지원합니다.
- MP4(H.264)·WebM을 권장합니다. MKV·AVI·WMV·FLV·MOV처럼 재생이 안 되는 영상은 EXE에서 **MP4로 변환**할 수 있으며, 폴더 메뉴의 **영상 일괄 MP4 변환**도 지원합니다. 변환은 500MB 이하만 가능하고 처음에는 ffmpeg 설치를 위해 인터넷이 필요할 수 있습니다.

### ▦ 폴더 이미지·PDF 모아보기

- **열기 → 폴더 열기**로 연 폴더에서 폴더 이름을 우클릭하면 **이미지 모아보기**와 **PDF 모아보기**가 나타납니다.
- 같은 메뉴 위쪽에서 **새 Python 코드**·**새 노트북**·**새 빈 표**·**새 텍스트 파일**·**새 블록 문서**를 우클릭한 폴더 안에 바로 만들 수 있습니다. 첫 저장(`Ctrl+S`) 때 파일 이름을 물어보고, 원본 저장으로 연 폴더라면 정한 이름으로 그 폴더에 파일이 만들어집니다. 같은 이름이 있으면 번호가 붙습니다. (화이트보드는 파일로 저장하는 형식이 없어 사이드바의 **새로 만들기**에서만 만듭니다.)
- 쓰기 권한을 허용해 연 폴더에서는 **새 폴더**를 선택해 우클릭한 위치의 실제 디스크에 빈 폴더를 만들 수 있습니다. ZIP·읽기 전용 폴더에는 이 메뉴가 나타나지 않습니다.
- 현재 폴더만 또는 하위 폴더까지 포함해 썸네일 격자로 보고, 항목을 누르면 원래 이미지 편집기 또는 PDF 탭으로 바로 이동합니다.

### 픽셀 펫·집중 모드

- 설정에서 **픽셀 펫**을 켜면 1~12마리의 작은 캐릭터가 화면과 창 위를 돌아다닙니다. 클릭해 말을 듣거나 붙잡아 던질 수 있습니다.
- 설정의 **대사 편집**과 **나만의 펫 만들기**에서 펫별 대사와 그림·움직임·색 조합을 저장할 수 있습니다.
- 헤더의 **집중**에서 1~180분 집중과 1~60분 휴식 시간을 정해 타이머를 시작합니다. 완료 횟수와 진행 중인 세션은 앱을 닫았다 다시 열어도 이어집니다.
- 라이트 모드에서는 설정의 **라이트 모드 배경**에서 쿨 그레이·웜·민트·라벤더·스카이 프리셋을 고를 수 있습니다.

### ZIP·압축 묶음

- 드래그 앤 드롭 또는 파일 열기로 ZIP 선택
- 내부 파일이 폴더처럼 사이드바에 펼쳐짐
- 암호 걸린 ZIP은 암호 입력 가능
- 단 **읽기 전용** — 원본 ZIP을 다시 압축해 저장하지는 못함 (편집물은 별도 저장)

---

## 6. 상황별 가이드

### 인터넷이 안 되는 환경에서 쓰고 싶어요

- **EXE 사용 가능** — 문서 보기·편집은 완전 오프라인
- **오프라인 HTML 사용 가능** — 같은 이유
- **단, Python을 브라우저(Pyodide)로 실행할 때**는 첫 실행에 인터넷 필요 (런타임 다운로드)
- **완전 오프라인 Python을 원하면**: EXE + 로컬에 Python 설치 → 로컬 Python으로 실행되므로 인터넷 불필요
- **오프라인에서도 Pyodide를 쓰려면**: EXE 옆 `vendor/pyodide/` 폴더에 코어 파일이 미리 있어야 함

### 파이썬은 설치하지 않고도 코드를 실행하고 싶어요

- 브라우저 Pyodide가 자동으로 동작합니다 — **별도 파이썬 설치 불필요**
- `numpy`·`pandas`·`matplotlib` 같은 공식 패키지와 PyPI의 순수 Python 패키지는 첫 실행 시 자동으로 받아옴
- `Faker`는 EXE에 내장되어 인터넷 없이도 사용 가능
- 첫 실행만 인터넷 필요, 이후 캐시
- **제한**: `selenium`·`konlpy` 등 Pyodide 미지원 패키지, OS 명령(`subprocess`)은 사용 불가

### 학생들에게 배포하려면?

- **단일 파일 배포 = `classdock-offline.html` 한 파일만** USB·메신저로 주면 끝
- 학생이 더블클릭하면 바로 브라우저에서 열림, 파이썬·라이브러리 설치 불필요
- 더 강력한 기능(로컬 Python 실행·자동 복원·PPT 변환)이 필요하면 `ClassDock.exe` 배포
- **숙제·수행평가는 [과제 패키지(.task)](#과제-패키지-task--만들고--풀고--검수하기)** 로 — 문제·시작 코드·자동채점을 한 파일로 배포하고 제출본(`.taskdone`)을 걷어 일괄 재채점·성적 CSV까지

### `input()`이 있는 코드를 가르치고 싶어요

- **자연스러운 대화형 입력**이 필요하면 **EXE + 로컬 Python**이 답
- 브라우저(Pyodide)에서는 코드 실행 전에 입력값들을 미리 한 줄씩 적어두는 방식

### 매번 EXE 누르기 귀찮을 때

EXE를 한 번 띄워두고 **브라우저 즐겨찾기로 바로 접속**할 수 있어요.

1. `desktop/start-server-hidden.vbs` 실행 — 콘솔창 없이 서버만 뜸 (브라우저는 자동으로 열리지 않음)
2. 브라우저에서 `http://127.0.0.1:17645/` 접속 후 **즐겨찾기**(`Ctrl+D`)
3. 그 `.vbs`를 **시작프로그램 폴더**(`Win+R` → `shell:startup` → 거기에 복사)에 넣어두면 PC 켤 때 자동으로 서버가 뜨고, 즐겨찾기만 누르면 바로 사용 가능
4. 끄려면 `desktop/stop-server.cmd` 실행

> ⚠️ 백그라운드 서버가 뜬 상태에서는 EXE를 직접 더블클릭하지 마세요 (중복 인스턴스가 됨).

### 작업 표시줄에 고정하고 싶어요

1. 파일 탐색기에서 `ClassDock.exe` 위치를 엽니다
2. EXE를 **마우스 오른쪽 클릭**
3. Windows 11: **더 많은 옵션 표시** → **작업 표시줄에 고정**
4. Windows 10: 바로 **작업 표시줄에 고정**

> ⚠️ EXE 파일을 다른 위치로 옮기거나 삭제하면 고정된 아이콘이 작동하지 않습니다.
> ⚠️ 위의 "즐겨찾기로 항상 켜두기"를 함께 쓰는 중이라면, 작업 표시줄의 EXE를 누르지 말고 브라우저 즐겨찾기를 쓰세요.

### PDF 파일에 암호가 걸려 있어요

- **Word·Excel·PowerPoint(.docx/.xlsx/.pptx)** 의 Agile 방식 암호는 지원 (열 때 입력창이 뜸)
- **PDF 자체 암호**는 아직 미지원 — 다른 도구로 암호를 제거 후 열어야 합니다
- **HWPX(신형 한글)** 는 간이 미리보기(문단·표·그림) — 정확한 모양이나 DRM·암호 문서는 한글에서 PDF로 저장 후 열기

### 작업 자동 복원

- 마지막으로 열어둔 파일·ZIP·폴더·탭 순서까지 다음 실행 때 자동 복원
- EXE는 로컬 서버 저장소에, 일반 브라우저(HTML)는 **이 브라우저의 IndexedDB**에 저장 (같은 브라우저로 다시 열어야 복원됨)
- 사이드바 **더보기 → 최근 작업공간 지우기**로 삭제 가능
- 합계 256MB·최대 10,000개 항목까지 저장. 더 큰 묶음은 현재 세션은 열리지만 자동 복원은 생략

### 미저장 작업 전체 백업·복원

- 사이드바 **더보기 → 전체 백업 ZIP 내보내기**로 작업공간, PDF 편집, 노트북, 임시 메모·이미지 메모, 화이트보드, 코드 초안과 설정을 한 번에 백업
- **백업 ZIP에서 복원**을 누르고 내보낸 ZIP을 선택하면 현재 복구 데이터를 교체한 뒤 자동으로 다시 불러옴
- 일반 ZIP이나 손상된 파일은 복원하지 않고 올바른 ClassDock 백업을 선택하라는 안내 표시
- 다른 PC에서는 파일·폴더 쓰기 권한을 다시 받아야 함. Python 실행 변수·되돌리기 기록·OCR 캐시는 백업 제외

---

## 7. 자주 쓰는 단축키

> ⚙ 표시는 **설정 → 단축키**에서 바꿀 수 있는 항목입니다(총 22개). 나머지는 편집기·표·노트북 내부 고정 단축키입니다. 전체 목록은 헤더의 `?` 버튼(노트북은 자체 치트시트)에서도 볼 수 있습니다.

### 전역 (어디서나)

| 단축키 | 동작 | ⚙ |
|--------|------|:--:|
| `Ctrl+K` | **명령 팔레트** — 기능을 이름으로 찾아 바로 실행 | ⚙ |
| `Ctrl+O` | 파일 열기 | ⚙ |
| `Ctrl+Shift+O` | 폴더 열기 | ⚙ |
| `Ctrl+S` | 현재 파일 저장 (PDF 내보내기 / Python·노트북 저장) | ⚙ |
| `Ctrl+W` | 현재 탭 닫기 | ⚙ |
| `Ctrl+Shift+T` | 닫은 파일 다시 열기 | ⚙ |
| `Ctrl+Shift+F` | 열린 파일 검색 (파일명·내용 통합) | ⚙ |
| `Ctrl+M` | 임시 메모 열기/닫기 | ⚙ |
| `Alt+N` | 새 파이썬 코드 | ⚙ |
| `Alt+B` | 새 화이트보드 (탭바 오른쪽 칠판 버튼과 같음) | ⚙ |
| `Ctrl+←` / `Ctrl+→` | 같은 묶음의 이전/다음 파일로 이동 | ⚙ |
| `Ctrl+F12` | 대기 화면 지금 시작 (모니터 전체 화면) | ⚙ |
| `Alt+←` / `Alt+→` | 왼쪽 사이드바 숨기기/보이기 | ⚙ |
| `Ctrl` 두 번 빠르게 | Python 편집기로 포커스 복귀 | |
| `Esc` | 열린 모달·메뉴·필기 모드 닫기 | |

> 사이드바 목록에 포커스가 있을 땐 `↑`/`↓`로 파일 이동, `Enter`/`Space`로 열기.

### PDF 편집·보기

| 단축키 | 동작 | ⚙ |
|--------|------|:--:|
| `Ctrl+F` | PDF 안에서 찾기 (형광 표시) | ⚙ |
| `Ctrl+Z` | 편집·페이지 작업 되돌리기 (최대 50단계) | |
| `Ctrl+Shift+Z` / `Ctrl+Y` | 다시 실행 | |
| `Delete` | 선택한 요소(서명·텍스트 등) 삭제 | |
| 페이지 번호 입력 후 `Enter` | 해당 페이지로 이동 | |
| `PageUp` / `PageDown`, `←` / `→` | **한 장씩 보기**에서 앞·뒤 페이지로 넘기기 | |

### 영상·오디오 재생

| 단축키 | 동작 | ⚙ |
|--------|------|:--:|
| `←` / `→` | **10초 뒤로 / 앞으로** (영상을 클릭하지 않아도 동작) | |
| `[` / `]` | **재생 속도** 한 칸 느리게 / 빠르게 (0.5×·0.75×·1×·1.25×·1.5×·2×) | |
| `Alt+P` | **작은 창(PiP)** 켜기/끄기 — 도구바의 `작은 창` 버튼과 같음 (크롬·엣지) | |

> 재생/일시정지·전체화면·볼륨은 브라우저 기본 재생 컨트롤을 그대로 씁니다. 브라우저 기본 메뉴(`⋮` → 재생 속도)로 속도를 바꿔도 도구바 속도 칸이 같은 값을 가리킵니다.

### Python 코드 편집기

| 단축키 | 동작 | ⚙ |
|--------|------|:--:|
| `Ctrl+Enter` | 코드 실행 | ⚙ |
| `Ctrl+F` | 찾기·바꾸기 | ⚙ |
| `Ctrl+G` | 줄 번호로 이동 (읽기 전용 코드 보기에서도 동작) | ⚙ |
| `Shift+Alt+F` | 코드 자동 정렬 (로컬 파이썬이면 black) | ⚙ |
| `Ctrl+Home` / `Ctrl+End` | 텍스트·코드 문서 맨 앞 / 맨 끝으로 이동 | |
| `Ctrl+F10` | 특수문자 문자표 (우클릭 → 특수문자…와 같음) | |
| `Alt+Shift+←` / `Alt+Shift+→` | 실행 결과를 오른쪽에 표시 / 숨기기 (실행 전에는 `←`로 위치만 예약) | |
| `Alt+Shift+↑` / `Alt+Shift+↓` | 실행 결과를 아래에 표시 / 숨기기 (실행 전에는 `↑`로 위치만 예약) | |
| `Ctrl+D` | 선택한 줄 삭제 | |
| `Alt+↑` / `Alt+↓` | 선택한 줄 위/아래로 이동 | |
| `Ctrl+Alt+↓` | 선택한 줄 아래로 복제 | |
| `Ctrl+/` | 선택한 줄 주석 토글 | |
| `Tab` / `Shift+Tab` | 들여쓰기 / 내어쓰기 | |
| `F3` / `Shift+F3` | 커서가 놓인 단어 선택 → 같은 단어 다음/이전 위치 | |
| `Ctrl+Space` | 자동완성 열기 (`↑`/`↓` 선택 · `Enter`/`Tab` 적용 · `Esc` 닫기) | |
| `Ctrl+클릭` | 함수 정의로 이동 | |
| `(`·`[`·`{` 바로 뒤 더블클릭 | 대응하는 괄호 안의 내용 전체 선택 | |
| 더블클릭 후 입력 | 같은 식별자 동시수정 | |
| `Alt`+세로 드래그 | 열(블록) 동시 편집 | |
| 열 편집 중 `Ctrl+C` / `Ctrl+X` / `Ctrl+V` | 사각 선택 복사 / 잘라내기 / 붙여넣기 | |
| 따라치기 중 `Esc` | 따라치기 그만두기 | |
| 음감 테스트 중 `Space` / `Esc` | 다시 듣기(한 번) / 테스트 그만두기 | 악보 |
| 우클릭 | 복사·붙여넣기, 대소문자 변경, 선택한 줄 중복 제거, 특수문자 | |
| `Ctrl+Z` / `Ctrl+Shift+Z`(`Ctrl+Y`) | 되돌리기 / 다시 실행 (최대 300단계) | |
| `Ctrl` `+` / `Ctrl` `-` | 코드와 실행 결과 문자 크게 / 작게 | |

> 폴더·ZIP·여러 파일로 함께 연 Python 프로젝트에서는 다른 폴더의 `.py` 모듈과 최상위 함수·클래스도 자동완성에 나옵니다. 이름을 고르면 필요한 `import` 문이 함께 들어가며(예: `State` → `from 패키지.모듈 import State`), 이 후보는 `Ctrl+Space` 없이 그냥 칠 때도 나옵니다. 설치한 라이브러리 전체 목록은 `Ctrl+Space`에서만 열립니다.
>
> `from ` 뒤에서는 프로젝트의 패키지·모듈 이름이, 점을 찍으면 그 아래 모듈이, `from 모듈 import ` 뒤에서는 그 모듈 안의 클래스·함수가 단계별로 나옵니다(`from 패키지 import (` 로 여러 줄에 걸쳐 쓴 목록 안에서도 동작). 한 번도 열어 본 적 없는 파일도 배경에서 미리 읽어 후보에 넣지만, 별도로 연 다른 프로젝트 폴더나 앱에 열지 않은 디스크 폴더는 검색하지 않습니다.
>
> EXE에 로컬 Python이 있으면 한 걸음 더 나갑니다. 작업공간의 `.py` 를 내 PC 임시 폴더에 한 벌 복사해 자동완성 엔진(Jedi)에게 프로젝트로 알려 주어, `import 내패키지.모듈 as n` 뒤의 `n.` 처럼 **내 코드 본문에서의 멤버 자동완성**과 내 모듈로의 **정의 이동(`Ctrl`+클릭)** 이 동작합니다. 정의 이동은 복사본이 아니라 원래 탭을 엽니다. 복사본은 내용이 바뀔 때만 갱신되고 앱을 닫을 때 지워지며, 파일이 인터넷으로 나가지는 않습니다.

### Jupyter 노트북

| 단축키 | 동작 | ⚙ |
|--------|------|:--:|
| `Ctrl+Enter` | 현재 셀 실행 | ⚙ |
| `Ctrl+Shift+Enter` | 모든 코드 셀 전체 실행 | ⚙ |
| `Shift+Enter` | 실행하고 다음 셀로 | |
| `Enter` / `Esc` | 셀 편집 시작 / 명령 모드로 | |
| `A` / `B` | 위 / 아래에 코드 셀 추가 | |
| `M` / `Y` | 마크다운 / 코드 셀로 변환 | |
| `D`, `D` | 셀 삭제 (D 연속 두 번) | |
| `↑`/`↓` 또는 `K`/`J` | 셀 선택 이동 | |
| `Shift+↑` / `Shift+↓` | 여러 셀 선택 | |
| `Ctrl+C` / `X` / `V` | 셀 복사 / 잘라내기 / 붙여넣기 | |
| `Ctrl+Z` / `Ctrl+Y` | 셀 작업 되돌리기 / 다시 실행 | |
| `Ctrl+↑` / `Ctrl+↓` | 위/아래 셀 편집으로 이동 | |
| `Ctrl+Home` / `Ctrl+End` | 첫 셀 맨 앞 / 마지막 셀 맨 끝으로 이동 | |
| `Shift+Tab` · `Alt+클릭` | 함수 도움말(설명) 보기 | |
| `Ctrl+F` / `Ctrl+Shift+H` | 노트북 전체 / 현재 셀 찾기·바꾸기 | |
| `Ctrl` `+` / `Ctrl` `-` | 코드 셀과 실행 결과 문자 크게 / 작게 | |

### Excel · CSV 표

| 단축키 | 동작 |
|--------|------|
| `Ctrl+C` | 선택 영역 TSV 복사 |
| `Ctrl+A` | 전체 선택 |
| `↑`/`↓`/`←`/`→` | 셀 이동 (`Ctrl`+화살표: 맨 끝으로) |
| `Home` / `End` / `Ctrl+Home` / `Ctrl+End` | 행 끝 / 시트 끝 |
| `PageUp` / `PageDown` | 한 화면 위/아래 |
| `Shift`+클릭·화살표 | 범위 선택 |
| `Ctrl`+클릭·드래그 | 비연속 셀·행·열 추가 선택 / 다시 선택해 해제 |
| `Ctrl+Z` / `Ctrl+Y`(`Ctrl+Shift+Z`) | 편집 모드 되돌리기 / 다시 실행 |

### 화이트보드

| 단축키 | 동작 |
|--------|------|
| `Ctrl+Z` / `Ctrl+Y`(`Ctrl+Shift+Z`) | 되돌리기 / 다시 실행 |
| `Ctrl+C` / `Ctrl+X` / `Ctrl+V` | 선택한 항목 복사 / 잘라내기 / 붙여넣기(우클릭한 자리) |
| `Ctrl+S` | 현재 보드를 PNG 로 내보내기 |
| `Delete` / `Backspace` | 선택한 이미지·도형·텍스트 삭제 |
| 마우스 휠 | 포인터 기준 화면 확대·축소 (0.25~4배) |
| `Space`+드래그 · 휠(가운데) 버튼 드래그 | 화면 이동 (도구막대의 `100%` 로 원위치) |
| 마우스 오른쪽 버튼 | 보드 빠른 메뉴 열기 |
| `Esc` | 우클릭 메뉴·배경색·도구상자 판 닫기 → 집중 도구는 조절점 숨기기 → 한 번 더 누르면 종료 → 선택 해제 |
| 텍스트 입력 중 `Enter` / `Shift+Enter` / `Esc` | 입력 확정 / 줄바꿈 / 취소 |
| 수식 틀 입력 중 `Tab` / `Shift+Tab` | 다음 / 이전 입력 부분으로 이동 |
| 수식 입력 중 `Ctrl+Enter` | LaTeX 수식 삽입 또는 수정 확정 |
| 리플레이 재생 화면에서 `Space` | 재생 / 일시정지 |

### 연대표

| 단축키 | 동작 |
|--------|------|
| `Ctrl+S` | `.timeline` 저장 |
| `Ctrl+Z` / `Ctrl+Y`(`Ctrl+Shift+Z`) | 사건 편집 되돌리기 / 다시 실행 |
| `Ctrl`+`+` / `Ctrl`+`-` / `Ctrl`+`0` | 카드·글자·시간축을 함께 확대 / 축소 / 100% |
| 빈 여백 마우스 드래그 | 긴 연대표 화면 가로·세로 이동 |
| 선택한 사건에서 `Enter` | 사건 수정 창 열기 |
| 선택한 사건에서 `Delete` | 사건 삭제 확인 |
| 발표 중 `→`·`Space` / `←` / `Esc` | 다음 / 이전 / 발표 끝내기 |

### 메모 · 이미지 메모

| 단축키 | 동작 |
|--------|------|
| `Ctrl+V` | 클립보드 이미지 붙여넣기 (일반 메모·이미지 메모) |
| 이미지 확대 중 `+`/`=` · `-` · 휠 | 확대 / 축소 (`Esc` 닫기, 더블클릭 화면맞춤↔100%) |

---

## 8. 개발자용 — 소스 수정 후 다시 빌드

이 프로젝트의 EXE 빌드는 다음 두 단계로 진행됩니다.

1. `classdock.html`, `src/`, `vendor/`를 합쳐 `classdock-offline.html` 생성
2. 오프라인 HTML을 C# 런처에 내장해 `ClassDock.exe` 생성

### 준비물

- **Node.js** — `node --version`으로 확인. 없으면 [nodejs.org](https://nodejs.org)에서 설치
- **.NET Framework C# 컴파일러** — 일반적인 Windows 환경에서는 아래 위치 중 하나에 있습니다
  - `C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe`
  - `C:\Windows\Microsoft.NET\Framework\v4.0.30319\csc.exe`
- 일반적인 소스 검사·단위 테스트·오프라인 빌드는 추가 npm 패키지 없이 실행됩니다. 단 `npm run test:e2e`의 Playwright E2E 테스트는 `@playwright/test` 개발 의존성이 있으므로 새 환경에서는 먼저 `npm install`이 필요합니다.

### 권장 빌드 절차

프로젝트 루트에서 PowerShell을 열고 다음 명령을 순서대로 실행합니다.

```powershell
cd <저장소를 클론한 폴더>       # 예: D:\my

# 실행 중인 기존 EXE가 있다면 종료
Get-Process classdock -ErrorAction SilentlyContinue |
    Stop-Process -Force

# 구문 검사 + 자동 테스트 + 오프라인 HTML 생성 + 배포물 검사
npm.cmd run verify

# 오프라인 HTML을 내장한 EXE 생성
cmd /c desktop\build.bat
```

> PowerShell에서는 실행 정책에 따라 `npm`이 `npm.ps1` 오류로 막힐 수 있습니다. 이때 실행 정책을 바꾸지 말고 위 예시처럼 `npm.cmd`를 사용하면 됩니다.

### 테스트를 생략하고 빠르게 빌드

코드 검증이 이미 끝난 상태에서 산출물만 다시 만들려면:

```powershell
cd <저장소를 클론한 폴더>
node build-offline.js
cmd /c desktop\build.bat
```

> `node build-offline.js`는 이미 만들어져 있는 `vendor/`를 그대로 씁니다. **처음 클론했거나 맞춤법 사전을 바꿨다면** `npm.cmd run build`(사전 워커 생성 → 오프라인 HTML)를 한 번 거쳐야 합니다.

개별 검증 명령은 다음과 같습니다.

```powershell
npm.cmd run check          # 구문 검사만
npm.cmd test               # 자동 테스트만(Node 내장 러너)
npm.cmd run build          # 맞춤법 사전 워커 + 사용법 HTML + 오프라인 HTML 생성
npm.cmd run build:manual   # 사용법.md → 사용법.html 만 다시 생성
npm.cmd run release-check  # 배포 산출물·vendor 해시 검사
npm.cmd run test:e2e       # 화면 흐름 테스트(Playwright, npm install 필요)
```

### 생성되는 파일

| 파일 | 설명 |
|------|------|
| `vendor/korean-hunspell-worker.js` | 한국어 맞춤법 사전 Worker (`npm run build`가 먼저 생성) |
| `사용법.html` | 이 문서(`사용법.md`)를 변환한 사용자용 HTML (`npm run build:manual`, 오프라인 HTML에 함께 내장) |
| `classdock-offline.html` | CSS·JavaScript·문서 라이브러리를 합친 단일 오프라인 HTML |
| `desktop/app.html` | EXE에 내장하기 위해 복사된 오프라인 HTML |
| `ClassDock.exe` | 최종 Windows 실행 파일 |

결과 파일의 크기와 수정 시각은 다음 명령으로 확인할 수 있습니다.

```powershell
Get-Item .\ClassDock.exe |
    Select-Object FullName, Length, LastWriteTime
```

### 자주 발생하는 빌드 오류

- **`npm.ps1`을 로드할 수 없음**: `npm` 대신 `npm.cmd` 사용
- **`ClassDock.exe` 액세스 거부**: 실행 중인 EXE를 위의 `Stop-Process` 명령으로 종료한 뒤 재시도
- **`classdock-offline.html`이 없음**: `node build-offline.js` 또는 `npm.cmd run build`를 먼저 실행
- **C# compiler not found**: .NET Framework 개발 구성 요소를 설치하거나 Go 1.21 이상을 설치

`desktop/build.bat`은 C# 컴파일러가 있으면 `desktop/launcher.cs`를 사용합니다. 컴파일러가 없으면 Go 빌드를 시도하지만, Go 폴백 버전은 PowerPoint 변환 등 일부 로컬 연동 기능이 제한되므로 **C# 빌드를 권장**합니다.

### 배포할 때

- 기본 앱 기능은 `ClassDock.exe` 하나로 실행할 수 있습니다.
- 인터넷 없이 브라우저 Python(Pyodide)까지 사용하려면 EXE와 함께 `vendor/pyodide/` 폴더를 같은 상대 위치로 배포합니다.
- 로컬 Python의 번들 패키지 설치까지 오프라인으로 지원하려면 `vendor/wheels/`도 함께 배포합니다.
- 자체 코드 서명을 하지 않은 EXE에는 Windows SmartScreen 경고가 표시될 수 있습니다.

### 즐겨찾기 상시구동 중이라면

- 재빌드 후 `desktop/stop-server.cmd` → `desktop/start-server-hidden.vbs`로 서버 재시작 필요

---

## 9. 제한사항 한눈에 보기

| 영역 | 제한 |
|------|------|
| **Word** | 보기 + 선택 글자/문단 서식·정렬·간격·들여쓰기·글머리표/번호·서식 복사/붙이기 + 표 행/열·셀 서식·오른쪽 병합/가로 분할 + 용지 방향·여백·머리글/바닥글 + PNG/JPG/GIF 그림 추가·교체·크기 + 본문 찾아 바꾸기. 세로 병합·중첩 표 구조 변경은 불가 |
| **PowerPoint** | 보기 + 슬라이드 찾아 바꾸기 (PDF로 변환되어 열린 경우는 제외) |
| **HWP** | 보기 전용 (편집 불가) |
| **구형 Word(.doc)** | **글자만** 뽑는 간이 미리보기(표·그림·서식 제외). 암호 걸린 `.doc`은 열 수 없음. 이름만 `.doc`인 docx·RTF·텍스트는 내용을 보고 알맞은 뷰어로 자동 연결 |
| **PDF 암호** | 열 수 없음 |
| **HWPX(신형 한글)** | 간이 미리보기(문단·표·그림). DRM·암호 문서 제외 |
| **ZIP** | 읽기 전용. 항목당 64MB, 해제 합계 256MB 제한. 앱 전용 문서(`.map`·`.timeline`·`.mnote`·`.msheet`)와 악보(`.musicxml`·압축형 `.mxl`)도 압축 안에서 제 편집기로 열림 |
| **Python 옆 파일 함께 실행** | 합계 50MB 초과 시 단독 실행으로 폴백 |
| **Python 실행 시간** | 터미널의 로컬 PowerShell 명령은 시간 제한 없음(프로세스 트리 메모리 4GB 제한 유지). 일반 로컬 Python 실행은 30분, Pyodide(브라우저)는 5분 자동 중지 |
| **Python 출력** | 표준출력·오류는 각각 앞 4MB까지 보관(진단·채점·단계 실행의 내부 결과는 별도 6MB 제한) |
| **자동 복원** | 합계 256MB, 최대 10,000 항목 |
| **PDF 텍스트 요소** | 저장 시 이미지로 들어가 선택·검색 불가 (서명·기입용) |
| **Pyodide 패키지** | `selenium`·`konlpy`·`subprocess` 등 일부 불가 |
| **PPTX 정확 변환** | 기본은 설치 여부와 관계없이 즉시 간이 미리보기. 문서 위 버튼 또는 설정의 자동 정확 변환을 사용하려면 EXE + 설치된 PowerPoint 필요 |
| **수업 리플레이** | 시간 제한 없음(용량은 판서·코드·실행 결과량에 비례). **화이트보드·PDF 필기·Python 코드/실행 결과** 녹화 지원. PDF 필기는 페이지 배경 이미지를 담아 파일이 더 큼 |
| **대기 화면(화면보호기)** | 기본 꺼짐(설정에서 켤 때만). 영상은 브라우저 저장소에 보관(용량 큰 영상은 저장 실패할 수 있음 → 짧은 영상 권장). **유휴 자동 표시는 브라우저 창 안 오버레이**(브라우저 보안상 전체화면 자동 전환 불가) — 모니터 전체는 설정의 **▶ 지금 시작** 버튼 또는 F11·⛶ 전체화면 상태에서 가능 |
| **대기 화면 웹 주소(URL)** | 인터넷 연결 필요. **삽입을 막아 둔 사이트가 많아** 주소마다 되는지 다름(저장 전 `미리보기 · 테스트` 필수). **로그인 상태가 그대로 보이므로 공개 페이지만** 사용. 페이지 조작은 불가(보기 전용). 안 열리면 영상 → 시계 애니메이션으로 자동 대체 |
| **메모의 노트북 셀** | 최대 200개. 실행 결과·실행 번호·세션 필기는 제외하고 코드·마크다운 첨부·일반 메타데이터만 보관 |
| **메모의 표** | 표 하나는 200행 × 20열이되 칸 3,000개까지(열이 많으면 행이 먼저 막힘 — 20열이면 150행). 지도의 **🧾 표로 메모**로 보낸 표시가 이보다 많으면 담긴 수와 빠진 수를 알려 주며, 전부 필요하면 지도의 **CSV 내보내기**를 사용 |
| **과제 패키지(.task)** | 파이썬 단일 파일 과제 또는 지도 문제(위치 찾기)만. 테스트 최대 20개·지도 문제 최대 30문항·첨부 합계 8MB. `.task`는 텍스트(JSON)라 **숨김 테스트·봉인은 완전한 보안이 아님** — 확실한 확인은 선생님 재채점으로 |
| **시험지(.exam)** | 문항 최대 100개(모두 1점)·보기 8개·문항당 이미지 4장·이미지 합계 12MB. 주관식 자동 채점은 정답 목록과의 글자 비교라 **표현이 다르면 선생님이 [답안 보기]에서 판단**해야 합니다. **암호 기능이 필요해 EXE(또는 https)에서만 동작**하고, `file://`로 연 페이지에서는 막힙니다. **선생님 암호를 잊으면 제출본을 열 수 없습니다**(복구 불가). 시험 중 화면 캡처·검색 같은 부정행위는 막지 못합니다 |
| **블록 문서(.mnote)** | 표 하나는 50행 × 20열, 이미지 한 장 25MB까지. 이미지가 파일 안에 들어가므로 사진이 많으면 파일이 커짐 |
| **악보(.msheet)** | 파트 수 제한 없는 다중 악기 악보를 지원하며 각 파트에 이름·음색·음량·음소거·단일 오선/피아노 대보표를 저장. 오선별 성부 2개, 화음·붙임줄·이음줄·가사·코드·셈여림·연주 기호·셋잇단·운지·페달·반복선과 1·2번 괄호·못갖춘마디·중간 박자/조표/빠르기 변경 지원. 장·단조 조표는 ♯·♭ 7개까지, 오른손 음역 G3~C6·왼손 C2~C5. 조옮김은 모든 파트를 위아래 한 옥타브까지 함께 옮기며 새 조표는 임시표가 적은 쪽으로 정해짐. MusicXML은 모든 파트와 오선별 두 성부를 가져오고 내보내며 고급 표기는 단순화할 수 있음. MIDI 건반 입력, 악기별 형식 1 다중 트랙 MIDI, 전체 합주 WAV, 기본·동요·발라드·행진·왈츠·록 드럼 및 코드 기호 기반 베이스·피아노·기타 자동 반주, 참고 이미지 나란히 보기를 제공하지만 이미지 자동 악보 인식과 마이크 녹음은 없음. 기본 음색은 내장 피아노 녹음이며 기타·실로폰·하프·플루트·클라리넷 녹음과 합성음도 선택 가능 |
| **지도(.map)** | 배경지도에 **인터넷 필요**(표시·메모·저장은 오프라인에서도 동작). 장소 **이름 검색은 인터넷과 ClassDock 런처 필요**. 기본 OpenStreetMap 검색 또는 설정한 개인 카카오 REST API 키로 국내 주소·장소를 찾고, 카카오 검색이 안 되면 기본 검색으로 자동 전환. 좌표 이동은 HTML 직접 실행에서도 가능. 런처는 화면에 실제로 표시된 타일만 자동 보관하고 공개 서버가 금지하는 사전 일괄 다운로드는 하지 않음. 캐시는 이 컴퓨터에 정확히 최대 400MB까지 보관하고 넘치면 오래된 것부터 지우며, 배경지도 종류마다 따로 쌓임. 칠판으로 보낸 지도는 **그림**이라 그 뒤에는 확대·이동이 안 됨 |
| **연대표(.timeline)** | 사건 최대 1,000개, 제목·유적지명 각 120자·유적지 주소 200자·설명 4,000자. 날짜는 연·연월·날짜와 기원전 표기를 지원하지만 `조선 후기`처럼 숫자 없는 시대 이름만으로는 정렬할 수 없음. 사진은 한 장 약 660KB·전체 40MB까지 파일 안에 포함. CSV에는 유적지명·주소·이미지 파일명이 들어가고, XLSX에는 사건별 실제 사진도 같은 행에 포함 |
| **파일 비교(diff)** | 텍스트·코드만. 파일 하나가 5MB 또는 5만 줄을 넘으면 비교하지 않음. 비교 화면은 저장·자동 복원 대상이 아님 |
| **여러 파일 찾아 바꾸기** | 열려 있는 텍스트·코드 문서와 Word(`.docx`) 본문·PowerPoint(`.pptx`) 슬라이드(PDF·Excel·한글·노트북 제외). 빈 문자열에도 일치하는 정규식은 거부 |
| **Word·PowerPoint 찾아 바꾸기** | 본문 글자만. 문단 추가·삭제나 글꼴 바꾸기는 안 됨. 머리말·꼬리말·각주·발표자 노트는 설정에서 켤 때만, 메모·차트·도형 안 글자와 슬라이드 마스터·레이아웃·자동 슬라이드 번호는 언제나 제외. 암호·내용 컨트롤 연결·40MB 초과 문서는 제외되고, 변경 이력이 켜진 문서는 기본 제외(설정에서 허용 가능 — 바꾼 내용이 Word 변경 이력에 남지 않음). 탭·줄바꿈을 넘나드는 일치는 건너뜀. **PowerPoint 설치 PC에서 PDF로 변환되어 열린 `.pptx`는 대상 아님** |
| **맞춤법 검사** | 오프라인 전용. 한국어 사전이 3MB가 넘어 **처음 검사할 때 한 번 불러옵니다**(못 받아오면 규칙 검사만 동작). 긴 문서는 선택 영역이나 현재 위치 주변만 검사 |
| **최근 검색어** | 이 컴퓨터 안에만 구획별 12개까지. '바꿀 내용'은 기억하지 않음. 설정 → 일반에서 끄거나 지울 수 있음 |

---

## 10. 문제 해결 FAQ

### Q1. EXE를 더블클릭했는데 SmartScreen 경고가 떠요

서명되지 않은 빌드라 정상입니다.

→ "Windows의 PC 보호" 창에서 **추가 정보** → **실행** 클릭

### Q2. 브라우저가 자동으로 안 열려요

EXE가 실행됐는데 브라우저가 안 뜨면:

1. 작업관리자에서 `ClassDock.exe` 프로세스가 살아 있는지 확인
2. 살아 있으면 브라우저에서 `http://127.0.0.1:17645/` 직접 접속
3. 17645가 다른 프로그램에 점유돼 있으면 **다음 고정 후보 포트(18645 · 19645 · 27645 · 37645 · 47645)** 중 하나로 떠 있을 수 있음 → 그 주소로 접속하거나, 콘솔창이 안 보이므로 종료 후 재실행. 한 번 뜬 포트는 기억되어 다음 실행 때 재사용됩니다

### Q3. 즐겨찾기로 접속이 안 돼요

- `start-server-hidden.vbs`가 실행 중인지 작업관리자에서 확인
- 안 떠 있으면 다시 실행
- EXE를 따로 더블클릭한 적이 있으면 중복 인스턴스가 떠서 포트가 어긋날 수 있음 → 모두 종료 후 `.vbs`만 다시 실행

### Q4. Python 실행이 안 돼요 / 너무 느려요

- **브라우저 첫 실행은 Pyodide 다운로드 때문에 30초~1분** 정도 걸립니다. 이후 캐시되어 빨라짐
- 인터넷이 없으면 첫 실행이 실패할 수 있음 → EXE + 로컬 Python 설치 권장
- 로컬 Python을 인식하지 못하면 편집기 도구바의 **Py Env** 클릭해 진단 확인
- 파이썬을 방금 설치했다면 **Py Env → 다시 검사**를 누르면 EXE를 껐다 켜지 않아도 인식됩니다
- PATH에 없어도 레지스트리와 기본 설치 폴더(`%LocalAppData%\Programs\Python`, `Program Files`, `C:\Python3xx`, Anaconda 등)에서 자동으로 찾습니다. 그래도 못 찾으면 기본 위치가 아닌 곳에 설치한 경우이므로 **`Add python.exe to PATH`를 체크해 다시 설치**하세요

### Q5. PowerPoint 파일이 깨져 보여요

- 기본은 PowerPoint 설치 여부와 관계없이 PPTXjs 간이 미리보기로 빠르게 엽니다. 애니메이션·복잡한 도형·차트는 원본과 다를 수 있습니다.
- 정확한 모양이 필요하면 문서 위 **정확한 PDF로 열기**를 누르세요. EXE + 설치된 Microsoft PowerPoint가 필요합니다. 이 버튼은 화면을 막지 않고 변환하며, PowerPoint가 15초 안에 응답하지 않으면 간이 미리보기를 그대로 유지합니다.
- 매번 정확 변환하려면 **설정 → 문서 → "PowerPoint 파일을 열 때 자동으로 정확 변환"** 을 켜세요.

### Q6. 저장한 파일이 안 보여요

- EXE에서 Python·Excel 저장 위치: **`내 문서\ClassDock 저장\`** 폴더
- PDF·이미지 내보내기는 브라우저 기본 다운로드 폴더로 떨어짐
- 편집기 상단에 마지막 저장 경로가 표시됨

### Q7. 작업 표시줄의 EXE 아이콘이 동작 안 해요

- EXE 파일을 다른 위치로 옮기거나 삭제했는지 확인
- 옮겼다면 작업 표시줄 아이콘을 우클릭 → **작업 표시줄에서 제거** 후 새 위치에서 다시 고정

### Q8. PDF 위에 한 필기가 사라졌어요

- PDF 필기는 자동 저장되지만, **다른 작업공간으로 옮기거나 EXE 자동 복원 데이터를 지우면** 함께 사라질 수 있음
- 다운로드한 PDF에는 필기가 그대로 구워지므로, 영구 보존하려면 **다운로드** 버튼으로 저장

### Q9. Python 편집기에서 필기를 켰는데 다시 끄고 싶어요

- 도구바의 **✏️ 필기** 버튼을 한 번 더 누르면 편집 잠금 해제 + 캔버스 OFF
- 필기는 세션 한정이라 문서를 닫으면 자동으로 사라집니다 (저장되지 않음)

### Q10. 지도가 회색으로만 보여요

배경지도는 인터넷에서 받아 옵니다. 표시·메모·저장은 그대로 되지만 배경만 비어 보이는 상태입니다.

1. 인터넷 연결을 확인합니다
2. 연결이 안 되는 교실에서 쓸 지역은, 연결되는 곳에서 **ClassDock EXE로 실제 수업 때 볼 자리와 확대 단계까지 직접 열어** 두세요. 화면에 표시된 타일은 자동 보관되어 다시 방문할 때 인터넷 없이 나옵니다
3. 전에 본 것 같은데도 비어 보이면 **배경지도 종류나 확대 단계가 달라지지 않았는지** 확인하세요 — 일반·지형·흑백·위성은 각각 따로 보관됩니다
4. **🗂️ 오프라인 지도** 버튼이 안 보이면 브라우저에서 HTML을 직접 열어 쓰는 중입니다. 자동 디스크 캐시와 현황·비우기는 런처(EXE)에서만 동작합니다

---

## 더 알아보기

- 전체 기능 목록·릴리스 노트: [README.md](README.md)
- 단축키 전체: 앱 상단 `?` 버튼
- 문제 보고·제안: GitHub Issues
